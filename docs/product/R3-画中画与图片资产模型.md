# R3 画中画与图片资产模型

> 状态：r3_c01_c163_confirmed_compiled；r3_c164_c180_proposed_waiting_confirmation
> 所属路线：R3 画中画与图片资产模型  
> 主责：定义从静态视觉编导、视觉策略、封面设计到图片资产、质检、HTML 嵌入、外部模型降级的字段和动作。
> 边界：本文件是 R3 产品真源之一；C01-C180 已完成对应本地分批编译。C164-C180 的五段视觉语义工作包已在 H2 闭合，并分别于 R7-L3-H3/H4 接入 current 直供与热点 blueprint v0.5；两条 v0.4 只允许历史 replay。本路线仍不生成视频、不做自动剪辑或发布。

<!-- ai-nav:start -->
## AI 阅读导航

- 对象和链路：读 [第 1-2 节](#1-核心对象)。
- 视觉需求、图片数量和文字：从 [核心对象](#1-核心对象)定位 `visual_need_analysis / visual_text_plan`。
- 验收规则：读 [R3 验收口径](#14-r3-验收口径)。
- 最新真实回归与 C81-C90：读文末 [对标后返修结论](#15-对标后返修结论)。
- 最终交付业务语义：读 [P0-H7 最终交付修订模型](#16-p0-h7-最终交付修订模型)。
- 最新画中画槽位、画幅、封面二次开发与消歧合同：读 [画幅、槽位与封面派生合同](#17-画幅槽位与封面派生合同r3-c91-至-c110)。
- 最新全文视觉覆盖与文案对齐合同：读 [全文视觉覆盖合同](#18-全文视觉覆盖合同r3-c125-至-c139)。
- 最新视觉来源分类、Image 2 默认路由与测试 profile 隔离：读第 19 节。
- 基础图、派生成品与唯一交付素材：读第 20 节。
- L3 视觉语义工作包、Prompt、真实看图与通用能力边界：读第 21 节。
<!-- ai-nav:end -->

---

## 1. 核心对象

| 对象 | 技术名 | 位置 | 说明 |
|---|---|---|---|
| 静态视觉编导方案 | `static_visual_director_plan` | `intermediate/05-visual-plan.md` 固定分区 | 从口播文案推导静态图片角色、构图、视觉概念和封面候选 |
| 视觉需求分析 | `visual_need_analysis` | `intermediate/05-visual-plan.md` 固定分区 | 逐语义节点证明是否需要视觉介入；图片数量由通过任务自然派生 |
| 视觉需求候选 | `visual_need_candidate` | `visual_need_analysis.candidates[]` | 记录缺图损失、视觉任务、预期改变、风险和 generate / reject 决策 |
| 视觉文字方案 | `visual_text_plan` | `intermediate/05-visual-plan.md` 固定分区 | 决定画中画是否含字、文字新增什么信息、怎样渲染和是否绑定来源 |
| 视觉方案 | `visual_plan` | `intermediate/05-visual-plan.md` | 从 draft 拆出的画中画策略 |
| 视觉 beat | `visual_beat` | `visual_plan.beats[]` | 一段口播对应的视觉任务 |
| 视觉覆盖账本 | `visual_coverage_ledger` | `intermediate/05-visual-plan.md` 固定分区 | 待确认对象；证明当前全文每个内容节点均已有生成、复用、主播表达、证据阻断或人工素材处置 |
| 文案—视觉对齐审查 | `script_visual_alignment_review` | `intermediate/06-quality-review.md` 固定分区 | 待确认对象；检查当前稿件 revision 与当前视觉 revision 是否一致、重复、冲突或遗漏 |
| 封面设计包 | `cover_design_package` | `intermediate/08-platform-package-draft.md` / `deliverables/final-platform-package.md` | 各平台封面图、封面标题、版式、安全区和质检 |
| 封面合成 | `cover_composition` | `assets/images/covers/` / `deliverables/final-delivery.html` | 把封面底图、封面标题、版式和平台比例合成为可上传成品封面 |
| 图片类型 | `image_asset_type` | `image_task` / `image_asset` / `cover_design_package` | 区分画中画图片和封面图片 |
| 图片生产路径 | `image_production_path` | `image_generation_record` / `image_asset_set` | 区分 Codex 直接出图和非 Codex Seedream 入参交付 |
| 提示词组 | `image_prompt_set` | `visual_plan.image_prompts[]` | 完整 prompt 卡集合 |
| 图片任务 | `image_task` | `visual_plan.accepted_visual_tasks[]` | 一个通过视觉需求门禁、必须进入生成链的一张图任务 |
| 生成记录 | `image_generation_record` | `assets/images/generation-records/` | 一次 provider / 模型 / 参数 / 输出 / 失败尝试 |
| 图片资产组 | `image_asset_set` | `assets/images/image-assets.md` | 图片文件和状态总表 |
| 单张图片资产 | `image_asset` | `image_asset_set.assets[]` | 图片路径、状态、provider、prompt_used |
| 元数据 sidecar | `image_metadata_sidecar` | `assets/images/metadata/` | 图片外置可复现元数据，补足图片内嵌 metadata 不可靠的问题 |
| HTML 嵌入记录 | `html_embed_manifest` | `deliverables/content-delivery-record.md` | 最终 HTML 展示 / 下载 / 追溯配置 |

---

## 2. 标准流程

```text
copywriting-draft-writer
-> talking-head-image-pip 编排 static-visual-director，原子生成 static_visual_director_plan / visual_need_analysis / visual_plan tasks / visual_text_plan
-> image-prompt-compiler 按 image_task_id 生成 image_prompt_set
-> image-asset-producer 执行 Codex 出图 / Seedream 降级 / 画中画确定性叠字，并生成 generation_record / metadata_sidecar / fallback_status
-> copywriting-quality-review(content_visual_review) 做正文 + 视觉联合质检
-> platform-packaging-adapter 生成 platform_package / cover_variant_set
-> cover-design-compiler 生成 cover_design_package / cover_composition
-> copywriting-quality-review(cover_review) 生成 cover_quality_gate
-> final-delivery-builder 嵌入实际图片或降级占位
```

自动推进口径：

```text
static_visual_director_status=director_plan_pass、visual_need_analysis_status=pass、visual_plan_status=visual_plan_pass、visual_text_plan_status=visual_text_plan_pass 且 accepted 任务一一对应 -> 进入 image-prompt-compiler。
prompt_integrity_check 全部 pass，且 accepted 图片任务已有真实资产或诚实 fallback -> 进入 content_visual_review / visual_text_quality_gate。
content_visual_review 通过 -> 进入 platform-packaging-adapter；随后才进入 cover-design-compiler 和 cover_review。
最终交付前 accepted image task 必须 generated 或有明确 fallback 状态。
图片缺失不阻断无图 HTML，但必须在 HTML 中清楚显示。
样本模式未通过前，不进入多篇批量出图。
静态视觉编导层未通过前，不得直接生成 image_prompt_set。
封面设计包缺失时，平台包装可以先给文字包装，但最终交付不得宣称“封面设计已完成”。
```

状态边界：

```text
`image_status` 只用于单张图片资产：generated / pending_external / generation_failed / manual_required / rejected。
`image_assets_status` 用于整组图片资产：all_generated / partially_generated / pending_external / generation_failed / manual_required / mixed / not_required。
```

---

## 2.1 静态图片边界

R3 当前只做静态图片资产：

```text
做：画中画图、证据图、解释图、情绪图、封面图、封面设计包。
不做：动态视频、分镜成片、剪辑时间线、Seedance / Veo / 视频生成 API、自动上传。
```

因此本层不使用完整影视 storyboard 作为主模型，而使用轻量静态视觉编导：

```text
draft
-> static_visual_director_plan
-> visual_plan / cover_design_package
-> image_prompt_set
-> image_asset_set
-> final_delivery
```

图片类型边界：

```yaml
image_asset_type:
  allowed_values:
    - picture_in_picture_image
    - cover_image
```

`picture_in_picture_image` 服务正文画中画；`cover_image` 服务平台入口封面。两者可以复用同一个文件，但必须分别通过对应质量门禁。

封面图片内部再区分：

```text
cover_background_asset：封面底图，可以无字，负责情绪、场景、主体和视觉冲突。
cover_composited_asset：最终成品封面，已经把封面标题、版式和平台比例合成进去，可直接上传。
platform_cover_asset：针对抖音 / 快手 / 小红书 / 视频号等平台裁切或标题策略不同的成品封面。
```

没有 `cover_composited_asset` 时，只能说“封面底图已生成 / 封面设计方案已生成”，不能说“成品封面已完成”。

图片生产路径：

```yaml
image_production_path:
  allowed_values:
    - codex_image2_render
    - seedream_prompt_delivery
    - manual_upload
```

`codex_image2_render` 表示 Codex 环境直接调用内置 image 生成，生成文件并整合进 HTML。
`seedream_prompt_delivery` 表示非 Codex 环境下不调用外部 API，只交付 Seedream 入参、完整 prompt 和外部生成说明。
`manual_upload` 表示用户或设计师人工补图。

---

## 2.2 static_visual_director_plan 字段

```yaml
static_visual_director_plan:
  static_visual_director_plan_id:
  draft_id:
  brief_id:
  topic_id:
  account:
  source_research_run_id:
  content_visual_mode: talking_head_static_assets
  non_goal:
    - video_generation
    - video_editing
    - dynamic_storyboard
  script_pacing_summary:
  opening_static_strategy:
    first_screen_job:
    cover_or_pip:
    viewer_stop_reason:
  visual_role_map:
    - visual_role_id:
      segment_id:
      script_range:
        insert_after_text:
        insert_before_text:
      visual_role: hook_conflict / comparison / mechanism_explainer / evidence_support / emotion_amplifier / scene_context / trust_builder / transition / memory_symbol / metaphor / cover_click / not_needed
      image_asset_type: picture_in_picture_image / cover_image / not_applicable
      viewer_job:
      retention_risk:
      static_image_reason:
      recommended_image_task_type:
  visual_concept:
  composition_strategy:
  style_anchor:
    medium:
    lighting:
    palette:
    texture:
    realism_level:
    people_policy:
    text_policy: no_text_first / visual_text_when_needed / source_native_text
  visual_text_plan_id:
  cover_candidate_policy:
  visual_consistency_rule:
  asset_task_list:
  director_plan_status: director_plan_pass / director_plan_needs_fix / director_plan_blocked
  next_skill:
```

硬规则：

```text
static_visual_director_plan 只负责编导静态画面任务，不写最终 prompt。
visual_role=not_needed 不得生成图片，除非用户明确要求追加。
cover_candidate 不等于正式封面设计；正式封面必须进入 cover_design_package。
text_policy 只表达全局倾向；每张图是否含字必须由 visual_text_plan 给出可审计决策。
```

---

## 2.2.1 visual_text_plan 字段

```yaml
visual_text_plan:
  visual_text_plan_id:
  static_visual_director_plan_id:
  visual_plan_id:
  draft_id:
  source_research_run_id:
  account:
  narration_source_type: draft_script_text
  narration_source_path:
  subtitle_source_status: available / not_available / not_applicable
  subtitle_source_path:
  visual_text_tasks:
    - visual_text_task_id:
      image_task_id:
      beat_id:
      visual_text_decision: forbidden / optional / required
      decision_reason:
      visual_text_units:
        - visual_text_unit_id:
          content:
          visual_text_role: none / inner_voice / role_label / mechanism_label / comparison_label / evidence_label / data_point / context_marker / visual_narration / subtext / source_note
          information_delta:
          speaker_or_target:
          placement:
          max_chars:
          is_source_required:
          evidence_source_type: research_source / source_asset / screenshot_asset / user_provided / not_applicable
          evidence_source_id:
          evidence_source_path:
          source_binding_status: not_required / source_bound / source_missing / source_conflict
          source_display_text:
          visual_text_render_strategy: no_text / model_text_in_image / deterministic_overlay / source_native_text / manual_design / prompt_only
      task_information_delta_summary:
      semantic_delta_score:
      effective_information_gain: low / medium / high
      visual_text_redundancy_status: pass / needs_fix / blocked
      cognitive_load_status: pass / needs_fix / blocked
      overflow_reason:
      source_binding_summary_status: not_required / source_bound / source_missing / source_conflict
      visual_text_task_status: visual_text_task_pass / visual_text_task_needs_fix / visual_text_task_blocked
  required_text_task_count:
  optional_text_task_count:
  forbidden_text_task_count:
  source_bound_task_count:
  plan_information_delta_summary:
  visual_text_plan_status: visual_text_plan_pass / visual_text_plan_needs_fix / visual_text_plan_blocked
  next_skill:
```

三轨边界：

| 信息轨 | 负责 | 不负责 |
|---|---|---|
| `narration_track` | 观点、因果、叙事推进 | 不要求把每句话画成图 |
| `subtitle_track` | 忠实转写、无声观看、可访问性 | 不承担视觉隐喻或第二叙事 |
| `visual_text_layer` | 内心、角色、机制、比较轴、证据、语境、潜台词 | 不机械复写口播或字幕 |

决策算法：

```text
1. 先问图片不含字能否准确完成 viewer_job；能则优先 forbidden。
2. 再问文字是否提供口播和字幕没有提供的新信息；没有则 forbidden / needs_fix。
3. 再问缺字是否会造成角色、机制、比较或证据歧义；会则 required。
4. 再问文字是否涉及数据、引语、截图或事实证据；涉及则必须 source_bound。
5. 最后选择渲染方式：准确性优先时 deterministic_overlay / source_native_text；模型含字失败可降级。
```

当前静态图片链不新增字幕产物。`narration_source_type` 固定为 `draft_script_text`，`narration_source_path` 指向本 session 的 draft；字幕存在时记录 `subtitle_source_path`，不存在时写 `subtitle_source_status=not_available`。字幕不存在仍必须对 draft 口播文本完成重复检查。

### 2.2.2 原子规划事务与事实源

```text
1. talking-head-image-pip 读取 draft / brief，启动 static_visual_planning_bundle。
2. static-visual-director 先分配 static_visual_director_plan_id、visual_plan_id、beat_id、image_task_id、visual_text_plan_id。
3. 同一事务内写 visual_role_map、visual_need_analysis.candidates[]、visual_plan.accepted_visual_tasks[] 和 visual_text_plan.visual_text_tasks[]。
4. 每个 image_task_id 必须恰好对应一个 visual_text_task；forbidden 任务的 units 必须为空。
5. 三个对象全部通过后，image-prompt-compiler 才能按 task 编译 Codex / Seedream prompt。
6. 任一对象失败时保留草稿和 trace，但不得把 bundle 标记为 pass。
```

物理事实源固定为 `intermediate/05-visual-plan.md`，其中分区保存三个对象。第一版不再允许另建 `05-static-visual-director.md / 05-visual-text-plan.md` 的双路径；未来拆文件必须通过迁移版本升级，不能临场选择。

视觉角色默认矩阵：

| visual role | 默认决策 | 推荐文字角色 | 关键门禁 |
|---|---|---|---|
| `emotion_amplifier / scene_context / memory_symbol / metaphor` | `forbidden / optional` | `inner_voice / subtext / context_marker` | 不得冒充事实 |
| `hook_conflict / comparison` | `optional / required` | `role_label / comparison_label / inner_voice` | 角色和比较轴必须清楚 |
| `mechanism_explainer`（内容子型可为 flow） | `required`，除非关系已一眼可懂 | `mechanism_label / visual_narration` | 不可堆满解释文字 |
| `evidence_support`（内容子型可为 data / quote / proof_screenshot） | `required` | `source_native_text / data_point / source_note` | 必须来源绑定、原始资产和隐私可追溯 |

`effective_information_gain` 不等于 raw information density。高质量画中画应让观众在短时间内多理解一层，而不是多读一段字幕。

默认视觉文字预算：

| 文字角色 | 默认预算 |
|---|---|
| role / comparison / context label | 2-8 个汉字 |
| inner_voice / subtext / visual_narration | 4-14 个汉字 |
| mechanism_label | 每节点 2-6 个汉字，默认最多 4 个节点 |
| data / evidence label | 保留准确数字或原生短语，不为凑字数改写事实 |
| source_note | 6-24 个汉字或短来源名，必须手机可读 |

单张图默认 1-3 个文字单元；机制图最多 4 个节点，证据图可额外增加 1 个来源说明。超过预算必须写 `overflow_reason` 并进入认知负荷复检。

评分门槛：`semantic_delta_score / glance_comprehension_score / mobile_readability_score` 均为 1-10 分。optional / required 含字任务默认不得低于 7；5-6 为 needs_fix，1-4 为 blocked。forbidden 任务的 semantic delta 可为空。

---

## 2.3 cover_design_package 字段

```yaml
cover_design_package:
  cover_design_package_id:
  package_id:
  visual_plan_id:
  static_visual_director_plan_id:
  account:
  source_research_run_id:
  platforms:
    - platform:
      cover_title:
      video_title:
      cover_image_source: existing_image_asset / generated_cover / video_frame / manual_required
      source_image_asset_id:
      image_asset_type: cover_image
      cover_asset_role: cover_background_asset / cover_composited_asset / platform_cover_asset
      cover_visual_role:
      cover_visual_concept:
      cover_visual_entry_type: conflict / evidence / emotion / method / trust / result
      cover_job: stop_scroll / curiosity_gap / result_promise / viewpoint_signal / trust_signal
      cover_text_render_strategy: model_text_in_image / deterministic_overlay / manual_design / prompt_only
      cover_background_asset_id:
      cover_composited_asset_id:
      platform_cover_asset_id:
      platform_cover_strategy: reuse / crop / retitle / independent_composition / prompt_only
      cover_composition_status: composition_ready / composition_needs_fix / composition_not_generated / prompt_only
      layout_spec:
        subject_position:
        text_position:
        subtitle_safe_area:
        face_safe_area:
        platform_ui_safe_area:
      aspect_ratio:
      typography_note:
      text_overlay_max_chars:
      cover_prompt_id:
      cover_composition_prompt_id:
      platform_specific_reason:
      brand_system_ref:
      thumbnail_readability_status: pass / needs_fix / blocked
      cover_contract_render_alignment_status: pass / needs_fix / blocked
      platform_preview_status: not_checked / preview_pass / preview_needs_fix / unavailable
      platform_preview_evidence_path:
      cover_quality_gate:
        small_screen_readability: pass / needs_fix / blocked
        title_visual_alignment: pass / needs_fix / blocked
        no_misleading_claim: pass / needs_fix / blocked
        no_privacy_risk: pass / needs_fix / blocked
  package_status: cover_package_pass / cover_package_needs_fix / cover_package_blocked
```

封面设计包规则：

```text
封面标题和视频标题必须分开。
封面底图来源必须清楚：复用已有图、单独生成封面、取视频帧或需要人工设计。
封面成品状态必须清楚：composition_ready / composition_needs_fix / composition_not_generated / prompt_only。
平台可以共用封面图，但必须说明为什么；如果平台需求冲突，应输出不同封面方案。
封面图如果只是一张普通画中画，必须说明是否只是 cover_background_asset，以及需要怎样裁切 / 加字 / 合成。
Codex image 可以生成含字封面候选，但最终成品必须通过文字准确、小屏可读和平台安全区检查。
```

### 2.3.1 cover_composition 字段

```yaml
cover_composition:
  cover_composition_id:
  cover_design_package_id:
  cover_variant_id:
  platform:
  source_background_asset_id:
  cover_title:
  text_lines:
  typography_spec:
    font_role:
    primary_text_size:
    secondary_text_size:
    stroke_or_shadow:
    color:
  layout_spec:
    canvas_size:
    aspect_ratio:
    text_box:
    subject_safe_area:
    platform_ui_safe_area:
  cover_text_render_strategy: deterministic_overlay / model_text_in_image / manual_design / prompt_only
  output_asset_id:
  output_path:
  cover_composition_status: composition_ready / composition_needs_fix / composition_not_generated / prompt_only
  quality_gate_status:
  next_skill:
```

`cover_text_render_strategy` 说明：

```text
deterministic_overlay：默认推荐。由工具 / HTML canvas / 设计软件把文字叠到封面底图上，文字最可控。
model_text_in_image：允许用于短字、海报感强的候选封面；必须保留 prompt 并质检文字准确性。
manual_design：交给设计师或用户手工排版。
prompt_only：非 Codex 或无法合成时，仅交付可复制提示词和版式说明。
```

平台变体规则：

```text
同一内容至少有主推荐 cover_composition。
跨平台发布时，final_delivery 必须展示各平台封面使用策略：共用 / 裁切 / 改标题 / 重新合成。
抖音、快手可共用强冲突竖版封面，除非账号风格不同。
小红书优先说明型 / 笔记型封面，标题可更解释，不强制沿用抖音封面标题。
视频号需检查裁切比例和安全区，必要时输出独立 platform_cover_asset。
```

资产归属和引用规则：

```text
cover_background_asset、cover_composited_asset、platform_cover_asset 都是 image_asset_set.assets[] 的资产角色。
cover_composition.output_asset_id 必须引用其中一个成品资产，不能只写未落盘的逻辑 ID。
cover_composited_asset 是默认主成品；platform_cover_asset 是平台差异确有必要时的变体。
cover_design_package 是策略包，cover_composition 是渲染记录，二者不得互相替代。
```

---

## 3. visual_plan 字段

在字段词典现有 `visual_plan` 基础上，R3 要求补充；返修后 visual_plan 必须引用 static_visual_director_plan：

```yaml
visual_plan:
  visual_plan_id:
  static_visual_director_plan_id:
  draft_id:
  brief_id:
  source_research_run_id:
  account:
  content_duration_estimate:
  visual_need_analysis_id:
  visual_count_policy: content_derived_unbounded
  derived_visual_count:
  first_screen_visual_task:
  visual_strategy:
  visual_concept:
  composition_strategy:
  visual_text_plan_id:
  cover_candidate_ids:
  visual_language:
    color:
    lighting:
    camera:
    texture:
    people_policy:
    product_ui_policy:
  beats:
  accepted_visual_tasks:
  rejected_visual_candidates:
  not_recommended_visuals:
  image_prompt_set_id:
  image_prompts:
  visual_plan_status:
  next_skill:
```

`derived_visual_count` 必须等于 `accepted_visual_tasks.length`，但它是分析结果而不是目标值。允许结果为 0，也不设置最大值。`content_duration_estimate` 只用于时间轴和节奏定位，不得推导图片数量。

旧 `visual_budget / required_visuals / optional_visuals / reduction_reason / expansion_reason / selected_optional_count` 只允许作为历史 session 兼容输入；新产物不得继续写入这些字段。

### 3.1 visual_need_analysis 字段

```yaml
visual_need_analysis:
  visual_need_analysis_id:
  static_visual_director_plan_id:
  draft_id:
  source_research_run_id:
  audience_profile_ref:
  audience_prior_knowledge: novice / mixed / expert / unknown
  platform_viewing_context: mobile_feed / known_audience / other
  visual_count_policy: content_derived_unbounded
  semantic_beats:
    - beat_id:
      script_range:
      beat_purpose:
      viewer_state_before:
      viewer_state_after:
  candidates:
    - visual_need_candidate_id:
      beat_id:
      covered_beat_ids: []
      trigger_text:
      insert_after_text:
      insert_before_text:
      viewer_problem_without_visual:
      attention_risk_without_visual: low / medium / high
      comprehension_risk_without_visual: low / medium / high
      primary_visual_job: attention_reset / hook_amplification / concept_explanation / evidence_support / process_demonstration / emotion_amplification / memory_anchor
      supporting_visual_jobs: []
      expected_viewer_change:
      information_added:
      why_image_is_better_than_talking_head:
      emotion_congruence_status: aligned / not_applicable / misaligned
      evidence_requirement: source_bound / generated_context_only / not_applicable
      redundancy_status: unique / duplicates_narration / duplicates_other_visual
      cognitive_load_risk: low / medium / high
      misleading_risk: low / medium / high
      visual_need_decision: generate / reject
      decision_reason:
  accepted_visual_task_ids: []
  rejected_visual_candidate_ids: []
  derived_visual_count:
  zero_visual_reason:
  visual_need_analysis_status: pass / needs_fix / blocked
```

`beat_id` 是主语义节点，`covered_beat_ids[]` 允许一个任务覆盖多个相邻节点。`generate` 不是成本选择，而是内容与传播判断。通过任务在 Codex 内置 Image 2 可用时全部生成；`reject` 保留审计证据但不得进入 prompt/provider。`attention_reset` 不能由固定秒数单独触发，`emotion_amplification` 必须与当前事件一致，`evidence_support` 必须来源绑定。`derived_visual_count=0` 时必须有 `zero_visual_reason`，并允许整体分析状态为 pass。

---

## 4. visual_beat 字段

```yaml
visual_beat:
  beat_id:
  segment_type:
  script_range:
    insert_after_text:
    insert_before_text:
  estimated_time:
  segment_function:
  retention_risk:
  visual_need: required / optional / not_needed / not_recommended
  retention_task:
  visual_type:
  visual_one_liner:
  no_visual_loss:
  risk_notes:
  image_task_id:
```

规则：

```text
visual_need=required / optional 才能进入 image_task。
visual_need=not_needed / not_recommended 不生成 prompt，除非用户明确要求。
每个 required beat 只能有一个主 retention_task。
每个 required beat 必须能追溯到 static_visual_director_plan.visual_role_map。
```

---

## 5. image_prompt 字段

每个 prompt 必须保留完整 prompt 卡：

```yaml
image_prompt:
  prompt_id:
  image_task_id:
  beat_id:
  visual_text_task_id:
  visual_text_decision: forbidden / optional / required
  visual_text_units:
  image_asset_type: picture_in_picture_image / cover_image
  image_production_path: codex_image2_render / seedream_prompt_delivery / manual_upload
  use_case:
  script_context:
  retention_risk:
  retention_task:
  visual_type:
  why_generate:
  no_visual_loss:
  aspect_ratio:
  quality_level:
  scene:
  subject:
  important_details:
  camera_composition:
  lighting_emotion:
  style:
  constraints:
  negative_prompt:
  allow_text_in_image:
  exact_text_if_any:
  fallback_caption_text:
  provider_notes:
  seedream_payload:
    provider:
    model:
    prompt:
    negative_prompt:
    aspect_ratio:
    width:
    height:
    reference_image_paths:
    text_overlay_policy:
    expected_output:
  acceptance_criteria:
  prompt_integrity_check: pass / fail
```

禁止：

```text
只保存短关键词。
只在聊天里有完整 prompt，落盘时缩水。
实际出图用短 prompt，却在 visual_plan 里伪装成完整 prompt。
```

---

## 6. image_asset_set 字段

```yaml
image_asset_set:
  image_asset_set_id:
  visual_plan_id:
  source_research_run_id:
  account:
  required_count:
  optional_count:
  generated_count:
  pending_count:
  failed_count:
  rejected_count:
  image_assets_status: all_generated / partially_generated / pending_external / generation_failed / manual_required / mixed / not_required
  source_of_truth:
    visual_plan_path:
    prompt_set_path:
    generation_records_dir:
    metadata_dir:
  assets:
    - image_asset_id:
      image_asset_type:
      cover_asset_role: not_applicable / cover_background_asset / cover_composited_asset / platform_cover_asset
      source_cover_composition_id:
      target_platforms:
      image_production_path:
      image_task_id:
      beat_id:
      visual_text_plan_id:
      visual_text_unit_ids:
      source_prompt_id:
      generation_run_id:
      generation_attempt_ids:
      provider:
      model:
      provider_mode:
      input_schema_version:
      asset_path:
      metadata_sidecar_path:
      asset_mime:
      width:
      height:
      aspect_ratio:
      asset_version:
      parent_asset_id:
      supersedes_asset_id:
      insert_after_text:
      insert_before_text:
      prompt_used:
      prompt_used_full_path:
      image_status:
      asset_status_detail:
      failure_reason:
      human_action_required:
      risk_check_notes:
      quality_gate_status:
      static_visual_quality_gate_status:
      visual_text_quality_gate_status:
  artifact_path:
  next_skill:
```

资产不可覆盖规则：

```text
任何图片重做、改 prompt、换 provider、换尺寸或人工替换，都必须生成新的 image_asset_id。
旧资产不得删除正文记录，只能改为 rejected / superseded / archived。
HTML 默认只展示当前通过质检的 asset_version。
```

---

## 7. image_generation_record 字段

每次图片生成或外部待生成都必须写生成记录。即使当前环境不能直接出图，也要写清楚为什么没有生成。

```yaml
image_generation_record:
  generation_run_id:
  generation_attempt_id:
  image_task_id:
  source_prompt_id:
  image_asset_type:
  image_production_path:
  image_generation_decision: render_now / deliver_prompt_only / manual_required
  prompt_delivery_mode: html_copyable_prompt / prompt_card_md / external_model_payload
  provider:
  model:
  provider_mode: codex_builtin / external_api / manual_upload / not_available
  input_schema_version:
  input_payload_path:
  prompt_used:
  negative_prompt:
  aspect_ratio:
  width:
  height:
  seed:
  reference_image_paths:
  started_at:
  finished_at:
  generation_status: generated / pending_external / generation_failed / manual_required
  provider_response_id:
  output_asset_path:
  external_model_payload_path:
  failure_reason:
  retry_suggestion:
  execution_trace_ref:
```

记录原则：

```text
prompt 卡是“设计意图”，generation_record 是“实际尝试”。
实际送给 provider 的 prompt、尺寸、负面词和参考图必须能从 input_payload_path 找到。
失败不是空白，失败也必须成为可恢复记录。
```

---

## 8. image_metadata_sidecar 字段

图片文件自身可能丢失 metadata，或不同 provider 的 metadata 格式不一致。因此 R3 要求每张 generated 图片旁边保留 sidecar。

```yaml
image_metadata_sidecar:
  image_asset_id:
  asset_path:
  source_prompt_id:
  generation_run_id:
  provider:
  model:
  input_schema_version:
  prompt_used_full_path:
  generation_record_path:
  visual_plan_path:
  quality_gate_path:
  created_at:
  checksum:
  license_note:
  privacy_note:
```

sidecar 最小要求：

```text
图片可预览是一回事，图片可追溯是另一回事。
最终 HTML 链接回 sidecar，而不是只链接回图片文件。
```

---

## 9. image_quality_gate

图片进入最终交付前，必须通过以下检查：

| 检查项 | 阻断条件 |
|---|---|
| 文件存在 | `generated` 但 asset_path 不存在 |
| 小屏可读 | 手机竖屏无法看懂主体 |
| 口播贴合 | 图片和 insert 位置语义不一致 |
| 产品边界 | 暗示未实现能力、后台数据、私信、抓取隐私 |
| 版权 / 隐私 | 出现真实个人信息、车牌、手机号、微信号 |
| prompt 追溯 | 找不到 source_prompt_id 或 prompt_used |
| 生成记录 | 找不到 generation_run_id；`generated` 图片找不到 metadata_sidecar_path；pending / failed / manual 找不到生成记录或人工任务说明 |
| 状态诚实 | pending_external 被当成 generated 展示 |
| 本地资源 | HTML 需要展示的图不是 session 本地文件 |
| 静态编导追溯 | 找不到 static_visual_director_plan 或该图没有视觉角色 |
| 视觉一致性 | 同一内容多张图像三张无关素材，缺少统一 style_anchor |
| 质量评分缺失 | accepted 图片缺少 static_visual_quality_gate |
| 类型缺失 | image_asset 缺 `image_asset_type`，无法判断是画中画还是封面 |
| 生产路径缺失 | image_asset / generation_record 缺 `image_production_path` |
| 非 Codex 降级缺 payload | `seedream_prompt_delivery` 没有 Seedream 入参或可复制 prompt |

输出：

```yaml
image_quality_gate:
  image_asset_id:
  quality_gate_status: pass / needs_fix / blocked
  blocking_issues:
  suggestions:
  checked_at:
```

---

## 9.1 static_visual_quality_gate

静态图片质量门禁不只看“文件是否存在”，而是看这张图是否值得进入最终交付。

```yaml
static_visual_quality_gate:
  static_visual_quality_gate_id:
  image_asset_id:
  static_visual_director_plan_id:
  visual_role:
  visual_role_fit_score:
  retention_task_score:
  composition_score:
  mobile_readability_score:
  style_consistency_score:
  prompt_alignment_score:
  misleading_risk: low / medium / high
  privacy_risk: low / medium / high
  quality_gate_status: pass / needs_fix / blocked
  blocking_issues:
  suggestions:
  checked_at:
```

评分口径：

| 分数 | 含义 |
|---|---|
| 9-10 | 可作为成熟样本证据，视觉角色清楚，手机小图可读 |
| 7-8 | 可进入最终交付，仍可人工优化 |
| 5-6 | 只能 pass_with_warnings，不作为成熟样本证据 |
| 1-4 | 不得进入最终交付，必须返工或替换 |

阻断规则：

```text
accepted 图片 visual_role_fit_score < 7，blocked。
accepted 图片 primary_visual_job 完成度 < 7，blocked。
accepted 图片 mobile_readability_score < 7，needs_fix 或 blocked。
misleading_risk=high 或 privacy_risk=high，blocked。
```

---

## 9.1.1 visual_text_quality_gate

```yaml
visual_text_quality_gate:
  visual_text_quality_gate_id:
  visual_text_plan_id:
  image_task_id:
  image_asset_id:
  information_delta_status: pass / needs_fix / blocked
  narration_redundancy_status: pass / needs_fix / blocked
  subtitle_redundancy_status: pass / needs_fix / blocked / not_available
  glance_comprehension_score:
  mobile_readability_score:
  cognitive_load_status: pass / needs_fix / blocked
  source_binding_status: not_required / source_bound / source_missing / source_conflict
  text_accuracy_status: pass / needs_fix / not_applicable
  quality_gate_status: pass / needs_fix / blocked
  blocking_issues:
  recovery_action:
  next_skill:
```

汇总到 `quality_review.visual_text_quality_gate_status` 时使用固定映射：

```text
所有图片任务 pass -> pass
任一 required 任务 needs_fix / blocked -> fail
没有图片任务 -> not_applicable
门禁未执行 -> not_run
```

以下情况直接阻断：

```text
required 文字缺失，或 forbidden 文字被渲染。
视觉文字只是口播 / 字幕复写，没有可说明的 information_delta。
证据、数据、引语、截图没有来源绑定。
生成情境图被标记为 evidence_support。
模型生成文字错误，或手机小图无法读清。
```

恢复动作与路由：

| 问题 | recovery_action | next_skill |
|---|---|---|
| 只是复写口播 / 字幕 | `remove_visual_text / rewrite_information_delta` | `static-visual-director` |
| 证据缺来源 | `bind_existing_source / downgrade_visual_role` | `static-visual-director` |
| prompt 错误要求模型出字 | `recompile_image_prompt` | `image-prompt-compiler` |
| 生成文字错误但底图可用 | `rerender_deterministic_overlay` | `image-asset-producer` |
| 风险无法自动判断 | `request_manual_review` | `human_confirm` |

---

## 9.2 cover_quality_gate

封面进入最终交付前必须检查：

| 检查项 | 阻断条件 |
|---|---|
| 小屏可读 | 手机小图看不清主体或文字 |
| 标题画面一致 | 封面标题和封面图表达不同主题 |
| 误导风险 | 封面暗示没有讲到的价格、收益、内幕或渠道 |
| 平台安全区 | 文字或主体被平台 UI / 字幕遮挡 |
| 封面来源 | 说是封面设计，但只有封面标题，没有图或版式 |
| 隐私 / 版权 | 出现真实车牌、手机号、微信号、平台后台隐私或未授权品牌 |
| 设计-渲染一致性 | 设计包写了主体、色彩、版式或文字层级，但成品没有落实 |
| 平台预览 | 重要内容没有检查平台 UI 覆盖和缩略图阅读效果 |

输出：

```yaml
cover_quality_gate:
  cover_design_package_id:
  cover_composition_id:
  cover_variant_id:
  platform:
  output_asset_id:
  text_accuracy_status: pass / needs_fix / not_applicable
  upload_readiness_status: ready / needs_fix / prompt_only / blocked
  small_screen_readability_score:
  title_visual_alignment_score:
  composition_score:
  platform_fit_score:
  click_reason_clarity_score:
  thumbnail_readability_status: pass / needs_fix / blocked
  cover_contract_render_alignment_status: pass / needs_fix / blocked
  platform_preview_status: not_checked / preview_pass / preview_needs_fix / unavailable
  quality_gate_status: pass / needs_fix / blocked
  blocking_issues:
  suggestions:
  checked_at:
```

---

## 9.3 cover_variant_set

成熟平台不会只依赖一个封面方向。R3 第一版不实现自动 A/B 测试，但要能交付多个封面候选和推荐理由。

```yaml
cover_variant_set:
  cover_variant_set_id:
  package_id:
  source_research_run_id:
  variants:
    - cover_variant_id:
      cover_visual_entry_type: conflict / evidence / emotion / method / trust / result
      cover_variant_difference_type: title_only / crop / visual_concept / subject / composition / evidence / emotion
      platform:
      cover_title:
      video_title:
      cover_image_source:
      source_image_asset_id:
      layout_spec:
      expected_click_reason:
      risk_note:
      cover_quality_gate_status:
  recommended_variant_id:
  recommend_reason:
  materially_distinct_variant_count:
```

规则：

```text
普通内容至少有主推荐封面。
信息密度高、强观点、强争议、跨平台发布的内容，建议给 2-3 个 cover_variant。
cover_variant 不是多写几个标题，而是不同视觉入口：冲突型、证据型、情绪型、方法型、信任型。
只换标题的候选必须标记 title_only；强观点、信息密度高或跨平台内容至少有 2 个非 title_only 视觉假设。
cover_variant_set 由 platform-packaging-adapter 先产，因此引用上游 package_id；cover_design_package_id 只允许作为下游兼容回链，不得作为前置必填。
```

---

## 9.4 asset_trace_quality_gate

资产追溯质量门禁负责判断图片是否可复盘：

```yaml
asset_trace_quality_gate:
  image_asset_set_id:
  checked_assets:
    - image_asset_id:
      prompt_trace_status: pass / fail
      generation_record_status: pass / fail
      metadata_sidecar_status: pass / fail / not_applicable
      html_embed_status: pass / fail
      checksum_status: pass / fail / not_run
  quality_gate_status: pass / needs_fix / blocked
```

---

## 9.5 发布后数据回流待办

本轮 R3 不开发发布后数据回流，不定义 `post_publish_feedback_gate` 为本轮必做对象。

未来增强模块可考虑以下人工字段，但不进入本轮字段词典和 skill 编译：

```yaml
post_publish_feedback_backlog:
  feedback_id:
  delivery_id:
  platform:
  published_at:
  cover_variant_used:
  video_title_used:
  manual_feedback:
  view_signal: strong / normal / weak / unknown
  click_signal: strong / normal / weak / unknown
  comment_signal: strong / normal / weak / unknown
  save_or_share_signal: strong / normal / weak / unknown
  next_iteration_note:
```

---

## 10. html_embed_manifest

最终 HTML 嵌入图片时必须有：

```yaml
html_embed_manifest:
  final_delivery_id:
  image_asset_set_id:
  visual_text_plan_id:
  visual_text_quality_gate_status:
  embeds:
    - embed_id:
      image_asset_id:
      display_mode: image / placeholder / prompt_card / rejected_hidden
      display_path:
      download_path:
      source_prompt_path:
      metadata_sidecar_path:
      generation_record_path:
      visual_text_decision:
      visual_text_summary:
      visual_text_source_links:
      insert_after_text:
      insert_before_text:
      status_label:
      human_note:
  cover_embeds:
    - platform:
      cover_composition_id:
      platform_cover_strategy: reuse / crop / retitle / independent_composition / prompt_only
      cover_background_asset_id:
      cover_composited_asset_id:
      platform_cover_asset_id:
      display_asset_id:
      display_path:
      download_path:
      cover_title:
      video_title:
      cover_composition_status:
      quality_gate_status:
      prompt_path_if_not_generated:
      trace_path:
```

展示规则：

```text
generated：展示图片预览和下载。
pending_external：展示占位、可复制 prompt、插入位置。
generation_failed：展示失败原因和重试建议。
manual_required：展示人工设计任务。
rejected：默认不展示，只在追溯区保留。
外部链接图片：不展示为最终图，只能作为 reference_note；必须落本地资产后才能 generated。
cover_embeds：按平台展示成品图和下载；只有底图时明确标注“非成品”，prompt_only 时展示可复制 prompt 和版式说明。
画中画主卡保持好读：有字图展示可复制视觉文字、角色和必要来源；无字图不展示空字段。更细的逐单元决策链接回 `intermediate/05-visual-plan.md`。
```

---

## 11. 外部模型降级

R3 只定义兼容，不实现 API。

```yaml
fallback_provider_contract:
  provider:
  model:
  input_schema_version:
  required_inputs:
    - prompt_card
    - aspect_ratio
    - negative_prompt
    - allow_text_in_image
    - reference_image
  optional_inputs:
    - seed
    - style_reference
    - quality_level
  output_expected:
    - image_file
    - provider_response_id
    - generation_status
    - provider_metadata
  user_needs_to_provide:
    - provider_choice
    - model_version
    - api_key_or_manual_upload
    - cost_quality_preference
```

禁止让用户重复输入：

```text
选题、口播、账号定位、产品边界、热点来源、完整提示词。
```

---

## 12. 样本模式与批量边界

R3 采用 dbskill 的“先样本、再批量”思想。即使用户要求多篇一起做，图片资产链也必须先在单篇样本上跑通。

```yaml
r3_sample_gate:
  sample_scope: one_content_minimum_asset_chain
  required_outputs:
    - visual_beat
    - prompt_card
    - generation_record
    - image_asset_or_honest_fallback
    - metadata_sidecar_if_generated
    - html_embed_manifest
  pass_condition:
    - no_missing_required_field
    - status_honest
    - local_path_valid_or_pending_clear
    - prompt_traceable
  batch_allowed: true / false
```

批量限制：

```text
样本不过，不开多篇出图。
样本可以固定验证 1 张 accepted 图的最小资产链；正式内容必须先完成 visual_need_analysis，再生成所有通过任务。样本的固定数量不得反推正式内容数量。
多篇任务必须按 R2 child session 隔离 image_asset_set。
fan-in 只汇总状态和路径，不混放不同账号或不同作品的图片。
```

---

## 13. R3 操作合同

| 操作 | 输入 | 输出 | 状态变化 | 失败处理 |
|---|---|---|---|---|
| `analyze_visual_needs` | draft、brief、account audience、semantic beats | visual_need_analysis | `visual_need_analysis_status=pass / needs_fix / blocked` | 无法说明缺图损失或预期改变则 reject；关键段分析缺失则 needs_fix |
| `build_visual_beats` | draft、brief、visual_need_analysis | beats / accepted_visual_tasks | `visual_beats_ready` | 允许 0 张；不得因首屏无图或时长触发补图 |
| `build_prompt_set` | beats、visual_language | image_prompt_set | `prompt_set_ready` | prompt_integrity_check fail |
| `record_generation_attempt` | prompt_card、provider_profile、input_payload | image_generation_record | `generation_record_ready` | 失败也落 generation_failed |
| `decide_image_production_path` | environment_capability、image_task | image_generation_decision / image_production_path | `production_path_ready` | 非 Codex 走 seedream_prompt_delivery |
| `materialize_image_asset` | generation_record、provider_output / fallback_status | image_asset | `image_status=generated / pending_external / generation_failed / manual_required` | 写 fallback_status |
| `write_metadata_sidecar` | image_asset、generation_record、visual_plan | metadata_sidecar | `metadata_sidecar_ready` | generated 缺 sidecar 则 blocked |
| `version_image_asset` | old_asset、new_asset_reason | new image_asset_id | `asset_version_incremented` | 禁止覆盖旧图 |
| `check_image_quality` | image_asset、visual_plan、draft | image_quality_gate | `quality_gate_status` | blocked 不进 HTML |
| `build_static_visual_director_plan` | draft、brief、segment_map | static_visual_director_plan | `director_plan_pass` | 角色不清回 draft / brief |
| `build_visual_text_plan` | draft、visual_plan.accepted_visual_tasks[]、subtitle source、source refs | visual_text_plan.visual_text_tasks[] | `visual_text_plan_pass` | accepted 任务未一一对应、无信息增量或证据缺来源则 blocked |
| `build_cover_design_package` | platform_package、visual_plan、image_asset_set | cover_design_package | `cover_package_pass` | 只有封面标题则 needs_fix |
| `compose_cover_asset` | cover_design_package、cover_background_asset、platform_spec | cover_composition、cover_composited_asset / platform_cover_asset | `composition_ready` | 文字错误、无成品文件或安全区失败则 needs_fix |
| `check_cover_quality` | cover_design_package、platform_package | cover_quality_gate | `cover_quality_gate_status` | blocked 不进最终交付 |
| `check_static_visual_quality` | image_asset、static_visual_director_plan | static_visual_quality_gate | `static_visual_quality_gate_status` | 低于 7 分回 visual / prompt |
| `check_visual_text_quality` | visual_text_plan、draft、subtitle source、image_asset、source refs | visual_text_quality_gate | `visual_text_quality_gate_status` | 重复字幕、来源缺失或文字错误则返工 |
| `build_cover_variant_set` | cover_design_package、platform_package | cover_variant_set | `cover_variant_set_ready` | 普通内容可只给主推荐 |
| `build_html_embed_manifest` | image_asset_set、delivery_record | html_embed_manifest | `embed_ready` | 缺图显示占位 |

---

### 13.1 产品验收 fixture

Skill 编译前必须把以下九类场景写成脱敏 fixture，并给出预期状态与恢复动作：

| fixture | 核心断言 |
|---|---|
| 纯场景无字 | `forbidden`、units 为空、图片仍可正常生成 |
| 内心想法 | `optional`、inner_voice / subtext、有信息增量、不复写口播 |
| 冲突对比 | 角色含糊时 `required`、comparison / role label |
| 机制解释 | 关系不清时 `required`、最小节点文字、认知负荷通过 |
| 来源证据 | 每个证据单元具备 source type / id / path，且 `is_source_required=true + source_bound` |
| 生成图冒充证据 | blocked，要求降级角色或更换真实来源 |
| 字幕不可用 | `subtitle_source_status=not_available`，仍检查 draft 重复 |
| 模型文字失败 | 降级 deterministic overlay / prompt_only，状态诚实 |
| 封面只换标题 | `title_only`，不计入 `materially_distinct_variant_count` |

---

## 14. R3 验收口径

R3 产品层通过标准：

```text
图片数量不是看心情或看时长，而是 visual_need_analysis 的派生结果；允许 0 到 N、不设上限。
图片生成不是内容语言直译，必须先有静态视觉编导方案。
图片资产先分画中画图片和封面图片，再决定生产路径。
每张 accepted 图都有 primary_visual_job、viewer_problem_without_visual、expected_viewer_change、insert_position、prompt_card、provider_profile、image_status。
每张 accepted 图都有 image_asset_type、image_production_path、image_generation_decision。
每张 accepted 图都有 visual_role 和 style_anchor 追溯。
每张图都有 visual_text_decision；含字图能说明 information_delta、文字角色和渲染策略。
视觉文字与字幕分轨；证据文字有来源绑定，生成情境图不冒充证据。
质量目标是有效信息增量，不是原始文字和元素数量。
每张 accepted 图都有 static_visual_quality_gate，且核心分数达标。
封面设计包不是封面标题，必须包含封面图来源、版式、安全区和平台差异。
封面设计包有 cover_quality_gate；重要内容有 cover_variant_set 和推荐理由。
重要封面变体具有实质视觉差异；封面设计合同、成品渲染和平台预览能互相核对。
generated / pending_external / generation_failed / manual_required / rejected 状态不会混用。
每次生成尝试都有 generation_record。
generated 图片有本地 asset_path 和 metadata_sidecar_path。
重做图片不会覆盖旧资产。
样本模式通过前不进入批量图片生产。
final_delivery.html 知道每张图怎么展示、下载和追溯。
发布后数据回流暂不开发；只作为 backlog，不阻断本轮 R3。
Codex 环境 final delivery 优先生成实际图片并嵌入 HTML；非 Codex 环境交付 Seedream 入参，不假装有图。
外部模型降级只保留兼容字段，不误称已实现。
```

---

## 15. 对标后返修结论

```text
R3 必须把图片资产当成“可恢复运行记录”，不是简单附件。
R3 必须区分 prompt_card、generation_record、image_asset、metadata_sidecar、html_embed_manifest。
R3 必须把样本模式写入产品层，否则多篇并行时图片链最容易串台。
R3-C01 到 R3-C90 已完成确认和对应编译。C71-C80 已进入内容驱动视觉合同；C81-C90 已进入字段、Schema、Skill / CONTRACT、可靠性 fixture、checker 与 AGENTS 编排。真实 H6A-D 已 `pass_with_warnings`。
R3-VN-H2 进一步封闭自动接续语义：H6A pass 后自动进入 H6B，accepted task 不存在人工确认门禁；审美意见只在首轮生成后作为版本化返工输入。
P0-H6 已在私有 session `PRIVATE-REGRESSION-REDACTED` 完成真实验证：8 accepted / 8 次 Image 2 / 8 张选中 PIP / 3 张确定性封面，30/30 `pass_with_warnings`。运行模型档位不可观察，发布与传播效果未测试。
R3-C81-C90 继续补齐真实运行可靠性：provider outcome 先于后处理持久化；中断 reconcile-first；completed 状态单调；checker 只读；数量从当前 analysis / selection 派生；overlay 输出可审计 layout sidecar。
```

---

## 16. P0-H7 最终交付修订模型

### 16.1 定位与真源

`final-delivery.html` 是创作者的发布执行工作台，不是新的业务事实源。事实与投影关系固定为：

```text
draft / visual_need_analysis / visual_plan / visual_text_plan
+ quality reviews / image_asset_set / platform_package / cover_composition
-> delivery_revision（绑定 source ID + digest）
-> official typed render input（当前 revision 的官方投影）
-> final-delivery.html / final-script.md / final-visual-plan.md / final-platform-package.md
```

`content_delivery_record` 必须登记当前 `delivery_revision_id` 和当前 source IDs。candidate 只允许作为待编译输入；不能因为 candidate 某个局部被替换，就让 final Markdown 和 content delivery record 留在旧 revision。

建议字段：

```yaml
delivery_revision:
  delivery_revision_id:
  session_id:
  revision_no:
  revision_status: current / superseded / blocked
  supersedes_delivery_revision_id:
  source_artifact_bindings:
    - artifact_type:
      artifact_id:
      sha256:
  generated_view_paths:
  semantic_gate_status:
```

### 16.2 平台交付单元

平台交付不再由“平台文字卡”和“封面图片卡”分别展示后让用户自己猜对应关系。每个平台形成一个 `platform_delivery_unit`：

```yaml
platform_delivery_unit:
  platform:
  cover_title:
  rendered_cover_text:
  cover_asset_id:
  cover_asset_path:
  cover_composition_id:
  cover_quality_gate_id:
  video_title:
  publish_description:
  hashtags:
  publish_readiness:
```

约束：

```text
cover_title 与 rendered_cover_text 规范化后必须一致。
cover_asset_path 必须存在且 hash 与 asset 记录一致。
共享一张封面成品时，共享平台必须使用相同 rendered_cover_text。
某个平台标题不同，就必须重新合成对应封面成品，不能只改文字卡。
任一项错配时 publish_readiness=blocked。
```

### 16.3 画中画执行卡

每张 generated / reused_verified PIP 的用户层最少展示：

```text
顺序。
插在“哪句话之后、哪句话之前”。
图片预览和下载。
这张图解决什么观看问题。
画面上真实可见的文字；无字图明确写无字，不展示空字段。
非阻断风险或使用注意。
```

折叠审计层展示：

```text
image_task_id / asset_id / source IDs。
完整 prompt 与 prompt digest。
generation record、metadata sidecar、来源绑定和 hash。
visual_text_role / information_delta / quality gate。
```

`trigger_text` 只用于识别语义节点，不能替代位置。`insert_after_text` 与 `insert_before_text` 是新交付的必填字段；只有真实音频或时间线存在时，才允许附加 `time_range`，不得凭预计时长伪造时间码。

### 16.4 Warning 聚合

最终交付 warning 不是字符串数组的最后一次覆盖，而是全链聚合对象：

```yaml
delivery_warning_item:
  warning_code:
  warning_category: research / copy / visual / cover / publishing / runtime_scope
  severity: blocking / non_blocking / known_scope
  user_message:
  impact:
  recommended_action:
  source_artifact_id:
  resolution_status: open / resolved / accepted
```

派生规则：

```text
任何 blocking warning -> delivery_readiness=blocked。
只有 non_blocking / known_scope -> ready_with_warnings。
resolved 不进入用户首屏，但保留审计记录。
下游重跑只能新增、关闭或确认 warning，不能用新的 warning_codes 数组覆盖上游开放项。
用户层只展示 user_message / impact / recommended_action；raw code 默认折叠。
```

### 16.5 时长与图片数量

时长和图片数量继续分离：

```text
图片数量：由 visual_need_analysis 派生，0 到 N，不设上限。
口播时长：用于用户拍摄和剪辑计划，不作为图片配额。
```

新交付建议字段：

```yaml
delivery_duration_estimate:
  duration_estimate_status: measured / derived_range / not_available
  measured_duration_seconds:
  estimated_duration_min_seconds:
  estimated_duration_max_seconds:
  speech_rate_profile:
  derivation_method:
  source_text_digest:
```

账号历史实测或已登记、可追溯的语速 profile 优先。没有校准来源时写 `duration_estimate_status=not_available` 和原因，不输出伪精确区间；禁止把 fixture 固定秒数或未经验证的全局语速常量带入真实内容。

### 16.6 业务语义门禁

最终交付 checker 必须从结构检查升级为跨产物语义检查，至少覆盖：

```text
所有派生视图属于同一 delivery revision。
content delivery record 使用当前 revision 的 draft / quality / image / cover / platform IDs。
平台文字与真实封面文字、平台、路径一一对应。
PIP 数量、顺序和 exact placement 与当前 visual plan 一致。
全链 warning 没有被覆盖或遗漏。
阶段标签来自 session context，不允许固定 H5 / H6 文案。
时长有测量或派生依据。
用户层不泄漏 raw enum、模板合同和无意义空分类。
```

P0-H7 编译完成后必须重建 `PRIVATE-REGRESSION-REDACTED`，旧 HTML 和旧 final Markdown 作为 superseded revision 保留或归档，不允许手工分别修到“看起来一致”。

### 16.7 物理提交点、版本与失败语义

多文件交付无法依赖普通文件系统提供跨文件事务，因此产品合同把 `delivery-revision.json` 定义为最后写入的 commit marker：

```text
1. candidate 使用 revision_status=preparing。
2. compiler 校验 source binding、warning union、platform binding 和 exact placement，官方 input 写 revision_status=compiled。
3. renderer 先生成并校验 HTML、三个 final Markdown 和 content delivery record。
4. 所有输出存在且 hash 闭合后，最后写 deliverables/p0/delivery-revision.json，revision_status=current。
5. commit marker 缺失、任一输出 hash 不符或语义门禁失败时，session 不得 completed；旧 current revision 继续有效。
```

不兼容必填字段升级固定使用：

```text
typed_components_v0.3
final-delivery-template-v0.3
final-delivery-renderer-v0.3
p0-contract-bundle-v0.3
```

v0.2 只允许历史 replay / 审计和原 renderer 复现；需要进入 H7 工作台时必须生成新的 v0.3 revision，不原地改写旧 receipt 或历史 event。

平台封面文字规范化只允许：统一换行分隔符、移除分隔符两侧空白、统一全半角空格。不得删除或改写汉字、数字、否定词和标点语义；规范化后仍不一致即 blocked。

warning 唯一键使用 `warning_code + source_artifact_id`。同一键只能更新 resolution 状态；不同来源的同码 warning 不得互相覆盖。

## 17. 画幅、槽位与封面派生合同（R3-C91 至 C110）

### 17.1 触发问题与产品归因

一次私有真实运行暴露了一个典型 false success：Image 2 基础图是 1672×941 的 16:9 横图，封面合成器用居中 `cover` 裁切生成 1080×1440 和 1080×1080 文件；按几何计算，竖封面只保留源图约 42.2% 的横向范围，方形封面只保留约 56.3%，但机器只核对尺寸、文件、标题和 hash，便把三个平台都记为 `pass`。实际 HTML 中竖封面出现构图截断、主体关系丢失和非意图黑区，用户人工验收不通过。真实账号名和 session 标识只保存在私有生产区。

因此产品归因不是“横图不能用于竖屏”，而是：

```text
系统把视频画布、画中画素材、画中画槽位和封面画布混成了一个比例概念；
把文件尺寸正确误当成业务视觉正确；
把正文画中画像素复用误当成封面已经设计；
没有记录焦点、保护区、裁切窗口、损失和真实平台预览。
```

历史 `cover_quality_gate=pass` 只保留为旧合同的机器语义结果，不再代表该真实交付可发布。当前交付应标记为 `human_review_rejected_cover / superseded_pending_recompile`，不得通过改文案掩盖封面失败。

### 17.2 成熟项目对标结论

成熟工具和研究没有把“居中裁切成功”当成最终质量：

| 对标 | 可复用做法 | 本项目取舍 |
|---|---|---|
| [TikTok Creative Best Practices](https://ads.tiktok.com/help/article/creative-best-practices?lang=en&redirected=1) | 原生 9:16、至少 720P、内容位于 UI safe zone | 抖音主发布画布默认绑定 9:16 和版本化安全区；平台相近不等于规格完全相同 |
| [Adobe Premiere Auto Reframe](https://helpx.adobe.com/premiere/desktop/add-video-effects/commonly-used-effects/auto-reframe-overview.html) | 先选目标比例，再跟踪主体并允许人工微调 | 先定目标画布 / 槽位，再决定生成或适配；复杂构图必须进入人工预览 |
| [Apple Final Cut Pro Smart Conform](https://support.apple.com/en-ie/guide/final-cut-pro/ver26664d93f/mac) | 为方形 / 竖形版本重新构图 | 平台 rendition 是独立交付物，不把一个源文件直接冒充全平台成品 |
| [Cloudinary Automatic Gravity](https://cloudinary.com/documentation/image_automatic_gravity) | 内容感知焦点、可用自定义坐标覆盖，并可返回建议裁切 | 自动焦点只做建议；人物、文字、图表节点等保护区必须可显式覆盖 |
| [Sharp resize](https://sharp.pixelplumbing.com/api-resize/) | `cover / contain / fill` 与 position、attention、entropy 分开配置 | 适配方式和焦点是合同字段；禁止把默认 center 当成产品判断 |
| [Adobe Research 自动裁图](https://research.adobe.com/publication/automatic-image-cropping-using-visual-composition-boundary-simplicity-and-content-preservation-models/) | 同时考虑构图、边界简洁和内容保留，避免切断对象 | 门禁必须检查内容保留和构图损失，不只检查输出尺寸 |
| [Instructor presence 系统综述](https://pmc.ncbi.nlm.nih.gov/articles/PMC9786415/) | 出镜者会改变注意分配，效果依知识类型、复杂度和呈现方式而异 | 画中画不是固定图片形状；是否保留主播、主播占位大小和素材位置是注意力编排决定 |

对本项目的核心推论：`PIP` 应从“图片资产类型”升级为“主视频中两个视觉信息源如何同时存在的展示决策”。解释图可能全屏替换主播，也可能与主播并列；新闻截图可能是证据卡片；情绪图可能只做背景。素材比例必须由展示任务和槽位反推，而不是每轮由 agent 临时写进 prompt。

### 17.3 四种画布和六种展示方式

必须分开记录：

```yaml
video_canvas:          # 最终视频画布，如 douyin-feed 9:16
visual_asset_canvas:   # Image 2 或截图产出的源素材画布
placement_slot:        # 素材在视频画布中的归一化矩形与安全区
cover_canvas:          # 平台封面 / 主页网格 / 分享卡等独立展示面
```

`visual_presentation_mode` 至少支持：

| 枚举 | 业务含义 | 比例原则 |
|---|---|---|
| `full_frame_replace` | 解释图 / 情绪图临时全屏替换主播 | 优先生成目标视频原生比例，本账号抖音主画布通常为 9:16 |
| `speaker_plus_visual` | 主播与解释图同时存在的真正画中画 | 按显式槽位比例生成；横图只有槽位能完整承载时才成立 |
| `split_screen` | 上下或左右并列对比 | 两侧保护区、字幕区和分割方式必须提前定义 |
| `floating_card` | 浮层卡片、数据点、步骤图 | 可使用 1:1 / 4:3 等卡片比例，但不能遮挡主播脸、手势和字幕 |
| `source_evidence_card` | 新闻网页 / 数据 / 引语截图证据卡 | 先裁出可读证据区域，再放入竖屏证据版式；不得把整页缩成看不清的小图 |
| `background_plate` | 情绪 / 场景背景，主播或文字在前景 | 重要区域服务于前景留白，不要求所有源画面都完整保留 |

`placement_slot` 以 0 到 1 归一化坐标记录 `x / y / width / height / z_order`，并绑定 `speaker_region / caption_safe_area / platform_ui_safe_area / protected_regions`。本项目当前只生成剪辑执行卡，不自动合成视频，但这些字段决定图片应生成什么比例，也让后续剪辑有明确依据。

### 17.4 R3-C91 至 C110 产品合同

| 编号 | 产品合同 |
|---|---|
| R3-C91 | `video_canvas / visual_asset_canvas / placement_slot / cover_canvas` 是四个独立对象，禁止复用一个 `aspect_ratio` 代表全部展示面。 |
| R3-C92 | 每个 accepted 视觉任务必须先选择 `visual_presentation_mode`，再派生素材比例和构图；比例不能由 provider 随机决定。 |
| R3-C93 | 画中画执行卡必须给出归一化槽位、层级、主播区、字幕区、平台 UI 安全区和保护区；未定义槽位时不得宣称剪辑就绪。 |
| R3-C94 | 抖音主发布视频默认采用账号快照绑定的 9:16 profile；`full_frame_replace` 优先原生 9:16，横图只允许进入能完整承载它的显式卡片 / 分屏槽位。 |
| R3-C95 | 目标比例、目标宽高、展示方式、槽位 ID 必须结构化下沉到 accepted task、prompt card、provider attempt、sidecar、asset rendition 和 H7 执行卡；prompt prose 不能代替机器字段。 |
| R3-C96 | 一个 `visual_need` 可以只有一个创意母资产，但拥有多个 `asset_renditions[]`；图片需求数仍是内容驱动 0 到 N，平台 rendition 数不得冒充新增视觉需求数。 |
| R3-C97 | 封面是独立的点击承诺与入口视觉任务，不是正文 PIP 的默认派生物。可以复用创意概念或母资产；只有独立封面门禁通过时，才允许复用同一像素文件。 |
| R3-C98 | `cover_adaptation_strategy` 仅允许 `reuse_native / focal_crop / fit_pad / outpaint_extend / independent_generation / manual_required`；源目标比例明显不同时，盲目 `center_crop` 不得通过。 |
| R3-C99 | 每个封面 rendition 必须记录 source/target 尺寸与比例、fit mode、焦点、保护区、crop window、保留对象、留白意图、transform digest 和预览证据。 |
| R3-C100 | 自动 attention / entropy / saliency 只提供裁切建议；多人物、图表节点、文字和证据区域必须支持显式焦点 / 保护区覆盖。 |
| R3-C101 | 平台 display profile 版本化，分别记录 feed 视频、封面 / 主页网格、分享面和 UI 安全区；未获可靠规格或真实端预览时写 `not_verified`，不能拍脑袋写 pass。 |
| R3-C102 | 安全区在生成 / 合成之前参与构图，不在成品后补检查；主播脸、手势、字幕、标题、账号视觉和重要证据互相冲突时进入 revise。 |
| R3-C103 | `composition_ready` 只表示工具产物存在。业务 `cover_quality_gate` 还必须检查焦点保留、裁切损失、标题与画面一致、手机缩略图可读性、留白是否有意、重要对象是否被切和整体点击承诺。 |
| R3-C104 | 每个平台封面都要生成真实栅格预览证据；HTML/CSS 无溢出不能代替平台封面预览。主平台缺预览证据时最终交付最多为 `waiting_visual_review`。 |
| R3-C105 | 失败码至少覆盖 `ratio_contract_missing / destructive_crop / focal_subject_clipped / multi_subject_loss / unintentional_empty_region / title_safe_area_conflict / platform_preview_missing / cover_visual_job_mismatch`。 |
| R3-C106 | 若 Image 2 当前入口没有结构化尺寸参数，完整 prompt 必须显式说明比例 / 方向 / 构图，返回后读取真实宽高核验；比例错则新建 revision 重新生成或走显式适配，不得只凭“看起来差不多”接受。 |
| R3-C107 | 母资产和派生 rendition 必须有 parent ID、rendition ID、裁切 / 适配参数、transform digest、输出 hash；禁止覆盖母资产或暗改已完成 rendition。 |
| R3-C108 | 最终 HTML 必须显示实际平台封面预览、适配策略和视觉 warning；主平台封面失败时不得显示“可上传 / 可发布”。 |
| R3-C109 | H7 finalize 要求每个主发布平台的当前 `cover_quality_gate=pass` 且预览证据有效；路径、标题、尺寸和 hash 的语义 pass 不能覆盖视觉 fail。次要平台可按账号策略标记 `waiting_visual_review`，但必须明确不就绪。 |
| R3-C110 | 该私有真实运行保留 H7 v0.3 semantic `pass_with_warnings` 作为历史事实，同时追加人工封面 `fail`；当前 revision 不可发布。用户确认本批后，旧封面合同登记 `superseded_pending_recompile`，再进入六层编译和新 revision 回归。 |

### 17.5 封面决策顺序

```text
先确定平台展示面和点击承诺
-> 选择独立封面视觉任务
-> 判断能否复用母资产的“创意概念”
-> 为目标画布选择 reuse / crop / pad / outpaint / independent generation
-> 记录焦点、保护区和裁切窗口
-> 生成真实平台缩略图预览
-> 人工 / AI 视觉验收
-> 通过后才进入 H7 可发布状态
```

复用优先级不是“省调用”，而是“同一视觉概念能否在目标展示面保持点击承诺”。Codex 内置 Image 2 不设成本或调用次数门禁；当独立竖封面更能保证质量时，应直接生成独立竖封面。

### 17.6 编译影响与当前停点

用户确认 R3-C91 至 C124 后，编译必须同步覆盖：

```text
交接物字段词典
R3 typed schema / contract bundle version
static visual director / prompt compiler / image producer
cover design compiler / deterministic composer
cover visual checker / positive and negative fixtures
H7 typed input / renderer / finalization gate
真实账号新 revision 回归与桌面 / 移动封面预览
```

### 17.7 可编译性反审计与消歧（R3-C111 至 C124）

R3-C91 至 C110 的方向正确，但首次反审计发现以下内容仍可能被不同 producer / checker 作出不同解释：

```text
PIP 是否包含全屏替换；
9:16 是字符串、比例还是实际像素；
槽位坐标从哪里起算；
中心裁切何时算破坏性裁切；
“平台预览”是本地模拟图还是真实 App 截图；
AI / 人工视觉审核由谁执行、机器能否自己写 pass；
主平台通过、次平台待审核时整个交付算什么状态；
同一封面能否跨平台复用；
旧 v0.3 如何回放、当前 revision 如何恢复。
```

这些问题不改变用户已经确认的业务方向，按质量优先、抖音主发、次平台同步、无成本门禁和不自动发布的既有口径消歧如下：

| 编号 | 无二义性合同 |
|---|---|
| R3-C111 | 用 `visual_insert` 统称插入主视频的静态视觉；只有 `speaker_plus_visual` 才是狭义 PIP。`full_frame_replace` 不能再以 `picture_in_picture` 名义交付。v0.3 的 `pip_card` 仅历史兼容。 |
| R3-C112 | 所有槽位和安全区使用目标 `video_canvas` 左上角为原点的 0–1 归一化坐标：`x` 向右、`y` 向下，`width / height > 0`，且矩形不得越界。像素换算必须记录 renderer 规则和结果。 |
| R3-C113 | 比例使用约分后的整数 `ratio_width / ratio_height`，另记 `orientation`；真实资产另记 `actual_width_px / actual_height_px`。禁止只存 `9:16` prose。原生兼容按整数交叉乘与至多 1 像素舍入误差判定。 |
| R3-C114 | `platform_surface_profile` 必须包含 profile ID、version、platform、surface role、target canvas、safe areas、priority 和规格证据状态。账号快照绑定 profile 版本，不从平台名临时猜尺寸。 |
| R3-C115 | 预览证据分 `deterministic_surface_mock / manual_app_observed / not_available`。本地栅格 mock 只能证明当前 profile 下的构图，不得写成真实抖音 / 小红书客户端已验证；未登录平台时 `manual_app_observed=not_run`。 |
| R3-C116 | 视觉审核记录必须绑定 cover hash、preview hash、profile ID 和 reviewer。reviewer 仅允许 `codex_visual_review / human_visual_review`；确定性 composer、schema 或 checksum checker 无权自行写 `visual_review_status=pass`。 |
| R3-C117 | 封面门禁拆成 `structural_gate` 与 `visual_gate`。前者检查尺寸、路径、hash、比例、焦点和预览；后者必须实际看栅格图。只有两者都 pass 才是 `cover_delivery_status=visual_pass`。 |
| R3-C118 | `focal_crop` 必须计算 `source_retained_area_ratio`，所有 `required_visual_element_ids` 对应保护区必须 100% 留在 crop window；任一保护区被切即 `destructive_crop`。非保护区域损失不设武断阈值，但保留率小于 1 时必须有 `crop_loss_justification`。 |
| R3-C119 | `reuse_native` 要求比例原生兼容；`focal_crop` 要求焦点和 crop contract；`fit_pad` 要求显式背景填充设计，默认黑边不合格；`outpaint_extend / independent_generation` 必须有新 provider attempt；`manual_required` 阻断自动就绪。 |
| R3-C120 | `cover_job` 表示一次点击承诺，`cover_rendition` 表示某个目标 surface 成品。同一 rendition 只有在目标 ratio、safe area、title contract 与 profile version 完全相同且分别通过预览绑定时，才可跨平台复用。 |
| R3-C121 | rendition 状态单向推进：`planned -> composed -> dimensions_verified -> preview_ready -> waiting_visual_review -> visual_pass / visual_fail`。失败修复必须新建 rendition revision，禁止原地覆盖历史 pass / fail。 |
| R3-C122 | 交付范围状态固定为 `ready_all_target_platforms / primary_ready_secondary_pending / not_publish_ready`。主平台未 visual pass 时整体 `not_publish_ready`；主平台 pass、次平台待审核时只能说“抖音可用，其他平台待检查”，不得说全平台可发布。 |
| R3-C123 | provider 任务携带 typed target canvas 与 slot；返回后先持久化实际宽高、比例核验和 attempt，再进入适配。封面独立生成计入封面 provider task，不改变正文视觉需求数。 |
| R3-C124 | 不兼容版本固定升级为 `session-execution-plan v0.4`、`visual-need-analysis v0.3`、`cover-render-plan v0.1 / cover-composer v0.2`、`typed_components_v0.4`、`final-delivery-template / renderer v0.4`。v0.3 仅 replay / 审计；当前真实 session 必须创建新 delivery revision 才能恢复。 |

### 17.8 反审计结论与授权

上述消歧后，产品对象、字段、状态、producer / consumer、失败语义和迁移边界已经达到可编译颗粒度，没有遗留需要新增人类业务决策的分叉。用户在 2026-07-14 明确要求“检查后如无问题进入 Skill 编译”，因此 R3-C91 至 C124 进入已确认编译范围；编译必须保持旧 v0.3 只读兼容，并以新 v0.4 fixture 和当前真实 session 新 revision 证明闭环。

## 18. 全文视觉覆盖合同（R3-C125 至 C139）

### 18.1 触发问题

现行 `visual_need_analysis` 能严格检查一个已提出候选是否具有注意力、理解、证据、演示、情绪或记忆价值，也允许一张图覆盖多个 `beat_id`。但它没有一个来自全文的权威 beat 集合，无法证明每段当前口播都被视觉导演处理过。

因此会出现一种新的 false success：候选本身、图片、比例、槽位和最终 HTML 全部合格，但 agent 只提出少数候选，其他重要段落从未进入 generate / reject 判断。图片质量正确，全文覆盖仍然不完整。

产品修复不是提高最低图片数，而是把“有没有逐段做出视觉处置”与“最终有几张图”分开：

```text
coverage completeness：全文是否全部处理
visual value：某个 generate 是否值得做
derived visual asset count：当前内容视觉任务去重后自然得到 0 到 N
```

### 18.2 上游与事实源

R3 不再自行重新切全文，而是只消费 R6-B01 定义、已绑定当前结构的 `content_beat_map(mapping_phase=structure_bound)`。直供稿用于结构诊断的 `semantic_only` revision 不得进入视觉规划：

```yaml
visual_coverage_ledger:
  ledger_id:
  content_beat_map_id:
  draft_id:
  draft_revision:
  draft_digest:
  coverage_records: []
  accepted_visual_task_ids: []
  coverage_completeness_status: complete / incomplete / stale
  visual_delivery_readiness: ready / ready_with_warnings / waiting_assets / waiting_authorization / blocked
  unresolved_beat_ids: []
  derived_visual_asset_count:
  materialized_visual_asset_count:
  provider_generation_task_count:
  provider_generation_attempt_count:
  source_capture_task_count:
  source_capture_attempt_count:
  visual_insert_occurrence_count:
```

`coverage_records[]` 与当前 `content_beat_map.beats[]` 必须集合相等。一个 record 必须选择一个主处置：

```text
generate_visual
create_deterministic_visual
use_source_evidence
use_existing_asset
reuse_visual_task
talking_head_intentional
evidence_blocked
manual_visual_required
```

记录还必须包含 viewer problem、decision reason、covered task、claim / source binding、expected viewer change、information delta、production path 和 recovery action 中适用的字段。确有不同观众任务时允许 `supplemental_visual_task_ids[]`，但每项必须有不同 visual job、独立插入锚点并通过冗余 / 认知负荷检查。不同条件路径分开校验，不把下游图像尺寸或 provider 输出反列为上游输入。

### 18.3 0 张与多节点共用图

0 张继续是合法产品结果，但证明方式从一个全文理由升级为逐节点处置：

- ready 的 0 张要求每个 current beat 均为 `talking_head_intentional` 且没有补充任务；`evidence_blocked / manual_visual_required` 虽然分析完整，但不能伪装成“无需图片”。
- `evidence_blocked / manual_visual_required` 可使 `coverage_completeness_status=complete`，但前者在主张未降级时 delivery blocked，后者为 waiting_assets。
- 历史已验证的非证据素材进入当前内容时使用 `use_existing_asset`：为本稿新建 current visual task，重新核对 asset hash / provenance / rights 与当前语义、画布适配，计入需要数和已成素材数，但 provider / source capture task 均为 0。
- 历史来源截图仍使用 `use_source_evidence(capture_mode=reuse_verified_capture)`，必须重验 claim / source binding、capture hash、captured_at 与 freshness；它不增加本轮 capture task / attempt，也不得改写成普通 existing asset 绕过证据门禁。
- 本次内容的一项 current visual task 可以覆盖多个相邻节点；在非相邻位置复用时使用 `reuse_visual_task` 并新增独立 `visual_insert_occurrence`，不重复计算图片或 provider 任务。
- 相邻节点若需要不同证据、不同展示时机或不同认知任务，不得为了少出图强行合并。

图片数量仍不受时长、fixture、账号默认值、成本或 provider 次数限制。Codex 内置 Image 2 对所有 `generate_visual` accepted task 直接生成；`create_deterministic_visual` 不创建 provider task；来源证据继续由 R6 source capture 路径生产。

### 18.4 结构门禁与质量门禁

确定性 checker 可以判断：

```text
beat 集合相等
每个节点恰有一个处置
决策枚举与条件字段合法
accepted task / covered beat 双向引用闭合
draft / beat map / ledger digest 未过期
derived_visual_asset_count 等于排除 reuse occurrence、平台 rendition 和封面后的唯一 current 内容视觉任务数
materialized / provider task / provider attempt / source capture task / capture attempt / occurrence 各自按独立口径闭合
当前视觉与最终 HTML lineage 闭合
```

确定性 checker 不得自行判断：

```text
主播表达是否真的比图片更好
图片是否真正帮助理解或留人
视觉节奏是否舒服
相邻画面是否造成审美疲劳
情绪与账号声音是否准确
```

这些结论由 `codex_visual_review / human_visual_review` 查看实际稿件与栅格资产后写入 `script_visual_alignment_review`，并给出可读理由。结构 pass 不得覆盖视觉质量 fail。

### 18.5 文案 revision 与失效

`visual_coverage_ledger` 绑定当前 draft revision。修改后按受影响范围恢复：

| 修改 | 视觉影响 |
|---|---|
| 排版、空白或可证明语义等价的错字修订 | 重新计算显示 digest；允许复用 beat / visual IDs。 |
| 局部节点的意思、顺序、事实强度或情绪作用变化 | 相关 coverage、视觉文字、来源 binding、asset acceptance 和 alignment 变为 stale。 |
| 核心承诺、受众或立场变化 | 全部视觉覆盖与对齐审查变为 stale。 |

恢复必须从最小受影响节点继续；旧资产保留审计，不得覆盖，也不得静默进入新 revision。

### 18.6 R3-C125 至 C139 产品合同

| 编号 | 产品合同 |
|---|---|
| R3-C125 | `visual_coverage_ledger` 必须覆盖 current `structure_bound content_beat_map` 的全部节点；`semantic_only` 诊断图或少数自选候选不得进入视觉规划。 |
| R3-C126 | 每个节点必须选择一个明确主处置并给出理由；只有承担不同视觉职责、具有独立插入锚点且通过冗余 / 认知负荷检查时，才可增加补充视觉任务。 |
| R3-C127 | 每项 current 视觉任务，不论 Image 2、确定性、来源、既有或人工路径，均需证明注意力、理解、证据、演示、情绪或记忆价值；完整覆盖不等于每段都出图。 |
| R3-C128 | 内容需要的图片数使用 `derived_visual_asset_count`：去重后的当前内容视觉任务数，包含新生成、来源证据、历史非证据素材、确定性与人工待补任务，排除 current task 复用引用、插入 occurrence、平台 rendition 和封面；保持 0 到 N、不设上限。 |
| R3-C129 | 图片任务数、已成素材数、provider / capture 任务与尝试数、插入次数分开；历史非证据素材用于本稿会新建 current task，历史来源截图仍走 verified evidence capture 复用；current task 非相邻再次出现只新增 `visual_insert_occurrence`。 |
| R3-C130 | 0 张是合法结果，但必须由每个节点的有效非生成决策共同证明，不能只写笼统全文理由。 |
| R3-C131 | `talking_head_intentional` 必须说明主播表达为何优于插图；“没有想到图”不是有效理由。 |
| R3-C132 | 需要证据但来源不足时写 `evidence_blocked`，同步降级正文或改为非证据解释图；生成图不得伪装证据。 |
| R3-C133 | 拍摄前以精确原文锚点定位；已有录音 / 视频时可增加时间码，但时间码不是纯文案入口的必填前置。 |
| R3-C134 | 先确定观众问题、视觉职责和信息增量，再选生产路径、presentation mode、slot、canvas、ratio 与 prompt。 |
| R3-C135 | checker 分开判断覆盖完整性与合同闭合；实际叙事价值、画面效果和注意力竞争必须由 Codex / 人查看后给理由。 |
| R3-C136 | 文案 revision 改变节点语义时，关联视觉任务、文字、来源 binding 和对齐审查自动 stale；旧资产不得静默沿用。 |
| R3-C137 | `script_visual_alignment_review` 检查当前稿与当前图的主张一致、信息重复、相邻连续性、注意力竞争和证据呈现。 |
| R3-C138 | 最终 HTML 按正文顺序展示每个节点的视觉处置；未解决节点不得显示“视觉方案完整”。 |
| R3-C139 | 本批是不兼容合同升级：新增共享 beat map / script review，`visual-need-analysis` 升 v0.4，最终交付升 typed / renderer / template v0.5；旧版本只 replay / 审计。 |

### 18.7 可编译性消歧

| 容易混淆的口径 | 当前唯一语义 |
|---|---|
| `coverage complete` 与 `delivery ready` | 前者只证明每个 beat 已有合法决策；后者还要求证据、生成或人工素材已经满足。 |
| 图片需要数与已得到图片数 | `derived_visual_asset_count` 是内容需要；`materialized_visual_asset_count` 是当前已有。 |
| 图片数与插入数 | 一项任务 / 母资产可有多个 `visual_insert_occurrence`；重复插入不增加图片数。 |
| 图片数与 Image 2 调用数 | `provider_generation_task_count` 统计唯一基础任务，`provider_generation_attempt_count` 统计实际尝试；截图、确定性叠字、人工素材不计 Image 2。 |
| 历史素材复用与 current task 复用 | 历史非证据素材使用 `use_existing_asset`，计入本稿需要数和已成素材数但不调用 provider；历史来源截图仍走 `use_source_evidence(reuse_verified_capture)`；current task 复用使用 `reuse_visual_task`，只增加覆盖引用 / occurrence。 |
| 0 张与阻断 | ready 的 0 张只能来自全部 beat 的 intentional talking head；缺证据或等人工素材不是 0 张完成。 |
| supplemental task | 必须是不同 visual job、独立插入锚点并通过冗余 / 认知负荷检查；否则拒绝。 |
| `unresolved_beat_ids` | 只记录缺决策、非法决策或引用断裂；诚实的 evidence / manual 状态不算“未分析”。 |
| 质量 pass | checker 只判结构；实际画面与叙事 pass 必须有 Codex / human review 与观察理由。 |

`derived_visual_count / zero_visual_reason` 在 v0.3 仅历史兼容。v0.4 使用分离数量字段；`zero_visual_reason` 只可由逐 beat 决策派生为人类摘要，不能作为独立输入绕过 coverage。

### 18.8 当前停点

R3-C125 至 C139 与 R6-C20 至 C50 已于 2026-07-14 共同完成本地 Skill 编译。当前 visual-need-analysis v0.4、coverage ledger v0.1 与 final delivery v0.5 已闭合；v0.3 visual need、v0.4 final delivery 只保留历史 replay，不能证明当前全文覆盖能力。

## 19. 视觉来源唯一分流与 Image 2 默认路由（R3-C140 至 C153）

### 19.1 触发问题

真实热点交付的 4 项画中画资产由 2 张已验证来源截图和 2 张纯确定性 SVG 构成，Image 2 调用为 0。来源截图符合证据边界；但两张非证据、非用户既有的新视觉被 `create_deterministic_visual` 直接满足，说明当前产品虽然写了 `generate_visual -> Image 2`，却没有阻止视觉分析为了文字准确或实现方便，把本应生成的情境 / 概念视觉改名为确定性信息卡。H6C 的 no-provider 测试 profile 又进一步隐藏了该问题。

### 19.2 唯一来源分类

每项通过视觉价值门禁的 current task，必须先进入互斥 `visual_source_class`：

| 来源类 | 适用条件 | 唯一主路径 |
|---|---|---|
| `source_bound_evidence` | 新闻、数据、公告、引语或可核验网页事实，需要观众看到真实来源 | R6 capture + deterministic annotation；禁止 Image 2 伪证据。 |
| `explicit_existing_asset` | 用户提供或本次明确认可复用的既有资产，且权利、语义、清晰度、画布均通过 | 校验后复用；保留来源和本稿 current task。 |
| `generated_context` | 除以上两类外，当前内容新需要的注意力、理解、演示、情绪或记忆视觉 | Image 2 生成基础画面；需要准确文字 / 图示时再做确定性后工程。 |

分类顺序是“先判证据，再判明确既有资产，其余全部生成”，不能把 `deterministic_render` 作为第四个并列主来源。确定性能力仍保留，但角色收窄为截图标注、准确文字、图表线框、裁切、封面排版和其他派生工程；它不能单独满足新建的非证据主视觉任务。

### 19.3 产品合同

`explicit_existing_asset` 不得只写一句“用户认可”。每个复用任务必须引用 immutable `asset_reuse_authorization`：

```yaml
asset_reuse_authorization:
  authorization_id:
  authorization_mode: user_supplied_for_current_run | human_selected_for_current_run | account_preapproved_asset
  authorized_asset_refs: [{asset_ref, sha256}]
  scope: {session_id, visual_task_ids}
  account_profile_snapshot_ref: null | artifact_ref
  rights_basis:
  semantic_role:
  authorized_at:
  currentness_check: pass | revoked | stale | not_assessed
```

`user_supplied_for_current_run` 由本次输入本身形成授权证据；`human_selected_for_current_run` 绑定人类选择记录；`account_preapproved_asset` 只适用于当前账号视觉档案快照中明确登记的 logo、角标、模板或可复用素材。授权默认只覆盖列出的 session、task、资产哈希，不允许 `all_future_content` 或跨账号通配。账号档案后续修改不改写历史授权；在 final candidate 前若权利或当前性变为 `revoked / stale / not_assessed`，该复用任务必须阻断并重新选择来源类。

| 编号 | 产品合同 |
|---|---|
| R3-C140 | 每项 current 视觉任务必须且只能属于 `source_bound_evidence / explicit_existing_asset / generated_context` 之一，分类依据和排除另外两类的理由必填。 |
| R3-C141 | `source_bound_evidence` 统一交给 R6 真实截图与后工程，不调用 Image 2 生成事实证据。 |
| R3-C142 | `explicit_existing_asset` 必须引用 immutable `asset_reuse_authorization`；只接受本次用户提供、本次人类选择或当前账号档案快照预批准的精确资产哈希，并通过 provenance、rights、semantic fit、currentness、canvas 和 quality 检查。“以前回归用过”不等于生产默认可复用。 |
| R3-C143 | 不满足前两类、又通过视觉价值门禁的所有新任务，固定为 `generated_context -> generate_visual -> Image 2 base`。 |
| R3-C144 | `create_deterministic_visual` 不再是当前合同的非证据 primary production path；旧 session 仅按历史合同 replay。 |
| R3-C145 | 需要准确文字、数字、标签或关系图时，Image 2 负责背景 / 场景 / 主体 / 氛围，确定性 renderer 负责准确文字与图形层；后工程不得改变基础图表达的事实含义。 |
| R3-C146 | 用户请求运行当前内容生产链，即授权执行所有通过门禁的 Image 2 任务；不再为每张图增加成本 / 调用次数确认。 |
| R3-C147 | 图片数量仍由全文视觉需要派生，0 到 N、不设上限；不设置成本预算、provider 调用上限或为了省调用而降级视觉职责。 |
| R3-C148 | provider 外部副作用先持久化 task、完整 prompt、prompt digest、attempt、outcome 和 output ref，再做叠字 / 裁切 / 封面；中断后先 reconcile，已有结果禁止盲目重调。 |
| R3-C149 | 叠字、证据标注、裁切、封面、平台 rendition 和同一 base 的派生版不增加 provider task 数；provider 基础任务与派生产物分别计量。 |
| R3-C150 | no-provider / reuse-only 只能作为显式测试 profile，必须进入 task envelope 和执行透明度；它不能改写 production source class 或让确定性图冒充 Image 2 结果。 |
| R3-C151 | 测试 profile 遇到 `generated_context` 且没有合法 current base asset 时，状态必须为 `waiting_assets`，不得生成纯 SVG 顶替、错配旧图或宣称全链完成。 |
| R3-C152 | asset / final HTML 必须展示 source class、route decision、base asset、父哈希、provider / capture、postprocess 和复用授权；`Image 2` 模型具体运行档位不可观察时诚实写 `not_observable`。 |
| R3-C153 | 视觉分析不得以“方便做文字、无需外部调用、测试想跑通”为理由选择确定性主图；只允许按内容证据属性和既有资产授权分类。 |

### 19.4 可编译性与停点

本批是不兼容路由修订。编译目标固定为 `visual-need-analysis v0.5`、`visual-coverage-ledger v0.2` 与 `visual-source-routing v0.1`，并同步升级 image task / asset provenance、task guidance、producer dispatch、candidate / HTML 和 compatibility matrix；R3-C127-C129 中允许纯确定性主图和普通历史复用的旧路由解释登记为 `superseded_pending_recompile`，其“逐 beat 证明视觉价值、数量分开计量”的其余语义继续有效。

正反 fixture 覆盖：来源证据不调用 Image 2、本次用户输入 / 人类选择 / 账号预批准三种合法复用、无授权 / 错 session / 错 task / 错 hash / 跨账号 / 已撤销复用拒绝、普通概念图必须 Image 2、文字信息卡采用 Image 2 base + overlay、no-provider profile 正确等待、纯 SVG 冒充 generated 拒绝、provider outcome 落盘后中断恢复、HTML 路由透明。当前状态为 `confirmed_joint_skill_compiled_local`；visual need v0.5、coverage v0.2、source routing v0.1 和 reuse authorization v0.1 已闭合到 runtime / checker，真实 Image 2 新 session 尚未执行。

## 20. 基础图与最终交付素材分离（R3-C154 至 C163）

### 20.1 触发问题

真实直供稿已经产生两张 Image 2 基础图，并另外生成了中文叠字版；但 current image asset set 和最终 HTML 仍引用基础图，页面甚至显示“无文字”。这证明现有合同能记录 postprocess，却没有定义“哪一个派生版本才是交付真源”，导致 base、overlay、封面和 HTML 各自存在但没有 current final binding。

### 20.2 资产角色

每个 visual task 分开保存：

```text
base_asset_ref        Image 2 / source capture / authorized existing 的不可变基础素材
derived_renditions[]  叠字、标注、裁切、排版等不可变派生版本
delivery_asset_ref    当前通过视觉 review、允许进入 HTML 与下载的唯一成品
```

`delivery_asset_ref` 必须指向 base 或某个 derived rendition，并绑定 sha256、尺寸、画布、文字摘要、父资产、postprocess recipe、review ref 与 current revision。若视觉任务要求准确文字、下划线、数据标注或平台适配，base 只能作为生产证据，不能同时冒充 final asset。

### 20.3 产品合同

| 编号 | 产品合同 |
|---|---|
| R3-C154 | 每个已物化 visual task 明确区分 `base_asset_ref / derived_renditions[] / delivery_asset_ref`；HTML 与下载只消费 delivery asset。 |
| R3-C155 | 视觉任务声明 `required_postprocess` 后，所有必需步骤未完成前状态为 `postprocess_pending`，不得把 base 标为 materialized final。 |
| R3-C156 | 要求准确文字、标注、下划线、数字或关系图时，delivery asset 的文字摘要、OCR/人工 review 与任务要求必须闭合；页面不得一边展示叠字需求、一边写“无文字”。 |
| R3-C157 | 每个派生 rendition 使用独立 ID、revision、文件、sha256 和 parent sha；不得覆盖 base 或前一版派生图。 |
| R3-C158 | `delivery_asset_ref` 只能由显式 finalize command 在 postprocess 与 visual review 通过后推进；candidate compiler 不选择目录中的“最新文件”。 |
| R3-C159 | final HTML 的预览、下载、alt、视觉摘要、插入位置和追溯链接必须来自同一个 delivery asset revision。 |
| R3-C160 | 封面可从 base 或任一已批准派生素材继续生产，但必须显式记录 parent；封面 current 不会反向把画中画 delivery asset 改回 base。 |
| R3-C161 | sidecar、generation record、postprocess record 和 visual review 都是独立证据；缺失时阻断，不由 candidate 临场创建或从 layout report 伪装 review。 |
| R3-C162 | visual review 必须查看 output hash 对应的真实图片，记录文字准确、裁切、可读性、语义一致和安全区；只验证尺寸与文件存在不能通过。 |
| R3-C163 | fixture 覆盖无后工程直接 base、Image 2 base + 叠字 final、新闻截图 + 标注 final、postprocess pending、错 parent/hash、HTML 错绑 base、finalize 幂等和返修新 revision。 |

本批已与 R7-C113 至 C132 联合完成 H7A 本地编译。image asset set v0.3、delivery asset set v0.1、显式 finalize runtime、逐 hash 视觉 review 和 candidate / HTML final-asset binding 已通过 focused fixture；`PRIVATE-H7-REJECTED-RUN` 的已生成基础图、叠字图和 HTML 继续保留为失败证据，不手改旧 current pointer 使其变成成功样本。新私有 session 的真实业务验收留在 H7B，未调用新图片 provider。

```yaml
product_definition_status: confirmed_joint_skill_compiled_local
compile_authorized: true_consumed_for_R7-H7A
compile_result: pass_image_asset_v03_finalize_and_final_asset_projection
real_private_regression: completed_R7-H7B_pass_with_warnings_project_L2_8
```

## 21. L3 视觉语义工作包（R3-C164 至 C180）

### 21.1 触发问题与产品父因

真实热点与直供回归已经能产出 Image 2、来源截图、叠字成品、封面和最终 HTML，但 L3 审计仍观察到视觉价值判断、完整 prompt、栅格目检和一次封面字级实现存在 Codex 实质扶持。问题不在于“视觉判断用了 AI”——视觉工作本来就需要语义与审美判断——而在于这些判断没有全部作为可追溯的 current task 进入统一工作包，导致：

```text
Codex 可以按大方向做对，但仍需临场猜本步输入和输出；
prompt 可能直接从聊天产生，而不是 current visual task 的版本化产物；
看图结论存在，但 producer/reviewer、输入 hash 和返修目标不总是闭合；
缺少通用后工程能力时，容易在真实 session 内临时写 helper 继续跑。
```

产品目标不是把视觉审美编成僵硬分数，而是把每次 AI 判断放进“输入固定、输出有类型、失败有路由、证据可追溯”的语义任务。

### 21.2 五段视觉语义工作包

一个 current visual task 依次产生五类对象；没有业务需要的阶段按来源路由明确跳过，不得凭聊天记忆省略：

```text
visual_intent_decision
-> visual_source_route_decision
-> visual_prompt_brief（仅 generated_context）
-> visual_asset_review
-> delivery_visual_review
```

#### `visual_intent_decision`

逐 current structure-bound beat 判断：观众此处可能划走、听不懂、不相信、看不见过程、缺情绪或记不住什么；视觉能否产生注意力、理解、证据、演示、情绪或记忆增量。输出 `required / no_visual / blocked_evidence / waiting_material`，并保存反事实理由。它不规定图片数，也不能把“我没想到图”当 `no_visual`。

#### `visual_source_route_decision`

只允许 current R3 三类来源：

```text
source_bound_evidence
explicit_existing_asset
generated_context
```

路由必须引用 visual intent、claim/evidence requirement、复用授权和账号快照。来源证据不能转 Image 2；没有明确授权的旧图不能转 existing；剩余通过任务自动进入 Image 2。

#### `visual_prompt_brief`

Codex 负责语义视觉化，不直接手拼 provider 调用。typed brief 至少包含：

```yaml
visual_task_ref:
audience_problem:
value_jobs: []
scene_and_subject:
composition_and_attention_path:
target_canvas_and_slot:
brand_visual_constraints:
truthfulness_constraints:
forbidden_misrepresentation: []
text_layer_plan:
reference_asset_bindings: []
revision_reason:
```

确定性 prompt compiler 再合并平台画布、安全区、禁止项、准确文字后工程策略和 provider 入参，生成调用前不可变 full prompt、negative / constraint block 与 digest。语义 brief 和工程编译分责，不能把 provider 最终 prompt 直接留在聊天里。

#### `visual_asset_review`

reviewer 必须实际查看 current output hash 对应图片，逐维给 `pass / revise / reject / not_applicable`：

```text
task_alignment           是否解决原观众问题和视觉价值任务
attention_composition    主体、层级、注意力路径与主播/画面竞争
truthfulness             是否把解释图伪装成现场、新闻或数据证据
text_number_accuracy     需要准确文字、日期、数字、单位时是否一致
crop_safe_area           current slot / canvas / platform rendition 是否保住关键内容
small_screen_readability 小屏是否可辨认，不用文件尺寸代替
brand_fit                是否符合账号视觉身份且没有模板化跑偏
artifact_integrity       output、sidecar、parent、hash 和后工程记录是否闭合
```

不使用无依据的总分阈值。gate 从各维 verdict 和 blocking issue code 派生；`revise` 必须输出最小返修目标、owning producer 和 stale 范围，不能只写“图片不好看”。

#### `delivery_visual_review`

它只审 current `delivery_asset_ref` 及最终 HTML 的 desktop/mobile screenshot，不回看 base 后直接给 final pass。检查成品文字、裁切、插入语境、平台卡、封面、重复展示和页面层级；任一 current asset / HTML / screenshot hash 改变，旧 review 自动 stale。

### 21.3 Producer 与 reviewer 的逻辑分离

同一个 Codex runtime 可以顺序承担不同语义任务，但 producer 和 reviewer 必须是不同 task envelope / role：

```text
visual_director          生成 intent / route / brief
visual_quality_reviewer  只消费已物化 asset、任务合同和当前展示上下文
delivery_reviewer        只消费 final asset 与当前 HTML / viewport evidence
```

reviewer 不能用 producer 的“我认为已经满足”作为通过证据，也不能在 review 阶段直接改图、改 sidecar 或补 prompt。发现问题只提交 typed revision request，由 owning producer 生成新 revision。这里的 role 是产品逻辑身份，不声称 Codex 前端自动切换模型或推理档位。

### 21.4 通用视觉能力注册与缺能力语义

新增 `visual_operation_registry`，只登记编译前已经存在且通过 fixture 的通用能力，例如：

```text
image2_base_generation
source_capture
crop_fit_pad
text_overlay
evidence_anchor_annotation
platform_rendition
cover_composition
asset_finalize
viewport_capture
```

每项声明输入、输出、适用 asset role、版本、入口、失败分类和幂等 / reconcile 语义。真实 session 需要未登记操作时进入 `waiting_capability`；不得在 session 目录或临时区新建一次性封面 / 字体 / 裁切 helper 后继续计自治。通用能力应退出内容运行，单独完成产品确认、Skill 编译、fixture 和 registry 登记，再以新 session 证明。

### 21.5 R3-C164 至 C180 产品合同

| 编号 | 产品合同 |
|---|---|
| R3-C164 | 每个 current visual task 由版本化视觉语义工作包推进，至少区分 intent、source route、prompt brief、asset review 和 delivery review；适用阶段不得靠聊天记忆省略。 |
| R3-C165 | `visual_intent_decision` 逐 current structure-bound beat 判断观众问题、视觉价值、反事实损失和处置；继续保持 0 到 N、不设图片上限。 |
| R3-C166 | `no_visual` 必须给出可审计理由；“没想到图、成本、测试方便、先跑通”不是合法理由。 |
| R3-C167 | `visual_source_route_decision` 只能在 source evidence、authorized existing、generated context 三类中选择，并绑定 intent、claim、授权和账号快照。 |
| R3-C168 | generated task 先由 Codex 在 task envelope 内提交 typed `visual_prompt_brief`；不得从聊天文字或临时文件直接调用 Image 2。 |
| R3-C169 | 确定性 prompt compiler 独占平台画布、安全区、准确文字后工程、禁止项和 provider 参数合并；调用前保存 full prompt、digest、task 与 revision。 |
| R3-C170 | Image 2 / capture 的 task、attempt、outcome、output 和 reconcile 逐任务留证；aggregate 只能求和，不能反写每项。 |
| R3-C171 | 必需叠字、证据标注、裁切、rendition 和封面在生成前形成 typed postprocess plan；未完成保持 pending，不能让 base 越级 finalize。 |
| R3-C172 | `visual_asset_review` 必须实际查看 current output hash 对应图片；路径、尺寸或 provider 成功不能自动生成视觉 pass。 |
| R3-C173 | review 至少检查任务对齐、注意力构图、事实诚实、文字数字、裁切安全区、小屏可读、账号适配和资产完整性；不使用无依据总分阈值。 |
| R3-C174 | review 只允许 `pass / revise / reject / not_applicable` 及注册 issue code；revise 必须给最小返修目标、owning producer 和 stale 范围。 |
| R3-C175 | visual producer、asset reviewer、delivery reviewer 是独立逻辑 task / role；可由同一 Codex 顺序执行，但输入输出和权限分离，不声称自动切模型。 |
| R3-C176 | `delivery_visual_review` 只消费 current delivery asset、HTML hash 和 desktop/mobile screenshot hash；查看 base 或旧截图不能通过 final。 |
| R3-C177 | asset、postprocess、delivery binding、HTML 或 screenshot 任一 current hash 改变，受影响 review 自动 stale 并按 owning layer 重开。 |
| R3-C178 | 所有 Image 2、capture、叠字、标注、裁切、rendition、封面、finalize 和 viewport 操作必须来自版本化 `visual_operation_registry`。 |
| R3-C179 | 运行中缺少通用操作时状态为 `waiting_capability`；run-specific helper、临时 sidecar / review 或手改成品会使该 session 失去 autonomous delivery 资格。 |
| R3-C180 | fixture 必须覆盖五段正常链、0 图、三来源、prompt revision、provider reconcile、逐维 review、reviewer 越权、hash stale、未注册 helper、waiting capability 和 final HTML 看图。 |

编译消歧：registry 中出现 operation 名称和入口路径，只能证明“已登记”，不能证明“可执行”。每个 current operation mode 还必须有 typed request、真实 producer、attempt/outcome 持久化、幂等或 reconcile 语义，以及至少一次代表性 fixture 实际产生并校验输出；缺任一项时仍为 `waiting_capability`。

### 21.6 可编译性消歧与当前停点

```text
语义判断允许由 Codex 完成，但每一步必须由 task envelope 给输入和 allowed output；
确定性工具只负责编译、校验、提交和派生，不能自称审美 pass；
人类最终验收不扣自治，但人类手改机器对象会扣；
图片数量与 provider 次数仍由内容产生，L3 能力覆盖不反向制造图片；
同一 Codex 可以生产与审查，但必须用不同逻辑 role 和不可变输入，禁止 review 阶段顺手修图；
缺能力是产品等待状态，不是现场写一次性脚本的理由。
```

```yaml
product_definition_status: confirmed_R7-L3-H3_direct_route_compiled
confirmed_scope: R3-C164-C180
ambiguity_audit: pass_product_layer_no_blocking_business_fork
compile_authorized: consumed_R7-L3-H3
joint_compile_with: R7-C133-C160
R7-L3-H1_dependency_result: pass_evidence_foundation_compiled
R7-L3-H2_compile_result: pass_offline_21_cases
R7-L3-H3_compile_result: pass_offline_7_cases_direct_v05
current_direct_visual_contract: visual_stage_set_v0.1+image_asset_set_v0.4+image_asset_delivery_set_v0.2
R7-L3-H4_compile_result: pass_offline_8_cases_hotspot_v05
current_hotspot_visual_contract: visual_stage_set_v0.1+image_asset_set_v0.4+image_asset_delivery_set_v0.2
recommended_next_batch: R7-L3-H5_private_real_certification
```
