# R4 产品确认清单

> 状态：r4_confirmed_packaging_rules_compiled_static_checked
> 所属路线：R4 开源交付标准  
> 主责：把 R4 是否可以进入开源规则 / 包装编译拆成可确认条目。  
> 边界：本清单已确认并进入规则 / 包装编译；确认本清单不等于允许推送 GitHub，也不代表真实 public_release 已生成。

---

## 1. 确认项

| ID | 确认项 | 推荐结论 | 状态 |
|---|---|---|---|
| R4-C01 | 确认 R4 只解决开源交付标准，不回头重做 R1-R3 | 是 | confirmed |
| R4-C02 | 确认公开 GitHub 不能直接使用当前工作母仓 | 是 | confirmed |
| R4-C03 | 确认必须先生成 `public_release/` 候选包 | 是 | confirmed |
| R4-C04 | 确认真实账号 runs 不得进公开包 | 是 | confirmed |
| R4-C05 | 确认真实账号档案默认不得进公开包，必须脱敏成 sample | 是 | confirmed |
| R4-C06 | 确认真实图片、视频、音频默认不得进公开包 | 是 | confirmed |
| R4-C07 | 确认 README 是人类入口，AGENTS 是 AI 入口，PROJECT_MAP 是索引入口 | 是 | confirmed |
| R4-C08 | 确认公开 README 必须包含快速开始、能力边界、样例、成熟度和贡献方式 | 是 | confirmed |
| R4-C09 | 确认开源包必须有 LICENSE，但最终 License 类型需涛哥单独确认 | 是 | confirmed |
| R4-C10 | 确认开源包必须有 CONTRIBUTING、SECURITY、CHANGELOG、CODE_OF_CONDUCT | 是 | confirmed |
| R4-C11 | 确认至少有 sample-account 和 sample-run | 是 | confirmed |
| R4-C12 | 确认 sample 必须标记 sample_only，不得含真实客户、真实账号和真实热点隐私 | 是 | confirmed |
| R4-C13 | 确认 sample 的 manifest、trace、final HTML 链接必须闭合 | 是 | confirmed |
| R4-C14 | 确认 R4 alpha 可以用 pending_external 图片占位样本，不强制真实图片 | 是 | confirmed |
| R4-C15 | 确认 public_release 必须做链接检查 | 是 | confirmed |
| R4-C16 | 确认 public_release 必须做敏感信息检查 | 是 | confirmed |
| R4-C17 | 确认 public_release 不得含本机绝对路径作为运行依赖 | 是 | confirmed |
| R4-C18 | 确认 README 不得宣称完整真实测试、L3 或 1.0，除非对应验证完成 | 是 | confirmed |
| R4-C19 | 确认开源包不实现自动发布、平台登录、外部图片 API | 是 | confirmed |
| R4-C20 | 确认 R4 产品确认后，只能进入开源规则 / 包装编译，不直接推 GitHub | 是 | confirmed |
| R4-C21 | 确认 release-checklist 是发布前主门禁 | 是 | confirmed |
| R4-C22 | 确认 examples 和 templates 必须被 README / PROJECT_MAP 索引 | 是 | confirmed |
| R4-C23 | 确认外部 AI 使用者应该能只读 README / AGENTS / PROJECT_MAP / sample 接上 | 是 | confirmed |
| R4-C24 | 确认 `public_release` 的失败状态必须阻断而不是警告 | 是 | confirmed |
| R4-C25 | 确认 R4 完成后再进入 R1-R4 综合 dry-run / 完整真实测试前置检查 | 是 | confirmed |
| R4-C26 | 确认公开包必须有 `VERSION`，且后续 tag / CHANGELOG 应与 VERSION 一致 | 是 | confirmed |
| R4-C27 | 确认公开包必须有 `public-manifest.yaml` 作为机器可读真源 | 是 | confirmed |
| R4-C28 | 确认 README 与 public-manifest 成熟度口径冲突时必须阻断 | 是 | confirmed |
| R4-C29 | 确认后续 R4 编译阶段需要 `build-public-release` 构建器或等价清单流程 | 是 | confirmed |
| R4-C30 | 确认 sample 不只放文件，还要有 quickstart / acceptance 说明 | 是 | confirmed |
| R4-C31 | 确认 skill 合集需要主入口、分组和宿主安装说明，不只暴露目录 | 是 | confirmed |
| R4-C32 | 确认 `.github/workflows/` 可作为后续 release 自动化候选，但 R4 产品层不实现 | 是 | confirmed |
| R4-C33 | 确认 OpenSSF / GitHub 社区健康检查先以人工 scorecard 承接，后续再脚本化 | 是 | confirmed |
| R4-C34 | 确认 public_release 不得引用工作母仓绝对路径 | 是 | confirmed |
| R4-C35 | 确认 R4 alpha 可以没有真实 generated 图片样本，但必须明确 pending_external 边界 | 是 | confirmed |
| R4-C36 | 确认候选包生成前必须单独确认 License 类型 | MIT | confirmed |
| R4-C37 | 确认公开样例范围只包含脱敏 sample、tutorial、template 和规则文档 | 是 | confirmed |
| R4-C38 | 确认真实 `accounts/*/runs/` 不得进入候选包 | 是 | confirmed |
| R4-C39 | 确认 `docs/tutorials/r3-generated-image-sample/` 可作为公开 generated 图片样例候选 | 是 | confirmed |
| R4-C40 | 确认 release_channel 暂定 `alpha`，workflow_maturity 暂定 `l2_8` | 是 | confirmed |

---

## 2. R4 进入编译的门槛

```text
R4-C01 到 R4-C35 全部由涛哥认可。
R4 内部冗余、重复、冲突检查通过。
R4 与原始方法论冲突检查通过。
成熟项目 / dbskill 对标返修完成。
```

---

## 3. 编译后候选产物

R4 确认后，下一阶段才允许生成：

```text
docs/reference/GitHub开源上线检查清单.md
templates/public-release/
examples/sample-account/
examples/sample-run/
release-checklist.md
```

不得在产品确认前直接生成 `public_release/`。

---

## 4. R4 内部一致性检查

> 检查时间：2026-07-07  
> 检查范围：`R4-产品总览.md`、`R4-开源交付与净化规则.md`、`R4-产品确认清单.md`、路线图 Phase 5、README / PROJECT_MAP 索引。

结论：

```text
未发现硬冲突。
```

保留的正常重复：

| 重复项 | 判断 |
|---|---|
| `public_release/` 同时出现在总览、细则、确认清单 | 正常。总览定义对象，细则定义允许 / 排除 / 阻断，确认清单用于人类确认 |
| README / AGENTS / PROJECT_MAP 多次出现 | 正常。它们分别是人类入口、AI 入口和索引入口，是 R4 的核心三件套 |
| sample / examples 多次出现 | 正常。R4 的开源价值依赖样例，必须在总览、净化规则和确认项里重复强调 |
| License 多次出现 | 正常。R4 只要求必须有 License，不在产品层决定具体 License 类型 |

已确认不冲突：

| 表面差异 | 判断 |
|---|---|
| 产品阶段“不生成 public_release”，但确认项要求“必须先生成 public_release 候选包” | 不冲突。前者约束当前产品开发阶段，后者约束 R4 经确认后的编译 / 包装阶段 |
| `.github/workflows/` 进入允许清单，但 R4 产品层不实现 release workflow | 不冲突。R4 只把它定义为后续编译候选，不在产品层落实现 |
| R4 alpha 可无真实图片，但 R3 强调 generated 图片与 sidecar | 不冲突。R4 alpha 可用 pending_external 样例，但不得把它说成 generated 路径通过 |
| 产品文档可以出现工作母仓绝对路径，但 public_release 不得引用绝对路径作为依赖 | 不冲突。产品文档记录本地事实，公开包 manifest / README 不得依赖本机路径 |

当前 R4 自洽口径：

```text
R4 产品阶段：定义 public_release 结构、净化规则、确认项和发布门禁。
R4 编译阶段：生成模板、sample、public-manifest、release-checklist 和候选 public_release。
R4 发布阶段：由涛哥另行确认 License、远端仓库、tag 和是否真正发布。
```

---

## 5. R4 与原始方法论冲突检查

> 检查时间：2026-07-07  
> 对照来源：`AGENTS.md`、`docs/explanation/工作流问题包与产品设计草案-20260706.md`、`docs/explanation/最终交付页与图片降级策略.md`、`docs/explanation/工作流工程缺陷复盘与修订方案.md`。

结论：

```text
未发现硬冲突。
```

对齐关系：

| 原始方法论 / 边界 | R4 对齐方式 |
|---|---|
| 不改业务代码、不发版、不登录平台、不自动发布、不接外部图片 API | R4 只定义开源交付产品层；明确不推 GitHub、不实现自动发布、不接外部 API |
| 当前工作母仓不是可直接公开仓 | R4 强制 `public_release/` 候选包，不直接发布工作母仓 |
| 真实账号、真实内容、客户资料必须脱敏 | R4 把真实 runs、真实档案、真实图片、客户资料列为公开包阻断项 |
| Markdown 是后台追溯资产，HTML 是人类验收入口 | R4 要求 sample-final-delivery 和最终 HTML 链接闭合，但不把一堆 MD 当最终交付 |
| project_local 不能冒充可转交包 | R4 区分 sample、public_release、portable / standalone 交付边界，不把本地页面当公开包能力证明 |
| pending_external 是诚实降级，不是 generated | R4 alpha 可用 pending_external 样例，但必须明确没有验证 generated 图片和 sidecar |
| skill 能不能被另一个 AI 理解 | R4 增加 README / AGENTS / PROJECT_MAP / public-manifest 四入口，降低外部 AI 读晕概率 |
| trace / validator 不能只是记录 | R4 引入 release-checklist、scorecard、link / secret / privacy 检查作为开源前门禁 |

可接受差异：

| 差异 | 判断 |
|---|---|
| 原始方法论讨论的是内容生产 workflow，R4 讨论开源包工程 | 不冲突。R4 是把 workflow 的规则、样例、边界对外发布，不改变内容生产链路 |
| 原始最终交付强调 `final-delivery.html`，R4 强调 README / public-manifest / sample | 不冲突。前者是单条内容交付，后者是开源项目交付 |
| 原始旁路策略暂不做 Seedream，R4 仍保留 pending_external sample | 不冲突。R4 只展示降级边界，不接任何外部模型 |

当前 R4 判断：

```text
R4 是对原始问题包 P07、P13、P16、P17 的开源交付层补齐。
R4 不替代 R1-R3，也不把产品草案写成已验收发布能力。
```

---

## 6. R4 编译后静态检查

> 检查时间：2026-07-07  
> 检查范围：R4 产品确认后的 reference、templates、examples、README、PROJECT_MAP、STATUS 和工作流状态记录。  
> 注意：本检查只验证 R4 编译产物和索引，不验证真实 `public_release/` 候选包。

结果：

```yaml
r4_static_check_id: R4STATIC-20260707-001
required_file_missing_count: 0
orphan_markdown_count: 0
index_status: pass
template_boundary_status: pass
real_public_release_generated: false
github_pushed: false
license_selected: false
```

已通过：

| 检查 | 结果 |
|---|---|
| `docs/reference/GitHub开源上线检查清单.md` 已生成 | pass |
| `templates/public-release/` 模板已生成 | pass |
| `examples/` sample 模板已生成 | pass |
| README / PROJECT_MAP 已索引新增文件 | pass |
| Markdown 孤岛扫描 | pass |
| 模板未宣称已发布 GitHub | pass |
| 模板未宣称真实 `public_release` 已生成 | pass |

未验证：

```text
真实 public_release 候选包。
真实 License 选择。
真实 GitHub remote。
真实 tag / release。
真实 public_release 链接扫描。
真实 public_release 密钥扫描。
```

下一步建议：

```text
R4 当前可进入“R1-R4 综合 dry-run 前置检查”。
仍不得进入完整真实测试或 GitHub 发布。
```

---

## 7. R4 public_release candidate 前置确认

> 记录时间：2026-07-07  
> 当前状态：waiting_human_confirmation  
> 边界：本节只确认候选包生成条件，不生成 `public_release/`，不创建远端仓库，不推 GitHub。

当前推荐确认口径：

```yaml
license_recommendation: MIT
license_status: confirmed_mit
public_sample_scope:
  include:
    - README / AGENTS / PROJECT_MAP
    - docs/reference
    - docs/product
    - docs/explanation
    - docs/tutorials/r3-generated-image-sample
    - skills
    - objects
    - templates
    - examples/sample-account
    - examples/sample-run
  exclude:
    - real accounts runs
    - real account profiles
    - external source cache
    - real media assets
    - secrets / cookies / tokens
release_channel: alpha
workflow_maturity: l2_8
```

进入候选包生成的最小条件：

```text
R4-C36 到 R4-C40 已经涛哥确认。
public_release 仍从净化包生成，不能直接发布工作母仓。
R3 generated 样本可以作为 sample evidence，但不能说完整真实内容测试已通过。
```
