# R3 Visual Presentation Fixtures

脱敏验证 R3-C91 至 C124：结构化画幅、归一化槽位、封面 rendition、焦点裁切、保护区、栅格预览、显式视觉审核和主次平台交付状态。

本 fixture 不调用图片 provider、不登录平台、不发布。运行时合成的 PNG、preview、review 和报告只写入 `state/checks/`；`fixture_only` 视觉审核不能作为真实账号交付证据。
