# P0 H7 Runtime Fixture

脱敏验证 `typed_components_v0.3 -> delivery revision -> HTML / final Markdown / content delivery record`。本样例不联网、不生成图片、不登录平台、不发布；SVG 只用于验证平台封面标题与实际成品的确定性绑定。

```powershell
.\tools\validate-p0-h7-fixtures.ps1
```
