# Expected Agent Behavior

1. 读取当前 session manifest 和 final-delivery.html。
2. 判断用户只要求局部返工。
3. 画中画新增需求路由到 `talking-head-image-pip`，标题局部修改路由到 `platform-packaging-adapter` 或 final-delivery-builder 局部更新。
4. 不重跑 hotspot-topic-research。
5. 更新 content_delivery_record、html_embed_manifest 和 final-delivery.html。
