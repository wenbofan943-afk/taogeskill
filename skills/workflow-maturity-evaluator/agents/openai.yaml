interface:
  display_name: "工作流成熟度证据评估器"
  short_description: "从注册运行证据推导会话、路线与项目成熟度"
  default_prompt: "Use $workflow-maturity-evaluator to freeze the current capability baseline and derive session, route, and project autonomy evidence without trusting self-reported manual-patch flags."
policy:
  allow_implicit_invocation: true
