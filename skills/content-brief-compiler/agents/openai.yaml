interface:
  display_name: "内容 Brief 编译"
  short_description: "把已确认选题编译成写作输入包"
  default_prompt: "Use $content-brief-compiler to turn the selected topic card into a content brief."

policy:
  allow_implicit_invocation: true
