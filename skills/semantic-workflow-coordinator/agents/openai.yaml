interface:
  display_name: "单篇语义工作流协调器"
  short_description: "按蓝图调度生产者并确定性提交单篇内容的唯一下一步"
  default_prompt: "Use $semantic-workflow-coordinator only after the deterministic session entry selects legacy_r7. Execute the version-pinned R7 plan, preserve honest waits, and never initialize or migrate a kernel_v1_current session."
