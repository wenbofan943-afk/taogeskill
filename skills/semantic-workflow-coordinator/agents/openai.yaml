interface:
  display_name: "单篇语义工作流协调器"
  short_description: "按蓝图调度生产者并确定性提交单篇内容的唯一下一步"
  default_prompt: "Use $semantic-workflow-coordinator with the session-pinned R7 blueprint. Prepare only the unique current task and commit through the runtime. Preserve direct semantic phases and hotspot request/set/panel/decision/source ordering. At delivery freshness, persist unassessed waits, apply monitoring versus semantic changes deterministically, and activate only a versioned recoverable replan before continuing to the plan-pinned v0.6 or v0.7 renderer."
