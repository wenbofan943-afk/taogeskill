# Legacy R1/R2 routing

Load this reference only for an explicit historical replay or a version-pinned
legacy session. Preserve the plan's recorded Skill and contract versions.
Never migrate a legacy session to the current blueprint implicitly. If a legacy
artifact cannot be consumed by its pinned runtime, report a compatibility
blocker and route to issue triage.
