# 交接物字段词典

> 状态：传播工作流字段基准  
> 主责：统一涛哥创作工作流中各 skill 的交接物名称、技术字段、中文展示名、状态值和别名处理。  
> 边界：本文件只约束涛哥创作工作流和 skill 编排，不是产品数据库 schema，不直接约束客户端、服务器或发布链路。

<!-- ai-nav:start -->
## AI 阅读导航

- 先查 [交接物总表](#二交接物总表)，再跳到具体对象，不要默认读取全文。
- 图片数量与 accepted task：查 `visual_need_analysis`。
- 图片生成、中断与恢复：查 `image_generation_record`。
- 用户直供文案与新闻来源截图：查 `6.2` 到 `6.6`。
- P0 plan / event / lineage / render：查 `19.4.2` 到 `19.4.5`。
- R7 蓝图、节点、任务单、submission、动作与兼容：查 `19.4.6`。
- R7 热点 research request / set、面板、决定、已选来源和 v0.4 Brief / Draft：查 `19.4.7`。
- R6 v0.2 证据锚点、R3 视觉来源路由与 R7 人工返修重开：查 `19.4.8`。
- R8-H1 业务 Skill 入口体量与 reference 加载合同：查 `47`。
- R8-H5 v0.2 独立评估九类对象、状态与 supersedes：查 `48`。
- M2-M5 workflow kernel shadow request、command、external activity、observation、session generation binding 与 compatibility resolution：查交接物总表中的 `kernel_*`。
- M6 direct same-digest certification request / report、writer ledger 与认证边界：查 `50`。
- 新增字段必须同时更新字段词典、Schema、Skill / CONTRACT、fixture 和 checker。
<!-- ai-nav:end -->

---

## 一、硬规则

1. 跨 skill 传递时必须使用“技术字段名”，例如 `brief_status`，不要只写“Brief 状态”。
2. 中文展示名只用于给人看，不能替代技术字段。
3. 同一个产物只能有一个标准技术名；旧叫法只作为别名解释，不得继续新增。
4. 每个交接物输出时必须带 `next_skill`；到人工确认或流程结束时，`next_skill` 写 `human_confirm` 或 `done`。
5. 每个状态字段只能使用本文件列出的允许值；需要新增值时，先修订本词典，再修订上下游 skill。
6. 用户交互位置必须同时给“状态字段”和“口语化引导语”，不能只输出字段名。

---

## 二、交接物总表

| 阶段 | 标准技术名 | 中文名 | 主责 skill | 下游 |
|---|---|---|---|---|
| 账号建档引导 | `account_onboarding` | 首次账号建档记录 | `account-onboarding` / `propagation-router` | `account_profile` |
| 入口路由请求 | `entry_router_request` | Quickstart 入口识别与路由请求 | `propagation-router` | `account_onboarding` / 专项 skill / `workflow_check_report` |
| 账号档案 | `account_profile` | 账号档案 | `hotspot-topic-research` / `propagation-router` | `topic_card` |
| 账号启动检查 | `account_startup_check` | 账号调用前的按需字段检查与会话快照决策 | `propagation-router` | `hotspot-topic-research` / `static-visual-director` / 人类补问 |
| 产品对象档案 | `product_profile` | 产品对象档案 | `hotspot-topic-research` / `propagation-router` | `research_run_record` |
| 活动对象档案 | `campaign_profile` | 活动对象档案 | `hotspot-topic-research` / `propagation-router` | `research_run_record` |
| 调研运行记录 | `research_run_record` | 本轮调研记录 | `hotspot-topic-research` | `hotspot_candidate` |
| 热点候选 | `hotspot_candidate` | 热点候选 | `hotspot-topic-research` | `topic_card` |
| 选题决策面板 | `topic_selection_panel` | Topic Gate 给人的选题候选反馈面板 | `hotspot-topic-research` | `topic_card` / `content_brief` |
| 选题卡 | `topic_card` | 选题卡 | `hotspot-topic-research` | `content_brief` |
| 直供文案入口 | `direct_content_intake` | 用户原稿、意图与改写边界登记 | `direct-content-intake` | `direct_content_card` |
| 直供内容卡 | `direct_content_card` | 不伪造热点选题的内容来源对象 | `direct-content-intake` | `content_brief` / `hotspot-topic-research` |
| 主张卡 | `claim_card` | 可核验主张、原句范围与类型 | `direct-content-intake` / draft claim map | `news-evidence-pip` / `quality_review` |
| 来源记录 | `source_record` | 公开来源及访问元数据 | `news-evidence-pip` | `source_capture_record` / `final_delivery` |
| 来源捕获记录 | `source_capture_record` | 可恢复网页截图、视口、片段和 hash | `news-evidence-pip` | `evidence_claim_binding` |
| 证据主张绑定 | `evidence_claim_binding` | 主张、来源、截图与证据关系 | `news-evidence-pip` | `evidence_screenshot_pip` / `quality_review` |
| 新闻证据画中画 | `evidence_screenshot_pip` | 来源事实与创作者解读分层的确定性资产 | `news-evidence-pip` | `quality_review` / `final_delivery` |
| 证据锚点标注 | `evidence_anchor_annotation` | 口播主张、截图精确可见引文、归一化坐标与后工程样式绑定 | `news-evidence-pip` | `semantic_fact_binding` / `final_delivery` |
| 语义事实绑定 | `semantic_fact_binding` | claim / visible quote / overlay / summary 跨层 typed fact 一致性记录 | `news-evidence-pip` | `quality_review` / candidate gate |
| 内容 Brief | `content_brief` | 内容 Brief | `content-brief-compiler` | `short_video_structure_plan` / `draft` |
| 短视频结构计划 | `short_video_structure_plan` | 新稿事前结构或直供稿现状结构诊断 | `short-video-structure-planner` | `draft` / `content_beat_map` / `script_design_review` |
| 口播草案 | `draft` | baseline、新稿或授权修订的版本化口播正文 | `copywriting-draft-writer` | `content_beat_map` |
| 全文内容节点图 | `content_beat_map` | 以 UTF-8 字节锚点完整覆盖正文并绑定当前结构 | `content-beat-mapper` | `short_video_structure_plan` / `script_design_review` / `visual_need_analysis` |
| 脚本设计审查 | `script_design_review` | 当前结构、节点和口播表达的只读审查 | `spoken-script-review` | `content_revision_decision` / `visual_need_analysis` / `final_delivery` |
| 内容修订决策 | `content_revision_decision` | 当前 revision 的继续、改稿、等待授权或停止记录 | `spoken-script-review` / 人类 | readiness / `draft` / `visual_need_analysis` |
| 静态视觉编导方案 | `static_visual_director_plan` | 静态图片编导方案 | `talking-head-image-pip` | `visual_plan` |
| 视觉需求分析 | `visual_need_analysis` | 逐语义节点判断图片是否确有注意力、理解、证据、演示、情绪或记忆价值 | `static-visual-director` | `visual_plan` / `image_prompt_set` / `quality_review` |
| 视觉覆盖账本 | `visual_coverage_ledger` | current structure-bound 全文节点的逐项视觉处置、任务、occurrence 和数量账本 | `static-visual-director` | 各 visual producer / `script_visual_alignment_review` / `final_delivery` |
| 视觉来源路由 | `visual_source_route` | 每个 current 视觉任务的互斥来源分类、生产路径与测试 profile | `static-visual-director` | `news-evidence-pip` / `image-asset-producer` / existing asset |
| 资产复用授权 | `asset_reuse_authorization` | 精确绑定 session、task、资产 hash 与账号快照的不可变复用授权 | 人类 / 账号档案迁移 | `image-asset-producer` / `final_delivery` |
| 视觉文字方案 | `visual_text_plan` | 画中画视觉文字决策与来源方案 | `static-visual-director`（由 `talking-head-image-pip` 编排） | `image_prompt_set` / `visual_text_quality_gate` |
| 画中画方案 | `visual_plan` | 画中画视觉方案 | `talking-head-image-pip` | `quality_review` |
| 文案—视觉对齐审查 | `script_visual_alignment_review` | 当前文案、节点、视觉处置和素材的联合审查 | `copywriting-quality-review` | `platform_package_input` / 局部返工 |
| 质检报告 | `quality_review` | 历史联合质检兼容对象 / 封面专项复检 | `copywriting-quality-review` | `platform_package_input` |
| 分发包装输入包 | `platform_package_input` | 分发包装输入包 | `platform-packaging-adapter` | `platform_package` |
| 多平台分发包 | `platform_package` | 多平台分发包 | `platform-packaging-adapter` | `cover_design_package` |
| 封面设计包 | `cover_design_package` | 发布前封面设计方案 | `cover-design-compiler` | `cover_composition` |
| 封面合成记录 | `cover_composition` | 可上传封面合成与降级记录 | `cover-design-compiler` | `cover_quality_gate` |
| 内容交付记录 | `content_delivery_record` | 内容交付记录 | `platform-packaging-adapter` / `propagation-router` | `cover-design-compiler` / `final_delivery` / `human_confirm` / `done` |
| 图片资产组 | `image_asset_set` | 图片资产组 | `image-asset-producer` / `cover-design-compiler` | `cover_quality_gate` / `final_delivery` |
| 最终图片交付组 | `image_asset_delivery_set` | 每个视觉任务唯一可交付成品 | `visual-asset-finalizer` | `script_visual_alignment_review` / `final_delivery` |
| 图片终态记录 | `asset_finalize_record` | 基础图、派生图、后工程与复核的终态绑定 | `visual-asset-finalizer` | `image_asset_delivery_set` |
| 图片生成记录 | `image_generation_record` | 图片生成记录 | `image-asset-producer` | `image_asset_set` |
| 图片元数据 sidecar | `image_metadata_sidecar` | 图片元数据追溯文件 | `image-asset-producer` | `image_asset_set` |
| 静态视觉质量门禁 | `static_visual_quality_gate` | 静态图片质量门禁 | `copywriting-quality-review` | `quality_review` |
| 视觉文字质量门禁 | `visual_text_quality_gate` | 画中画视觉文字增量、可读性与证据门禁 | `copywriting-quality-review` | `quality_review` / `final_delivery` |
| 封面质量门禁 | `cover_quality_gate` | 封面成品质量门禁 | `copywriting-quality-review` | `final_delivery` |
| 图片追溯质量门禁 | `asset_trace_quality_gate` | 图片链路追溯门禁 | `copywriting-quality-review` / `final-delivery-builder` | `final_delivery` |
| 封面变体组 | `cover_variant_set` | 封面候选与推荐组 | `platform-packaging-adapter` | `cover_design_package` |
| 最终交付页 | `final_delivery` | 最终 HTML 验收页 | `final-delivery-builder` / `propagation-router` | `workflow_session_record` |
| 可转交交付包 | `export_bundle` | 可转交交付包 | `final-delivery-builder` / `propagation-router` | `workflow_session_record` |
| 执行透明度记录 | `execution_trace` | Skill 执行轨迹 | 各专项 skill / `propagation-router` | `workflow_session_record` |
| 工作流检查报告 | `workflow_check_report` | 只读 Checker 报告 | `propagation-router` / 后续 checker | `workflow_session_record` |
| 支持日志包 | `support_log_bundle` | 外部测试反馈日志包 | support log exporter / `propagation-router` | 维护者复盘 / issue 反馈 |
| 发布检查报告 | `release_check_report` | public_release / zip / sha256 检查报告 | checker / build 工具 | `release_record` |
| 样例检查报告 | `sample_check_report` | examples / sample-run 检查报告 | checker / build 工具 | `sample_record` |
| 工作流回放报告 | `workflow_replay_report` | 样例 / dry-run trace 只读回放报告 | runner-lite / checker | `sample_record` / `workflow_check_report` |
| 会话执行计划 | `session_execution_plan` | 单 session 轻量运行计划 | P0 runtime compiler | `execution_event_log` |
| 执行事件日志 | `execution_event_log` | 追加式步骤执行事件 | P0 runtime | `artifact_lineage_manifest` |
| 资产血缘清单 | `artifact_lineage_manifest` | 已 materialized 资产的 producer / input 关系 | P0 runtime | `deterministic_final_delivery_render_input` |
| 资产检查集合 | `artifact_check_set` | 资产质量检查执行记录 | checker / agent review / human review | `artifact_lineage_manifest` / `deterministic_final_delivery_render_input` |
| 最终交付渲染输入 | `deterministic_final_delivery_render_input` | renderer 专用、规范化输入 | final-delivery-builder | `final_delivery` |
| P0 合同兼容矩阵 | `p0_contract_compatibility_matrix` | 旧合同 replay / resume / render 兼容规则 | P0 runtime governance | `session_execution_plan` / resume gate |
| R7 工作流蓝图 | `workflow_blueprint` | 单篇直供 / 热点业务节点、依赖和路由定义 | `semantic-workflow-coordinator` / product governance | `semantic_task_envelope` |
| R7 节点注册表 | `workflow_node_registry` | 节点 Skill、输入选择器、输出合同和失败路由登记 | product governance | `workflow_blueprint` / coordinator |
| R7 合同状态注册表 | `contract_status_registry` | active / pending / superseded 的机器状态真源 | product governance | Skill / checker / 状态页 |
| R8 Skill 上下文注册表 | `skill_context_registry` | 项目业务 Skill 职责、主输入输出、node owner、入口摘要 / 行数、current / legacy 与条件引用清单 | product governance | R8 checker / 后续 Skill 编译 |
| R8-H5 语义案例 | `h5_semantic_case v0.2` | 双臂共享的业务意图、事实、目标和结果不变量 | semantic case recorder | baseline / candidate input adapter |
| R8-H5 依赖快照 | `h5_dependency_snapshot v0.2` | 单臂 entry、CONTRACT、直接引用和机器真源的不可变依赖闭包 | dependency snapshot builder | input compiler / arm executor |
| R8-H5 单臂输入 | `h5_arm_input v0.2` | 对应版本可直接消费且已通过 Schema 的 canonical typed input | versioned arm adapter | arm executor |
| R8-H5 单臂结果 | `h5_arm_result v0.2` | 单臂执行状态、客观观察、业务产物引用和输出门禁 | arm executor / result recorder | machine evaluator / comparability gate |
| R8-H5 机器结论 | `h5_machine_verdict v0.2` | baseline、candidate 与共享不变量的分离机器审计 | machine evaluator | comparability gate / finalizer |
| R8-H5 可比性结论 | `h5_comparability_verdict v0.2` | 区分质量可比、行为审计、回归处置、样本不足和无效 | comparability evaluator | blind pair generator / finalizer |
| R8-H5 匿名对 | `h5_blind_pair v0.2` | 只含机器通过且可比较的匿名业务产物 | blind pair generator | human verdict recorder |
| R8-H5 人类结论 | `h5_human_verdict v0.2` | 对匿名 A / B / tie 的业务判断和理由 | human owner / deterministic recorder | finalizer |
| R8-H5 最终定案 | `h5_evaluation_finalization v0.3` | H5 总状态、显式 blockers 与 current switch readiness 的唯一真源；v0.2 只作历史兼容 | deterministic finalizer | aggregate checker / state projection |
| Kernel 直供运行请求 | `kernel_direct_shadow_request v0.1` | M2 直供完整正向 typed result 与冻结比较输入 | direct shadow fixture / caller | workflow kernel direct adapter |
| Kernel 热点追加命令 | `kernel_hotspot_shadow_command v0.1` | M3 start/resume、上一命令 digest、结果切片、外部活动和 stop/replan 目标 | hotspot shadow fixture / caller | workflow kernel hotspot adapter |
| Kernel 外部活动记录 | `kernel_external_activity_record v0.1` | research/freshness 的 request、attempt、outcome、output reference、acceptance 与零重试事实 | external activity recorder / reconciler | hotspot result consumer / projection |
| Kernel shadow 观察 | `kernel_shadow_observation v0.1` | 输入、artifact/event 投影、stop、next component、final HTML 与外部活动计数 | deterministic projection builder | parity / branch checker |
| Kernel session 代际绑定 | `kernel_session_runtime_binding v0.1 / v0.2` | M4 历史 binding 与 M5 current / legacy 条件绑定；current v0.2 禁止兼容目录摘要 | deterministic session entry | propagation router / generation-pinned runtime |
| Kernel session 入口决定 | `kernel_session_entry_decision v0.1` | start/resume 的代际、执行入口、binding 状态和选择原因 | deterministic session entry | propagation router / caller |
| Kernel 兼容解析 | `kernel_compatibility_resolution v0.1` | M5 对 version-pinned legacy blueprint 的只读解析、catalog digest 与禁止迁移声明 | compatibility loader | legacy generation-pinned runtime |
| R7 动作注册表 | `action_registry` | 交付动作 code 与作用目标登记 | product governance | task envelope / candidate compiler |
| R7 语义任务单 | `semantic_task_envelope` | 当前 projection 唯一下一步的不可变 typed 任务 | `semantic-workflow-coordinator` | 指定 `skill_ref` |
| R7 语义提交 | `semantic_artifact_submission` | Codex 按任务单返回的待确定性提交对象 | 指定 semantic Skill | R7 submitter |
| R7 当前指针 | `semantic_current_pointer` | revision / lineage 成功后最后提交的 current 物理标记 | R7 submitter | 下游 selector |
| R7 提交收据 | `semantic_commit_receipt` | pointer-last 各阶段与中断恢复证据 | R7 submitter / reconciler | coordinator / checker |
| R7 Producer adapter 注册表 | `producer_adapter_registry` | node 到 payload Schema / artifact type / 校验模式的唯一映射 | product governance | submission builder |
| R7 兼容矩阵 | `r7_contract_compatibility_matrix` | current v0.6 与 v0.5 及更早合同的 new / replay / resume 边界 | product governance | coordinator / migration gate |
| R7 视口验收报告 | `viewport_acceptance_report` | 浏览器调用、DOM 测量、截图 hash、视觉结论与自主完成计量 | final-delivery-builder / viewport runtime | `workflow_session_record` |
| R7 业务交付验收 | `business_delivery_acceptance` | 对 HTML 、视口截图和真实图片的六维人工业务验收 | `business-delivery-acceptance` | `workflow_session_record` |
| R8 最终人工决定 | `final_delivery_human_decision v0.1` | 用户对当前交付的采纳、返修、导出或归档语义决定 | `final-delivery-decision-gate` / deterministic recorder | `final_delivery_decision_apply` |
| R8 会话决定应用 | `workflow_session_record v0.4` | 确定性应用已提交决定后的会话状态新 revision | `final_delivery_decision_apply` | done / coordinator / handoff / archive |
| R7 交付返修请求 | `delivery_revision_request v0.1` | 一次人工退回的 1..N 原子修改项、最早重启节点和失效并集 | human recorder / semantic coordinator | plan revision v0.9 / owning producer |
| R7 热点研究请求 | `hotspot_research_request v0.1` | 冻结账号、策略、研究模式与触发引用的不可变请求 | deterministic request compiler | `hotspot-topic-research` |
| R7 热点研究集 | `hotspot_research_set v0.1` | 聚合 R5 run / signal / event / candidate / topic option / evidence / source 的单一提交物 | `hotspot-topic-research` | panel projector / freshness / audit |
| R7 选题面板 v0.2 | `topic_selection_panel v0.2` | 从研究集 panel model 确定性生成的只读选择投影 | deterministic panel projector | Topic Gate |
| R7 选题决定 | `topic_selection_decision v0.1` | 不改写选题与证据的不可变人工决定 | `topic-selection-decision-gate` / deterministic recorder | selected source / rerun / R2 branch / archive |
| R7 已选热点来源 | `selected_topic_source v0.1` | 绑定研究、选择、证据、时效策略与双摘要的唯一内容来源 | deterministic selected source compiler | hotspot Brief / freshness |
| 回归套件报告 | `regression_suite_report` | 多样例 regression fixture 汇总报告 | checker / runner-lite | `release_check_report` / CI |
| 样例记录 | `sample_record` | 开源样例说明与验收记录 | examples / checker | `release_check_report` |
| 发布记录 | `release_record` | GitHub release 分层状态记录 | release governance | `human_confirm` / `done` |
| 发布门禁报告 | `release_gate_report` | release commit / tag / remote / push 前的人类门禁报告 | release governance / checker | `human_confirm` |
| 工作流状态记录 | `workflow_session_record` | 本轮工作流状态记录 | deterministic state apply / coordinator | `human_confirm` / `done` |

---

## 三、字段命名规范

| 规则 | 写法 |
|---|---|
| ID 字段 | `{artifact}_id`，例如 `topic_id`、`brief_id`、`draft_id` |
| 状态字段 | `{artifact}_status`，例如 `brief_status`、`review_status` |
| 下游 skill | `next_skill` |
| 来源 ID | `source_{artifact}_id`，例如 `source_review_id` |
| 调研来源 ID | `source_research_run_id` |
| 产物路径 | `artifact_path` 或 `artifact_paths`，必须指向 `accounts/{账号名}/runs/{session_id}/` 下的文件 |
| 布尔判断 | 用 `is_` / `has_` 前缀，或直接用状态值；避免混用 `pass` |
| 中文字段 | 可以出现在展示层，但旁边必须有标准技术字段 |

禁止新增这些模糊字段：

```text
状态
pass
是否通过
是否进入下一步
下一步
Brief 状态
draft 状态
package_pass
```

如需展示，统一写成：

```text
- brief_status：brief_pass
- next_skill：copywriting-draft-writer
- 给用户的引导语：这版 Brief 能写稿了，我会进入口播草案。
```

---

## 四、标准交接物字段

### -1. `entry_router_request`

`entry_router_request` 用于 P2 Quickstart 入口识别。它把用户的人话入口归一成标准路由，不生产内容。

```text
entry_request_id
entry_intent
entry_phrase
entry_case
entry_route
account_resolution_status
entry_confidence
entry_resolution_reason
entry_preflight_status
safe_start_mode
sample_run_offered
first_response_card_status
resume_requested
checker_requested
image_generation_capability_notice
capability_matrix_status
troubleshooting_route
next_visible_step
output_location_hint
next_skill
human_prompt
human_reply_examples
artifact_path
```

`entry_case` 允许值：

```text
first_use_no_account
existing_account_run
switch_account
resume_last
checker_only
image_capability_question
unknown_entry
```

`entry_confidence` 允许值：

```text
high
medium
low
```

`entry_route` 允许值：

```text
account-onboarding
propagation-router
hotspot-topic-research
workflow-checker
capability_notice
human_confirm
```

`account_resolution_status` 允许值：

```text
account_missing
account_exists_needs_confirmation
account_confirmed
account_choice_required
not_applicable
```

`entry_preflight_status` 允许值：

```text
pass
needs_account
needs_account_choice
needs_product_or_campaign
resume_available
checker_only
capability_notice
blocked
```

`safe_start_mode` 允许值：

```text
create_account
use_existing_account
run_sample
resume_last
check_only
capability_answer
ask_clarifying_question
```

`sample_run_offered` 允许值：

```text
yes
no
not_applicable
```

`first_response_card_status` 允许值：

```text
card_ready
card_not_needed
card_blocked
```

`image_generation_capability_notice` 允许值：

```text
codex_can_generate
non_codex_prompt_only
unknown_check_runtime
not_requested
```

### 0. `account_onboarding`

`account_onboarding` 用于首次使用、没有账号或账号不存在时创建账号档案草案。它不做热点、不写文案，只负责把用户的人话输入整理成 `account_profile`。

```text
onboarding_id
account_slug
account_display_name
input_summary
missing_questions
onboarding_status
account_profile_status
confirmation_status
artifact_path
next_skill
human_prompt
human_reply_examples
auto_next_action
execution_trace_update
```

`onboarding_status` 允许值：

```text
draft_created
waiting_human_confirmation
confirmed
blocked
```

`account_profile_status` 允许值补充：

```text
account_profile_draft
account_profile_p0_ready
account_profile_confirmed
```

### 1. `account_profile`

```text
account
account_profile_path
positioning
business_goal
target_audience
core_business
current_stage
mother_topics
content_boundaries
conversion_path
profile_status
visual_identity_ref
visual_identity_status
column_visual_template_refs
visual_count_policy
publishing_platforms
target_duration
audience_priority
high_risk_topic_policy
account_startup_check_version
account_session_snapshot_template_ref
```

`profile_status` 允许值：

```text
profile_ready
profile_needs_info
profile_blocked
```

换账号或本轮首次使用账号时，`workflow_session_record` 或 `manifest.yaml` 必须记录：

```text
account_profile_confirmed_for_session
account_profile_confirmed_at
```

`account_profile_confirmed_for_session` 允许值：

```text
pending
yes
no_changes
needs_update
blocked
```

含义：

```text
pending：已读取账号档案，等待用户确认是否仍按此档案创作。
yes / no_changes：用户认可本轮继续使用该账号档案。
needs_update：用户指出账号实际情况变化，先修订档案或写入本轮临时覆盖。
blocked：账号档案无法确认，本轮不能进入热点研究。
```

R5-H1 account visual fields:

```text
visual_identity_ref：账号私有视觉身份文件的相对路径；未启用时可为空或 identity_draft。
visual_identity_status：identity_draft / identity_active / identity_needs_revision / identity_archived。
column_visual_template_refs：账号私有栏目视觉模板相对路径数组。
visual_count_policy：只能为 content_derived_by_r3_0_to_n；账号视觉身份不得设置图片数量、调用次数或成本上限。
```

R5-H2 account radar fields:

```text
radar_policy_ref：账号私有 account-topic-policy.yaml 的相对路径。
query_lexicon_ref：账号私有 query-lexicon.yaml 的相对路径。
hotspot_memory_ref：账号私有热点浅层记忆目录。
radar_policy_status：policy_draft / policy_active / policy_needs_review / policy_blocked。
used_car_priority_mode：只能为 direct_first。
new_car_spillover_threshold：只能为 3；本轮可核验 direct 候选少于该值才允许新车外溢。
new_car_spillover_proof_types：残值、置换、库存现金流、二手新能源流通或政策传导证明类型。
topic_exclusions：账号硬禁区；只有硬禁区、合规或用户明确封禁可令词条 blocked。
term counters：run_count / signal_assist_count / candidate_assist_count / selected_candidate_assist_count / rejected_candidate_assist_count。
```

R5-H5 account startup fields:

```text
publishing_platforms：本账号当前主要发布平台；为空时先口语补问，不从旧文章猜测。
target_duration：本账号当前单篇目标时长；为空时与平台同组补问。
audience_priority：buyer / seller / dealer 的当前排序或均衡选择；三类受众存在不等于已确认优先级。
high_risk_topic_policy：verify_mechanism_only / named_fact_with_sources；前者只做交叉核验和行业机制拆解，后者仍要求可核验来源。
account_startup_check：运行前输出 missing_fields / blocking_fields / non_blocking_fields / question_set_id / account_snapshot_ref / account_snapshot_status / next_skill。
account_snapshot_ref：当前 session 的版本化账号快照路径；下游读取它，不重新自由解释散文档案。
snapshot_id：同一 session 内按任务阶段保持唯一；热点研究沿用 `AS-{session_id}-002`，后续 topic / content / visual 快照使用阶段标记，禁止不同文件或不同摘要复用同一 snapshot_id。
snapshot_at：账号启动请求在调用前物化的 ISO 8601 时间（必须含 `Z` 或 UTC offset）；快照逐字继承，下游热点初始 `requested_at` 逐字复制。缺失或非法时阻断，禁止回落到 epoch 或运行时临时补时。
account_snapshot_status：snapshot_ready / snapshot_needs_input / snapshot_policy_incomplete / snapshot_blocked。
startup_result：account_ready / account_needs_input / account_policy_incomplete / account_blocked。
account_identity_id：账号绑定版本的不可变主键；用于把账号档案、私有目录、策略、词库、视觉资产和 session 快照绑定为同一账号。
account_technical_slug：账号 ASCII 机器标识；不得以中文展示名或私有目录键代替。R5-H6 已编译该字段；现有 `account_slug` 仅表示历史私有目录键，不能误作技术身份。
identity_binding_ref / identity_binding_digest：当前账号绑定文件和去除 digest 字段后的稳定 SHA256；下游只接受已验证的绑定版本。
identity_verified：只有账号目录、展示名、技术身份、绑定摘要、策略 / 词库 / 视觉引用和 session 快照均同源时为 true。
identity_errors：绑定缺失、错绑、根目录逃逸、摘要变化或旧快照串号的机器错误码；不得转成口语补问。
```

### 1.2 `account_startup_check`

```text
account_slug
account_identity_id
account_technical_slug
identity_binding_ref
identity_binding_digest
identity_verified
identity_errors
session_id
task_type
startup_result
account_snapshot_ref
account_snapshot_status
source_account_slug
account_switch_isolated
missing_fields
blocking_fields
non_blocking_fields
unasked_blocking_fields
question_set_id
questions
high_risk_topic_policy
next_skill
human_gate_required
```

规则：每轮最多 3 个自然语言问题；热点 / 选题 / 内容任务缺视觉身份时仅列入 `non_blocking_fields`，视觉交付才将它列为阻断字段。账号切换必须重建当前账号的快照，不能传入前一账号的快照、词库、候选或偏好。R5-H6 中身份绑定缺失、错配、目录越界、摘要变化或旧快照错绑必须输出 `account_identity_inconsistent` / `snapshot_identity_inconsistent` 并阻断，不纳入补问。

### 1.3 `account_radar_policy`

```text
policy_id
account_slug
policy_status
audience_priority
used_car_priority_mode
new_car_spillover_threshold
new_car_spillover_proof_types
topic_priority_rules
topic_exclusions
manual_watchlist_policy
lexicon_feedback_policy
```

`lexicon_feedback_policy` 只能为 `exploratory_selection_feedback`；扩词可探索，`selected_candidate_assist_count` 只表示辅助选择，不表示单词独占因果。

### 1.1 `account_visual_identity`

`account_visual_identity` 是账号级表达合同，供 `static-visual-director` 在单篇 R3 视觉需求分析前读取；它不生成图片、不替代 `visual_need_analysis`，也不包含真实账号的运行记忆。

```text
identity_id
account_slug
account_identity_id
account_technical_slug
identity_status
visual_promise
evidence_visual_grammar
cover_hierarchy
tone_and_color_direction
negative_aesthetics
image_count_policy
column_visual_template_refs
identity_override_policy
```

`identity_status` 允许值：

```text
identity_draft
identity_active
identity_needs_revision
identity_archived
```

`image_count_policy` 允许值：

```text
content_derived_by_r3_0_to_n
```

新建或启用的 `account_visual_identity` 使用 `taoge://schemas/r5/account-visual-identity/v0.2`；`account_identity_id` 与 `account_technical_slug` 必须和当前账号 binding、栏目模板及 session 视觉快照一致。v0.1 仅保留历史 replay。

### 1.2 `column_visual_template`

```text
column_template_id
account_identity_id
account_technical_slug
column_name
applies_when
viewer_job
preferred_visual_roles
cover_hierarchy_override
visual_rules
prohibited_patterns
```

同一单篇可选择零或一个适用栏目模板；没有适用模板时遵循账号视觉身份。栏目模板不能修改图片数量规则，也不能把真实回归的图片数写成默认值。

新建或启用的栏目模板集合使用 `taoge://schemas/r5/column-visual-templates/v0.2`，并与视觉身份使用同一技术身份标记。

### 2. `product_profile`

`product_profile` 记录“这轮内容服务哪个产品、服务或观点对象”。账号回答“谁来说”，产品对象回答“说什么、不能怎么说、希望用户下一步做什么”。

```text
product_profile_id
product_name
product_type
source_project
positioning
target_audience
core_capabilities
allowed_claims
forbidden_claims
conversion_goal
cta_boundary
proof_assets
risk_notes
product_profile_status
next_skill
```

`product_profile_status` 允许值：

```text
product_profile_ready
product_profile_needs_info
product_profile_blocked
```

### 3. `campaign_profile`

`campaign_profile` 记录“一次活动、一次推广周期或一次内容主题包”的边界。没有独立活动时，可以为空；但如果用户明确说“这次推广免费学习版 / GitHub 下载 / 某个申请页”，就必须使用它。

```text
campaign_profile_id
campaign_name
campaign_goal
campaign_period
target_audience
offer_or_hook
allowed_claims
forbidden_claims
conversion_path
success_metric
risk_notes
campaign_profile_status
next_skill
```

`campaign_profile_status` 允许值：

```text
campaign_profile_ready
campaign_profile_needs_info
campaign_profile_blocked
campaign_profile_not_used
```

### 4. `research_run_record`

`research_run_record` 记录“这一轮调研怎么来的”，不是热点候选本身。它解决来源可信度、时间、搜索边界和为何采用/淘汰的问题。

```text
research_run_id
account
product_profile_id
campaign_profile_id
research_goal
time_range
hotspot_time_window
hotspot_freshness_status
content_position
source_plan
sources_used
search_queries
source_quality_notes
fact_check_notes
candidate_count
selected_candidate_count
discarded_candidate_count
risk_notes
research_status
next_skill
```

`research_status` 允许值：

```text
research_planned
research_done
research_needs_more_sources
research_blocked
```

`hotspot_time_window` 允许值：

```text
breaking_0_24h
live_hot_1_3d
current_hot_3_7d
warm_trend_7_30d
background_trend_30_90d
evergreen_90d_plus
```

`hotspot_freshness_status` 允许值：

```text
fresh_enough
aging_but_usable
too_old_for_hotspot
evergreen_only
unknown_time_blocked
```

`content_position` 允许值：

```text
breaking_hotspot
current_hotspot
industry_trend
evergreen_problem
case_review
methodology_content
```

来源质量必须写清楚：

```text
来源是否公开可访问
发布时间是否清楚
热度信号是否能解释
是否存在单来源风险
是否需要二次核验
```

### 5. `hotspot_candidate`

```text
hotspot_id
account
source_research_run_id
hotspot
hotspot_pool
source
source_time
hotspot_time_window
hotspot_freshness_status
content_position
one_sentence_fact
heat_signal
fact_level
lifecycle
possible_mother_topic
routing_chain
weakest_jump
risk_notes
candidate_status
```

`candidate_status` 允许值：

```text
candidate_new
candidate_scored
candidate_filtered
candidate_archived
```

### 6. `topic_card`

```text
topic_id
account
product_profile_id
campaign_profile_id
source_research_run_id
topic_title
hotspot
hotspot_pool
source
source_time
hotspot_time_window
hotspot_freshness_status
content_position
heat_signal
fact_level
lifecycle
target_audience
mother_topic
strategy
product_capability
bridge_status
gate_result
derivation_chain
weakest_jump
must_not_say
risk_notes
content_format
content_type
addon_suggestions
topic_status
next_skill
```

`topic_status` 允许值：

```text
topic_pending_human_choice
topic_selected_for_brief
topic_needs_fields
topic_needs_bridge
topic_rejected
```

别名处理：

```text
选题名称 / 标题 -> topic_title
热点池 -> hotspot_pool
目标人群 -> target_audience
账号母题 -> mother_topic
产品能力 / 对应产品能力 -> product_capability
不能怎么讲 -> must_not_say
风险 -> risk_notes
内容形式 -> content_format
内容类型建议 -> content_type
外挂项建议 -> addon_suggestions
status -> topic_status
```

### 6.1 `topic_selection_panel`

`topic_selection_panel` 是 Topic Gate 给用户看的“选题候选反馈面板”。它不替代 `topic_card`，而是解释本轮热点探索做到了什么、为什么筛掉一批、为什么推荐这几个，以及用户选不同题的收益和代价。

```text
topic_selection_panel_id
source_research_run_id
account
product_profile_id
campaign_profile_id
panel_status
exploration_scope_summary
source_scope_summary
time_window_summary
raw_candidate_count
scored_candidate_count
main_recommendation_count
degraded_candidate_count
rejected_candidate_count
filtered_reason_summary
recommended_topic_id
topic_option_ids
topic_role_map
selection_tradeoff_map
recommendation_reason
human_prompt
human_reply_examples
decision_type
next_skill
artifact_path
```

`panel_status` 允许值：

```text
panel_draft
panel_ready_waiting_human
panel_selected
panel_needs_rerun
panel_archived
```

`topic_role` 允许值：

```text
main_recommendation
discussion_candidate
experimental_candidate
low_risk_candidate
backup_candidate
```

字段关系：

```text
raw_candidate_count：进入热点候选池的原始候选数。
scored_candidate_count：进入热点评分表的候选数。
main_recommendation_count：进入主推荐区的候选数。
degraded_candidate_count：降级为轻观点、行业趋势、复盘或方法论的候选数。
rejected_candidate_count：淘汰或不展示的候选数。
filtered_reason_summary：只写原因摘要，不重复整张候选池。
topic_option_ids：只引用 topic_card 的 topic_id，不复制完整选题卡。
topic_role_map：topic_id -> topic_role。
selection_tradeoff_map：topic_id -> 选择收益和代价。
recommended_topic_id：默认推荐的 topic_id，必须存在于 topic_option_ids。
```

禁止：

```text
不得只给 3 个标题而不输出 topic_selection_panel。
不得用 candidate_funnel 这类不可拆字段替代 raw/scored/main/degraded/rejected 五个计数字段。
不得把被筛掉的候选静默删除；至少要在 filtered_reason_summary 说明主因。
```

### 6.2 `direct_content_intake` / `direct_content_card`

R6-C01 至 C06 允许用户原稿作为一等内容入口。它只跳过不适用的热点发现 / 选题选择，不能跳过账号、Brief、主张处理、视觉、质检、平台包装、封面、最终交付、trace 或 finalize。

```text
schema_id = taoge://r6/direct-content-intake/v0.1
schema_version = 0.1.0
intake_id
content_source_id
session_id
account.account_id / account_slug / account_display_name
account.identity_binding_id / account_snapshot_id
content_origin = user_supplied_draft
topic_origin = direct_user_input
direct_intent
revision_policy
structural_rewrite_requested
original_draft.artifact_id / relative_path / sha256 / character_count
content_goal
target_audiences
claim_map
direct_content_status
human_gate
next_skill
lineage.producer_skill / consumer_skill / input_artifact_ids / output_artifact_ids
```

`direct_intent` 允许值：

```text
direct_delivery
direct_polish
direct_evidence_enrichment
direct_to_radar
```

`revision_policy` 允许值：`preserve_voice / polish_allowed / rewrite_allowed`。默认 `preserve_voice`；若结构性改写可能改变主旨、受众、指控强度、事实确定性或 CTA，且未获 `rewrite_allowed`，必须写 `direct_content_status=needs_rewrite_confirmation`、`human_gate=true`、`next_skill=human_confirm`。

普通直供通过时写 `direct_content_status=direct_content_ready`、`next_skill=content-brief-compiler`；只有 `direct_to_radar` 才写 `next_skill=hotspot-topic-research`。禁止为直供稿伪造 `topic_id` 或 `source_research_run_id`，也不得把它计入雷达覆盖与扩词效果。

### 6.3 `claim_card`

```text
claim_id
source_text
claim_type：opinion / experience / factual_claim / quote / statistic / prediction
claim_evidence_status：not_required / supported / refuted / not_enough_info / contested / not_checked
```

直供入口只做初始分类：`opinion / experience / prediction` 默认 `not_required`；`factual_claim / quote / statistic` 默认 `not_checked`。入口不能把未执行的核验预填成 `supported`，找不到来源也不能自动写成 `refuted`。

### 6.4 `source_record` / `source_capture_record`

```text
source_record：
  source_id / publisher / canonical_url / title / source_type
  published_at / accessed_at / source_access_status

source_capture_record：
  capture_id / source_id / captured_url / fixture_mode
  capture_at / viewport / selected_target.selector / selected_target.visible_quote
  selected_target.crop.x / y / width / height
  screenshot_path / sha256 / capture_status / capture_integrity_status
  attempt_number / attempt_history[] / started_at / completed_at / process_exit_code / error_category
  image_production_path = source_capture
```

生产运行要求 `fixture_mode=false` 且 `captured_url=canonical_url`。`fixture_mode=true` 只允许公开脱敏 fixture 使用本地 `file:` 页面；公开包构建时会把静态 fixture URL 净化为不可访问的 `local-file-url-disabled:` 哨兵，checker 只把它当结构样例，不得把任一形式当真实外部来源证据。浏览器启动前先落 `capture_started`，返回或启动异常后先写 outcome/path/hash 或 `error_category`，再裁切或叠字。重复调用先核对已完成记录与文件 hash；一致时 `reused_verified`，不重新访问页面。失败或无输出中断后的重试必须增加 `attempt_number` 并把前次摘要写入 `attempt_history[]`，不能覆盖掉失败证据。

`source_access_status` 只回答页面能否访问；`capture_integrity_status` 只回答截图、URL、时间、片段和 hash 是否同源，两者都不直接证明主张为真。

### 6.5 `evidence_claim_binding`

```text
binding_id
claim_id
source_id
capture_id
claim_evidence_status
rationale
```

每个 binding 只连接一个主张、一个来源和一个捕获 revision。`supported / refuted / not_enough_info / contested / not_checked` 是来源与当前主张的关系；`not_enough_info` 是合法结果。`evidence_support` 只接受 `supported`，`contested` 必须降为有争议的 `context_signal`，不得排版成确定事实。

### 6.6 `evidence_screenshot_pip`

```text
pip_id / binding_id
asset_role：evidence_support / context_signal / text_source_card / none
creator_commentary
copyright_review_status / privacy_review_status / publish_risk_status
render_status：render_ready / rendered / downgraded / blocked
asset_path / asset_sha256
renderer_digest / template_digest
next_skill = copywriting-quality-review
```

来源截图固定走 `image_production_path=source_capture`，禁止使用 Image 2 模仿新闻、数据、公告或引语截图。确定性 renderer 必须同时展示来源区 / 来源条和账号 `evidence_visual_grammar.commentary_label` 标注的创作者解读区，不能让推论看起来像来源原话。

`evidence_support` 必须同时满足：来源可访问、捕获完整性 verified、证据关系 supported、版权 / 隐私 / 发布风险均 approved。免责声明不能替代任何 gate。真实截图只留在私有账号 run；公开仓只保留合成 fixture。

R6-C20 至 C50 与 R3-C125 至 C139 已确认并进入现行合同。单一 Hook / 密度分数不得决定 readiness；现行口播结构、节点、审查、决策和视觉覆盖字段见 7A 至 9.0.2。更广泛的写作质检增强仍可作为后续产品待办，但不能覆盖本轮已编译合同。

### 7. `content_brief`

现行 `content_brief@0.3.0` 在既有字段外必填：

```text
content_source_id / content_origin
account_snapshot_ref
audience_entry_state / audience_exit_state
core_promise / core_point
material_inventory[] / evidence_availability[]
account_voice_constraints[]
platform_viewing_context
depth_reasoning
```

`brief_pass` 按来源分流：热点入口进入 `short-video-structure-planner(design_before_draft)`；直供入口进入 `direct_baseline_draft / copywriting-draft-writer(materialize_user_baseline)`。不得统一硬写为先生成草案。

```text
brief_id
topic_id
account
product_profile_id
campaign_profile_id
source_research_run_id
topic_title
content_goal
target_audience
core_point
hotspot_fact
source
source_time
hotspot_time_window
hotspot_freshness_status
content_position
derivation_chain
weakest_jump
strategy
content_format
content_type
addon_suggestions
channel_suggestions
account_tone
product_mention_level
product_claim_boundary
must_not_say
opening_direction
body_structure
cta
success_metric
human_gate
brief_status
next_skill
```

`brief_status` 允许值：

```text
brief_pass
brief_needs_human_confirm
brief_risk_high
brief_needs_format_change
brief_blocked
```

### 7A. `short_video_structure_plan@0.1.0`

```text
structure_plan_id / structure_plan_revision
plan_mode：design_before_draft / diagnose_existing_draft
brief_id / content_source_id / content_origin / account_snapshot_ref
target_audience / audience_entry_state / audience_exit_state
core_promise / core_point / material_inventory[] / evidence_availability[]
account_voice_constraints[] / platform_viewing_context / depth_reasoning
strategy_registry_version
source_draft_ref / source_beat_map_ref
alternatives_considered[] / recommended_candidate_id / selected_candidate_id
selected_strategy_ref / transformation_level
stages[] / selection_reason / selection_decision_id / authorization_ref
selection_status：auto_selected / human_selected / waiting_human / blocked
plan_status：pending_selection / ready / ready_with_warnings / blocked / stale
next_skill
```

候选和 stage 都是内容派生 1 到 N，无产品上限；直供必须包含 `keep_current`。stage order 从 1 连续；策略读取 `routes/content-structure-strategies.yaml@0.1.0`，`custom` 必须说明内置策略不适配原因。

### 7B. `content_beat_map@0.1.0`

```text
beat_map_id / beat_map_revision
mapping_phase：semantic_only / structure_bound
draft_id / draft_revision / normalized_body_digest
account_snapshot_ref / target_audience / platform_viewing_context
anchor_unit=utf8_byte / anchor_interval=half_open
structure_plan_ref
beats[]：beat_id / order / start_byte / end_byte / source_text_digest / semantic_function /
  viewer_state_before / viewer_state_after / information_increment / dependency_beat_ids[] /
  claim_ids[] / evidence_ids[] / stance_strength / emotion_function / spoken_intent / stage_id
full_coverage_status：complete / incomplete / overlap / invalid_anchor
unresolved_ranges[]
mapping_status：ready / blocked / stale
next_skill
```

正文每个非空白 UTF-8 字节必须恰好属于一个 beat。`semantic_only` 只供直供结构诊断；R3 只消费 current `structure_bound` revision。

R7 直供 blueprint v0.2 对同一类型连续提交两个不可变对象：

```text
semantic_beat_map node -> mapping_phase=semantic_only -> beat_map_revision=1
direct_structure_plan node -> source_draft_ref / source_beat_map_ref 均已 materialized
content_beat_map node -> mapping_phase=structure_bound -> beat_map_revision=2
```

`semantic_task_envelope`、结构计划和 submission 禁止引用未来 artifact。artifact commit registry 通过 `revision_field` 从 payload 派生顶层 `output_revision`；同类型 current pointer 只允许 revision 单调增加，不能把第二个 beat map 再固定成 revision 1。

### 7C. `script_design_review` / `content_revision_decision`

`script_design_review@0.1.0` 是只读观察，记录当前 draft / structure / beat refs、审查维度、精确 issue、`issue_gate=advisory / authorization_required / hard_boundary` 和 `review_status`。`content_revision_decision@0.1.0` append-only 记录当前 review ref、`accept_current / revise_script / waiting_authorization / stop`、accepted advisory IDs、authorization refs 和派生 readiness。

```text
script_readiness：ready / ready_with_warnings / waiting_authorization / needs_revision / blocked / stale
```

只允许 `ready / ready_with_warnings` 进入视觉覆盖；接受 warning 不得修改或删除原 review。

### 8. `draft`

现行 `draft@0.3.0` 使用三模式：

```text
mode：materialize_user_baseline / generated / revision
draft_id / draft_revision / draft_status
brief_id / content_source_id / content_origin / account_snapshot_ref
original_draft_artifact_id / original_draft_digest / normalized_original_digest
normalized_body_digest
structure_plan_ref / review_ref / decision_ref / authorization_refs[]
script / artifact_path / next_skill
```

`materialize_user_baseline` 只允许 LF 规范化和元数据封装；normalized body digest 必须与 normalized original 相同。`generated` 必须绑定热点事前结构；`revision` 必须绑定覆盖语义变化的当前 decision / authorization。Hook / 密度分数只作诊断信号，不能独立决定进入视觉。

以下旧草案字段保留历史读取兼容：

```text
draft_id
brief_id
topic_id
account
source_research_run_id
content_format
title_options
hook_options
recommended_hook
hook_score
first_5_seconds_script
transition_sentence
script
product_mention
cta
visual_pre_marks
risk_notes
draft_status
next_skill
```

`draft_status` 允许值：

```text
draft_created
draft_needs_hook_fix
draft_needs_brief_fix
draft_blocked
```

### 9.0 `static_visual_director_plan`

`static_visual_director_plan` 是 R3 的静态图片编导层。它先把口播段落翻译成视觉角色、构图策略和留存任务，再进入 `visual_plan` / `image_prompt_set`；不得直接从内容语言跳到图片 prompt。

```text
static_visual_director_plan_id
draft_id
brief_id
source_research_run_id
account
content_duration_estimate
visual_need_analysis_id
first_screen_visual_task
visual_role_map
visual_language
style_anchor
composition_strategy
cover_strategy_hint
image_asset_type_plan
visual_text_plan_id
static_visual_director_status
artifact_path
next_skill
```

### 9.0.1 `visual_need_analysis`

现行 `visual_need_analysis@0.4.0` 只消费 current structure-bound beat map 和 `script_readiness=ready / ready_with_warnings`：

```text
visual_need_analysis_id / analysis_revision
draft_ref / structure_plan_ref / beat_map_ref
script_readiness
visual_count_policy=content_derived_0_to_n_unbounded
generation_policy=dispatch_each_accepted_task_by_disposition
codex_provider=codex_builtin_image2 / cost_gate=not_applicable / provider_call_limit=null
coverage_ledger_ref
analysis_status：ready / ready_with_warnings / waiting_assets / blocked / stale
next_skill
```

下述 `content_derived_unbounded / generate_all_accepted / derived_visual_count` 是 v0.3 及更早版本的历史兼容字段；新产物不得以其替代全文覆盖账本。

`visual_need_analysis` 是 R3-C71 到 C80 的现行数量真源。`visual_count_policy` 固定为 `content_derived_unbounded`；图片数量允许 0 到 N，不设最低 / 最高数量，不按时长、成本或 provider 调用次数分配。

```text
visual_need_analysis_id
static_visual_director_plan_id
draft_id
source_research_run_id
account
audience_profile_ref
audience_prior_knowledge：novice / mixed / expert / unknown
platform_viewing_context：mobile_feed / known_audience / other
visual_count_policy=content_derived_unbounded
generation_policy=generate_all_accepted
codex_provider=codex_builtin_image2
cost_gate=not_applicable
provider_call_limit=null
accepted_task_dispatch_policy=auto_continue_all_accepted_without_human_confirmation
human_confirmation_required=false
generation_dispatch_status：ready_for_prompt_compile / not_ready
next_skill：image-prompt-compiler / static-visual-director
cover_count_excluded=true
semantic_beats[]
candidates[]
accepted_visual_tasks[]
rejected_visual_candidate_ids[]
derived_visual_count
zero_visual_reason
visual_need_analysis_status：pass / needs_fix / blocked
```

`candidates[]` 每项：

```text
visual_need_candidate_id
beat_id
covered_beat_ids[]
trigger_text
insert_after_text
insert_before_text
viewer_problem_without_visual
attention_risk_without_visual：low / medium / high
comprehension_risk_without_visual：low / medium / high
primary_visual_job
supporting_visual_jobs[]
expected_viewer_change
information_added
why_image_is_better_than_talking_head
attention_trigger_basis：specific_content_risk / elapsed_time_only / not_applicable
emotion_congruence_status：aligned / not_applicable / misaligned
evidence_requirement：source_bound / generated_context_only / not_applicable
evidence_source_type / evidence_source_id / evidence_source_path
redundancy_status：unique / duplicates_narration / duplicates_other_visual
cognitive_load_risk：low / medium / high
misleading_risk：low / medium / high
visual_need_decision：generate / reject
decision_reason
```

`primary_visual_job` / `supporting_visual_jobs[]` 只允许：

```text
attention_reset
hook_amplification
concept_explanation
evidence_support
process_demonstration
emotion_amplification
memory_anchor
```

`accepted_visual_tasks[]` 每项固定包含 `image_task_id / visual_need_candidate_id / beat_id / primary_visual_job / generation_intent=render_now / provider_route=codex_builtin_image2`。`derived_visual_count` 必须等于 accepted 数组长度和 `generate` 候选数量；所有 generate 候选恰好对应一个 accepted task。Codex 内置 Image 2 必须 generate all accepted，不存在因成本、optional 或 call limit 跳过任务。

当 `visual_need_analysis_status=pass` 时，必须同时写 `accepted_task_dispatch_policy=auto_continue_all_accepted_without_human_confirmation`、`human_confirmation_required=false`、`generation_dispatch_status=ready_for_prompt_compile`、`next_skill=image-prompt-compiler`。H6A 与 H6B 是两个可审计 / 可恢复步骤，不是两次用户批准；pass 后必须自动接续。审美偏好只能作为首轮生成后的版本化返工输入，不能把 accepted task 停在 `human_confirm`。未解决的事实、来源、隐私、版权或承诺风险应在候选通过前 reject / 修复，不能以 accepted 状态等待确认。

`derived_visual_count=0` 时 `zero_visual_reason` 必填，且不得使用“时长太短 / 成本 / 调用上限”作为理由。封面任务继续单独计数。旧 `visual_budget / required_visuals / optional_visuals / default_* / final_* / selected_optional_count / reduction_reason / expansion_reason / expected_provider_call_count` 只读兼容历史 session，新产物禁止写入。

### 9.0.2 `visual_coverage_ledger@0.1.0`

```text
visual_coverage_ledger_id / ledger_revision
draft_ref / structure_plan_ref / beat_map_ref
coverage_records[]：每个 current beat 一个主处置和理由
accepted_visual_tasks[] / visual_insert_occurrences[]
provider_attempt_refs[] / source_capture_attempt_refs[]
counts：derived_visual_asset_count / materialized_visual_asset_count /
  provider_generation_task_count / provider_generation_attempt_count /
  source_capture_task_count / source_capture_attempt_count /
  visual_insert_occurrence_count / platform_rendition_count / cover_asset_count
coverage_completeness_status：complete / incomplete / stale
visual_delivery_readiness：ready / ready_with_warnings / waiting_assets / waiting_authorization / blocked / stale
unresolved_beat_ids[] / ledger_status / next_skill
```

主处置只允许 `generate_visual / create_deterministic_visual / use_source_evidence / use_existing_asset / reuse_visual_task / talking_head_intentional / evidence_blocked / manual_visual_required`。每项 current visual task 必须证明注意力、理解、证据、演示、情绪或记忆价值。`generate_visual` 严格对应 Image 2；其他路径不得产生 provider task。0 张只有在所有节点均为有具体理由的 `talking_head_intentional` 且无补充任务时才可 ready。

`static_visual_director_status` 允许值：

```text
director_plan_pass
director_plan_needs_fix
director_plan_blocked
```

`visual_role` 允许值：

```text
hook_conflict
comparison
mechanism_explainer
evidence_support
emotion_amplifier
scene_context
trust_builder
transition
memory_symbol
metaphor
cover_click
not_needed
```

兼容映射：旧值 `conflict / evidence / emotion / method / trust / memory` 只读迁移为 `hook_conflict / evidence_support / emotion_amplifier / mechanism_explainer / trust_builder / memory_symbol`，不得继续新增旧值。

`image_asset_type` 允许值：

```text
picture_in_picture_image
cover_image
```

### 9.0.1 `visual_text_plan`

`visual_text_plan` 只规划静态图片中的视觉文字层，不承担逐字字幕。它必须说明文字相对口播和字幕新增了什么信息，以及是否需要绑定证据来源。

```text
visual_text_plan_id
static_visual_director_plan_id
visual_plan_id
draft_id
source_research_run_id
account
narration_source_type
narration_source_path
subtitle_source_status
subtitle_source_path
visual_text_tasks
required_text_task_count
optional_text_task_count
forbidden_text_task_count
source_bound_task_count
plan_information_delta_summary
visual_text_plan_status
artifact_path
next_skill
```

`visual_text_decision` 允许值：

```text
forbidden
optional
required
```

`visual_text_tasks[]` 必须与 `visual_plan.accepted_visual_tasks[]` 中实际创建的图片任务一一对应。任务字段：

```text
visual_text_task_id
image_task_id
beat_id
visual_text_decision
decision_reason
visual_text_units
task_information_delta_summary
semantic_delta_score
effective_information_gain
visual_text_redundancy_status
cognitive_load_status
overflow_reason
source_binding_summary_status
visual_text_task_status
```

`visual_text_units[]` 是单个图片任务中的文字单元。单元字段：

```text
visual_text_unit_id
image_task_id
beat_id
content
visual_text_role
information_delta
speaker_or_target
placement
max_chars
is_source_required
evidence_source_type
evidence_source_id
evidence_source_path
source_binding_status
source_display_text
visual_text_render_strategy
```

`visual_text_role` 允许值：

```text
none
inner_voice
role_label
mechanism_label
comparison_label
evidence_label
data_point
context_marker
visual_narration
subtext
source_note
```

`visual_text_render_strategy` 允许值：

```text
no_text
model_text_in_image
deterministic_overlay
source_native_text
manual_design
prompt_only
```

`placement` 允许值：

```text
top_safe
left_subject
right_subject
bottom_safe
attached_to_subject
node_inline
source_footer
```

默认文字预算：

```text
role_label / comparison_label / context_marker：2-8 个汉字。
inner_voice / subtext / visual_narration：4-14 个汉字。
mechanism_label：每节点 2-6 个汉字，默认最多 4 个节点。
data_point / evidence_label：保留准确数值或原生短语，不为缩短而改写事实。
source_note：6-24 个汉字或短来源名；必须保证手机可读。
单张图默认 1-3 个 visual_text_unit；机制图最多 4 个节点，证据图可额外增加 1 个 source_note。
超过预算必须写 overflow_reason，并进入 cognitive_load_status 复检。
```

`effective_information_gain` 允许值：`low / medium / high`。它衡量单位注意成本获得的有效信息增量，不等于画面元素或文字数量。

`semantic_delta_score`、`glance_comprehension_score`、`mobile_readability_score` 使用 1-10 分。`optional / required` 含字任务的 `semantic_delta_score` 和两项可读分默认不得低于 7；5-6 为 needs_fix，1-4 为 blocked。`forbidden` 任务的 `semantic_delta_score` 可为空。

`visual_text_redundancy_status` 与 `cognitive_load_status` 允许值：

```text
pass
needs_fix
blocked
```

`source_binding_status` 允许值：

```text
not_required
source_bound
source_missing
source_conflict
```

`evidence_source_type` 允许值：

```text
research_source
source_asset
screenshot_asset
user_provided
not_applicable
```

`evidence_source_id` 负责稳定引用，`evidence_source_path` 负责本 session 可打开追溯；二者不能只写一个无法解析的自然语言来源名。

`subtitle_source_status` 允许值：

```text
available
not_available
not_applicable
```

`visual_text_task_status` 允许值：

```text
visual_text_task_pass
visual_text_task_needs_fix
visual_text_task_blocked
```

`visual_text_plan_status` 允许值：

```text
visual_text_plan_pass
visual_text_plan_needs_fix
visual_text_plan_blocked
```

硬规则：

```text
每个 image_task_id 必须恰好有一个 visual_text_task；不得用计划级单值代替逐图决策。
visual_text_decision=forbidden 时 visual_text_units 必须为空，下游 prompt 必须写 allow_text_in_image=false。
视觉文字不得只复制口播或字幕；没有 information_delta 时应 forbidden 或 needs_fix。
证据、数据、引语和来源说明必须 is_source_required=true，并在文字单元内写 evidence_source_id 和 source_binding_status=source_bound。
source_binding_status=source_bound 时 evidence_source_type / evidence_source_id / evidence_source_path 必须同时可解析；缺任一项只能写 source_missing。
模型生成的情境图不得标为 evidence_support；应降级为 scene_context / metaphor / emotion_amplifier，或因来源冲突 blocked。
narration_source_type 当前固定为 draft_script_text；narration_source_path 必须指向本 session 的 draft。
subtitle_source_status=not_available 时 subtitle_source_path 为空，但仍须与 draft 口播完成重复检查。
```

字段关系：

```text
picture_in_picture_image：服务正文画中画留存、解释、证据、情绪和信任。
cover_image：服务平台点击、推荐流停留、搜索 / 转发第一眼理解。
```

### 9. `visual_plan`

```text
visual_plan_id
static_visual_director_plan_id
draft_id
brief_id
source_research_run_id
account
content_duration_estimate
visual_need_analysis_id
visual_count_policy
derived_visual_count
first_screen_visual_task
visual_strategy
visual_language
visual_text_plan_id
beats
pip_table
image_prompt_set_id
image_prompts
negative_prompts
edit_notes
beats_count
accepted_visual_tasks
rejected_visual_candidates
not_recommended_visuals
image_prompts_count
image_asset_type_plan
visual_plan_status
next_skill
```

`visual_plan_status` 允许值：

```text
visual_plan_pass
visual_plan_needs_fix
visual_plan_blocked
```

### 10. `quality_review`

```text
review_id
review_mode
draft_id
brief_id
visual_plan_id
source_research_run_id
review_status
blocking_issues
quality_scores
retention_score
where_viewers_drop
suggestions
risk_notes
visual_quality_gate_status
static_visual_quality_gate_status
visual_text_quality_gate_status
cover_quality_gate_status
prompt_integrity_status
image_asset_trace_status
asset_trace_quality_gate_status
html_embed_readiness_status
source_review_time
next_skill
```

`review_status` 允许值：

```text
review_pass
review_needs_copy_fix
review_needs_visual_fix
review_needs_brief_fix
review_blocked
```

`review_mode` 允许值：

```text
content_visual_review
cover_review
```

`content_visual_review` 在平台包装前执行，`cover_quality_gate_status=not_applicable`；`cover_review` 在 cover composition 后执行，只返工封面层。

`static_visual_quality_gate_status` / `visual_text_quality_gate_status` / `cover_quality_gate_status` / `asset_trace_quality_gate_status` 允许值：

```text
pass
fail
not_applicable
not_run
```

### 11. `platform_package_input`

```text
package_input_id
account
product_profile_id
campaign_profile_id
source_research_run_id
brief_id
draft_id
visual_plan_id
visual_text_plan_id
image_asset_set_id
brand_or_product
content_goal
target_audience
target_platforms
core_topic
core_point
recommended_hook
first_5_seconds_script
first_screen_visual_task
video_body_summary
visual_style_summary
product_mention_level
product_claim_boundary
must_not_say
risk_words
cta
platform_goals
source_review_id
visual_text_quality_gate_status
image_asset_trace_status
asset_trace_quality_gate_status
html_embed_readiness_status
input_status
next_skill
```

`input_status` 允许值：

```text
input_pass
input_needs_fix
input_blocked
```

### 12. `platform_package`

```text
package_id
package_input_id
review_id
source_research_run_id
visual_plan_id
visual_text_plan_id
image_asset_set_id
visual_text_quality_gate_status
target_platforms
cover_title_options
recommended_cover_title
cover_image_source
cover_visual_concept_hint
platform_cover_strategy_hint
cover_layout_hint
cover_variant_set_id
video_title_options
recommended_video_title
publish_description_options
recommended_publish_description
hashtag_sets
recommended_hashtags
platform_notes
recommended_package
platform_goal
trigger_type
risk_notes
manual_publish_notes
package_status
next_skill
```

`package_status` 允许值：

```text
package_pass
package_needs_fix
package_blocked
```

### 12.1 `cover_design_package`

`cover_design_package` 是发布前封面设计方案，不等同于 `recommended_cover_title`。封面标题是文案；封面设计包必须说明封面图来源、版式、安全区、平台差异和质量门禁。

```text
cover_design_package_id
package_id
visual_plan_id
image_asset_set_id
source_research_run_id
account
target_platforms
platform_cover_specs
recommended_cover_title
cover_image_source
cover_background_asset_id
cover_visual_concept
cover_job
cover_visual_entry_type
cover_text_render_strategy
cover_layout
cover_safe_area
cover_text_overlay
platform_cover_notes
brand_system_ref
thumbnail_readability_status
cover_contract_render_alignment_status
platform_preview_status
platform_preview_evidence_path
cover_variant_set_id
cover_design_status
artifact_path
next_skill
```

`cover_image_source` 允许值：

```text
existing_image_asset
generated_cover
video_frame
manual_required
prompt_only
```

`cover_design_status` 允许值：

```text
cover_design_pass
cover_design_needs_fix
cover_design_blocked
```

`cover_visual_entry_type` 允许值：

```text
conflict
evidence
emotion
method
trust
result
```

`thumbnail_readability_status`、`cover_contract_render_alignment_status` 允许值：`pass / needs_fix / blocked`。

`platform_preview_status` 允许值：

```text
not_checked
preview_pass
preview_needs_fix
unavailable
```

`cover_text_render_strategy` 允许值：

```text
model_text_in_image
deterministic_overlay
manual_design
prompt_only
```

兼容规则：

```text
`platform-packaging-adapter` 历史样例中的 cover_design_package 可以读取，但新链路长期 producer 是 cover-design-compiler。
旧字段 cover_image_asset_id 只作为 cover_background_asset_id 的兼容别名读取，不再新增。
旧草案字段 render_strategy 统一迁移为 cover_text_render_strategy。
```

### 12.2 `cover_variant_set`

```text
cover_variant_set_id
package_id
source_research_run_id
variants
recommended_variant_id
recommend_reason
materially_distinct_variant_count
variant_set_status
artifact_path
next_skill
```

`variants[]` 单项字段：

```text
cover_variant_id
cover_visual_entry_type
cover_variant_difference_type
platform
cover_title
video_title
cover_image_source
source_image_asset_id
layout_spec
expected_click_reason
risk_note
cover_quality_gate_status
```

`cover_visual_entry_type` 允许值：

```text
conflict
evidence
emotion
method
trust
result
```

`cover_variant_difference_type` 允许值：

```text
title_only
crop
visual_concept
subject
composition
evidence
emotion
```

重要内容、信息密度高或跨平台发布时，至少要有 2 个非 `title_only` 的视觉假设；只换标题不算成熟的封面变体测试。

`variant_set_status` 允许值：

```text
variant_set_ready
variant_set_not_needed
variant_set_needs_fix
```

兼容规则：历史样例中的 `cover_design_package_id` 可作为下游回链读取，但 `cover_variant_set` 的上游必填引用是 `package_id`，避免平台包装与封面设计循环依赖。旧字段 `variant_role` 只读迁移为 `cover_visual_entry_type`，不得继续作为长期真源。

### 12.3 `cover_composition`

`cover_composition` 记录一次平台封面合成或 prompt 降级。它不负责选题、标题策略或视觉创意，只把已经确认的封面设计编译成可追溯成品。

```text
cover_composition_id
cover_design_package_id
cover_variant_id
package_id
source_research_run_id
platform
platform_cover_strategy
source_background_asset_id
cover_title
text_lines
typography_spec
layout_spec
cover_text_render_strategy
output_asset_id
output_path
cover_composition_status
quality_gate_status
artifact_path
next_skill
```

`platform_cover_strategy` 允许值：

```text
reuse
crop
retitle
independent_composition
prompt_only
```

`cover_composition_status` 允许值：

```text
composition_ready
composition_needs_fix
composition_not_generated
prompt_only
```

字段关系：

```text
output_asset_id 必须引用 image_asset_set.assets[] 中 cover_asset_role=cover_composited_asset / platform_cover_asset 的资产。
prompt_only 时 output_asset_id / output_path 可以为空，但必须有可复制 prompt、layout_spec 和人工动作。
旧草案字段 composition_status 统一迁移为 cover_composition_status。
```

### 12.4 `cover_quality_gate`

```text
cover_quality_gate_id
cover_design_package_id
cover_composition_id
cover_variant_id
platform
output_asset_id
text_accuracy_status
upload_readiness_status
small_screen_readability_score
title_visual_alignment_score
composition_score
platform_fit_score
click_reason_clarity_score
thumbnail_readability_status
cover_contract_render_alignment_status
platform_preview_status
quality_gate_status
blocking_issues
suggestions
checked_at
artifact_path
next_skill
```

`text_accuracy_status` 允许值：

```text
pass
needs_fix
not_applicable
```

`upload_readiness_status` 允许值：

```text
ready
needs_fix
prompt_only
blocked
```

### 12.4.1 `visual_insert`

`visual_insert` 是主视频中的静态视觉插入上位对象。`picture_in_picture` 只保留给 `speaker_plus_visual`；全屏解释图必须写 `full_frame_replace`，不得继续以 PIP 名义交付。

```text
visual_insert_task_id
image_task_id
presentation_mode
platform_surface_profile_id
video_canvas
visual_asset_canvas
placement_slot
speaker_region
caption_safe_area
platform_ui_safe_areas
protected_regions
aspect_ratio_verification_status
```

`presentation_mode` 允许值：

```text
full_frame_replace
speaker_plus_visual
split_screen
floating_card
source_evidence_card
background_plate
```

`video_canvas / visual_asset_canvas`：

```text
width_px
height_px
aspect_ratio.ratio_width
aspect_ratio.ratio_height
aspect_ratio.orientation：portrait / landscape / square
```

比例必须使用约分后的整数对；实际文件宽高必须单独记录，不得只在 prompt prose 中写“9:16”。

`placement_slot / speaker_region / caption_safe_area / platform_ui_safe_areas[] / protected_regions[]` 均使用目标视频画布左上角为原点的 0–1 归一化矩形：

```text
x：向右
y：向下
width > 0
height > 0
x + width <= 1
y + height <= 1
```

### 12.4.2 `platform_surface_profile`

```text
surface_profile_id
surface_profile_version
platform
surface_role：feed_cover / profile_grid_cover / share_cover
platform_priority：primary / secondary
target_canvas
safe_areas
profile_evidence_status：official_verified / observed_verified / provisional_not_verified
profile_evidence_ref
```

账号 session snapshot 必须绑定 profile ID / version。`provisional_not_verified` 允许本地构图，但不得宣称真实客户端规格已验证。

### 12.4.3 `cover_job / cover_rendition`

`cover_job` 表示一次点击承诺；`cover_rendition` 表示某个目标 surface 的不可变成品 revision。正文视觉需求数与封面 rendition 数分开计数。

```text
cover_job_id
cover_rendition_id
rendition_revision
platform
platform_priority
surface_profile_id
source_asset_id
source_canvas
target_canvas
adaptation_strategy
focal_point
protected_regions
required_visual_element_ids
crop_loss_justification
background_fill_spec
cover_title
title_safe_area
output_path
composition_record_path
preview_path
preview_evidence_type
visual_review_record_path
rendition_status
```

`adaptation_strategy` 允许值：

```text
reuse_native
focal_crop
fit_pad
outpaint_extend
independent_generation
manual_required
```

约束：

```text
reuse_native：源 / 目标比例原生兼容。
focal_crop：必须有 focal point、crop contract 和 required visual elements；任一必保元素被切即 fail。
fit_pad：必须有显式背景填充设计；默认黑边不合格。
outpaint_extend / independent_generation：必须绑定新的 provider_task_id / attempt。
manual_required：不得进入自动就绪。
```

`rendition_status` 单向推进：

```text
planned
composed
dimensions_verified
preview_ready
waiting_visual_review
visual_pass
visual_fail
```

修复必须新建 `rendition_revision`，禁止覆盖历史成品和审核记录。

`preview_evidence_type` 只允许：

```text
deterministic_surface_mock：由确定性模板生成的平台表面模拟图，不得称为真实平台截图。
manual_app_observed：人在真实平台界面观察并留存的预览证据。
```

### 12.4.4 `cover_composition_record_v0.2`

```text
schema_id：taoge://schemas/r3/cover-composition-record/v0.2
cover_rendition_id
rendition_revision
surface_profile_id
platform / platform_priority
adaptation_strategy
source_asset_id
source_canvas / target_canvas
crop_contract.crop_window
crop_contract.source_retained_area_ratio
crop_contract.required_visual_element_ids
crop_contract.protected_region_retention_status
crop_contract.crop_loss_justification
output_path / output_sha256
preview_path / preview_sha256
preview_evidence_type
profile_evidence_status
structural_gate_status
visual_review_status
cover_delivery_status
plan_sha256
composed_at
```

确定性 composer 只能写：

```text
structural_gate_status=pass
visual_review_status=not_reviewed
cover_delivery_status=waiting_visual_review
```

它无权根据尺寸、hash 或无溢出结果自行写视觉 pass。

### 12.4.5 `cover_visual_review`

```text
cover_visual_review_id
cover_rendition_id
rendition_revision
surface_profile_id
reviewer_type：codex_visual_review / human_visual_review
observation_mode：raster_inspection
review_scope：real_delivery / fixture_only
output_sha256
preview_sha256
visual_review_status：pass / fail
findings
review_statement
reviewed_at
```

`fixture_only` 只能验证合同，不得作为真实账号交付证据。真实交付 review 必须实际打开输出图和 preview，且绑定当前 hash。

### 12.4.6 `platform_delivery_scope_status`

```text
ready_all_target_platforms
primary_ready_secondary_pending
not_publish_ready
```

派生规则：

```text
唯一 primary platform 未 visual_pass -> not_publish_ready。
primary visual_pass，但任一次要平台未 visual_pass -> primary_ready_secondary_pending。
全部目标平台 visual_pass -> ready_all_target_platforms。
```

最终 HTML 必须用业务语言展示具体可用平台，不得把 `primary_ready_secondary_pending` 写成“全平台可发布”。

### 12.5 `visual_text_quality_gate`

```text
visual_text_quality_gate_id
visual_text_plan_id
image_task_id
image_asset_id
information_delta_status
narration_redundancy_status
subtitle_redundancy_status
glance_comprehension_score
mobile_readability_score
cognitive_load_status
source_binding_status
text_accuracy_status
quality_gate_status
blocking_issues
recovery_action
artifact_path
next_skill
```

`information_delta_status`、`narration_redundancy_status` 允许值：`pass / needs_fix / blocked`。

`subtitle_redundancy_status` 允许值：`pass / needs_fix / blocked / not_available`。当链路尚未生成字幕时必须写 `not_available`，并至少与 draft 口播文本完成重复检查。

阻断条件：required 文字缺失、forbidden 文字出现、视觉文字只是字幕复写、证据没有来源绑定、生成插图冒充证据、模型文字错误或手机小图不可读。

`recovery_action` 允许值：

```text
remove_visual_text
rewrite_information_delta
bind_existing_source
downgrade_visual_role
recompile_image_prompt
rerender_deterministic_overlay
request_manual_review
none
```

恢复路由：决策、信息增量、来源或角色问题回 `static-visual-director`；prompt 含字策略错误回 `image-prompt-compiler`；成品叠字错误回对应合成能力；只有风险边界无法自动判断时才进入人工复检。

`quality_gate_status` 允许值：

```text
pass
needs_fix
blocked
```

`visual_text_quality_gate_status` 是写入 `quality_review` 的跨 Skill 汇总状态，允许值与既有质量门禁一致：

```text
pass
fail
not_applicable
not_run
```

映射规则：单图 `quality_gate_status=pass` 汇总为 `pass`；任一 required 任务为 `needs_fix / blocked` 汇总为 `fail`；没有图片任务时为 `not_applicable`；门禁尚未执行时为 `not_run`。

### 13. `content_delivery_record`

`content_delivery_record` 是涛哥创作工作流的收口交接物。它不代表已经发布，也不触发任何平台自动发布；它只记录“这条内容已经生产到可人工处理的状态”，并把确认、返工、废弃或归档路径说清楚。

```text
delivery_id
package_id
package_input_id
review_id
visual_plan_id
visual_text_plan_id
image_asset_set_id
cover_variant_set_id
draft_id
brief_id
topic_id
product_profile_id
campaign_profile_id
source_research_run_id
account
topic_title
strategy
content_format
target_platforms
recommended_package_summary
artifact_paths
human_decision
revision_path
delivery_status
approval_status
publish_status
delivery_page_mode
final_delivery_status
image_assets_status
visual_text_quality_gate_status
export_status
next_skill
human_prompt
human_reply_examples
```

`delivery_status` 允许值：

```text
delivery_ready
delivery_needs_fix
delivery_confirmed
delivery_archived
delivery_discarded
```

`approval_status` 允许值：

```text
approval_pending
approval_approved
approval_needs_revision
approval_rejected
approval_not_required
```

`publish_status` 允许值：

```text
publish_not_started
publish_manually_published
publish_skipped
publish_unknown
```

`delivery_page_mode` 允许值：

```text
project_local
portable_bundle
standalone_html
not_generated
```

`final_delivery_status` 允许值：

```text
html_ready
bundle_ready
standalone_ready
needs_export
blocked
not_generated
```

`image_assets_status` 允许值：

```text
all_generated
partially_generated
pending_external
generation_failed
manual_required
mixed
not_required
```

`export_status` 允许值：

```text
not_requested
export_ready
export_needs_fix
export_blocked
```

状态含义：

```text
delivery_ready：平台包已通过，等待人工确认、发布或返工。
delivery_needs_fix：用户指出需要改，必须按 revision_path 回到对应上游；返工完成后自动重新生成 final_delivery，不要求用户再说“继续”。
delivery_confirmed：最终 HTML 验收通过后，用户认可这一版，可由人工自行发布。
delivery_archived：暂不发布，但保留本条内容资产。
delivery_discarded：放弃本条，不继续返工。
```

`revision_path` 只能写清楚可执行回退路径，不得写模糊词：

```text
back_to_platform_package
back_to_cover_composition
back_to_quality_review
back_to_visual_plan
back_to_draft
back_to_content_brief
back_to_topic_card
none
```

### 14. `image_asset_set`

```text
image_asset_set_id
visual_plan_id
source_research_run_id
account
required_count
optional_count
generated_count
pending_count
failed_count
rejected_count
image_assets_status
source_of_truth
assets
artifact_path
next_skill
```

单张图片资产字段：

```text
image_asset_id
image_asset_type
cover_asset_role
source_cover_composition_id
target_platforms
image_production_path
image_generation_decision
prompt_delivery_mode
visual_text_plan_id
visual_text_unit_ids
image_task_id
beat_id
source_prompt_id
generation_run_id
generation_attempt_ids
provider
model
provider_mode
input_schema_version
asset_path
metadata_sidecar_path
asset_mime
width
height
aspect_ratio
asset_version
parent_asset_id
supersedes_asset_id
insert_after_text
insert_before_text
image_status
asset_status_detail
prompt_used
prompt_used_full_path
failure_reason
human_action_required
risk_check_notes
quality_gate_status
static_visual_quality_gate_status
visual_text_quality_gate_status
cover_quality_gate_status
asset_trace_quality_gate_status
```

`image_status` 允许值：

```text
generated
pending_external
generation_failed
manual_required
rejected
```

`cover_asset_role` 允许值：

```text
not_applicable
cover_background_asset
cover_composited_asset
platform_cover_asset
```

关系：

```text
picture_in_picture_image 默认 cover_asset_role=not_applicable。
cover_background_asset 不是可上传成品。
cover_composited_asset 是默认主成品。
platform_cover_asset 是按平台独立裁切、改标题或重新合成的成品变体。
```

`image_production_path` 允许值：

```text
codex_image2_render
seedream_prompt_delivery
manual_upload
not_available
```

字段关系：

```text
codex_image2_render：Codex 环境直接调用内置 image 生成图片，写入 assets/images 并嵌入 HTML。
seedream_prompt_delivery：非 Codex 环境不调用外部 API，只交付 Seedream 入参、完整 prompt 和外部生成说明。
manual_upload：用户或人工设计师上传图片。
not_available：当前无图片生产路径，必须说明阻断原因。
```

`image_assets_status` 允许值：

```text
all_generated
partially_generated
pending_external
generation_failed
manual_required
mixed
not_required
```

### 14.1 `image_generation_record`

`image_generation_record` 记录一次图片生成、外部待生成、失败或人工上传尝试。它不是 prompt 卡；prompt 卡说明“想生成什么”，generation record 说明“实际尝试了什么”。

```text
generation_run_id
generation_attempt_id
image_task_id
image_asset_type
image_production_path
image_generation_decision
prompt_delivery_mode
source_prompt_id
provider
model
provider_mode
input_schema_version
input_payload_path
external_model_payload_path
prompt_used
negative_prompt
aspect_ratio
width
height
seed
reference_image_paths
started_at
finished_at
generation_status
provider_response_id
provider_outcome_status
provider_result_observed_at
output_asset_path
postprocess_status
reconciliation_status
interruption_recovery_policy
failure_reason
retry_suggestion
execution_trace_ref
artifact_path
next_skill
```

`provider_mode` 允许值：

```text
codex_builtin
external_api
manual_upload
not_available
```

`provider` 允许值示例（`image_generation_record` / `image_metadata_sidecar` 中使用）：

```text
gpt-image-2          Codex 环境内置出图
seedream-4.0         豆包 Seedream 4.0（快速生成）
seedream-4.5         豆包 Seedream 4.5（文字渲染/品牌级质量）
seedream-5.0         豆包 Seedream 5.0（复杂语义/逻辑图表）
manual               人工设计/上传
```

`provider_mode` 与 `provider` 配合使用：`provider_mode=external_api` 时，`provider` 写具体模型名。

`generation_status` 允许值：

```text
generated
pending_external
generation_failed
manual_required
```

`provider_outcome_status` 区分 `not_invoked / succeeded / failed / outcome_unknown`；`postprocess_status` 区分 `not_started / running / completed / failed / interrupted`；`reconciliation_status` 区分 `not_required / pending / reconciled_existing_output / retry_authorized`；现行 `interruption_recovery_policy` 固定为 `reconcile_existing_output_before_retry`。provider 返回后先持久化 attempt、outcome 和输出引用，再进入复制、叠字、裁切或封面派生。若后处理被中断，必须先 reconcile 已存在输出；`outcome_unknown` 或已找到成功输出时不得盲目重调 provider。

### 14.2 `image_metadata_sidecar`

`image_metadata_sidecar` 是 generated 图片的外置追溯元数据。图片文件自身可能丢失 metadata，因此 generated 图片必须有 sidecar；pending / failed / manual 图片必须有 `image_generation_record` 或人工任务说明。

```text
image_asset_id
asset_path
source_prompt_id
generation_run_id
provider
model
input_schema_version
prompt_used_full_path
generation_record_path
visual_plan_path
visual_text_plan_id
visual_text_unit_ids
quality_gate_path
created_at
checksum
license_note
privacy_note
artifact_path
next_skill
```

### 15. `final_delivery`

```text
final_delivery_id
delivery_id
package_id
image_asset_set_id
visual_text_plan_id
visual_text_quality_gate_status
visual_text_delivery_summary
cover_design_package_id
cover_composition_ids
cover_variant_set_id
source_research_run_id
account
delivery_page_mode
entrypoint_path
source_links
copy_blocks
download_assets
html_builder_mode
html_template_source
html_link_check_result
html_asset_check_result
html_embed_manifest
cover_embeds
cover_design_manifest
upload_ready_cover_count
prompt_only_cover_count
final_delivery_status
export_status
artifact_path
next_skill
```

`visual_text_delivery_summary` 最小字段：

```text
visual_text_task_count
required_text_task_count
optional_text_task_count
forbidden_text_task_count
source_bound_task_count
visual_text_failed_task_count
detail_artifact_path
```

`html_builder_mode` 允许值：

```text
skill_template_rendered
agent_handcrafted_html
external_manual
```

说明：

```text
skill_template_rendered 才能计入 skill 独立成熟度证据。
agent_handcrafted_html 只能说明本轮交付页可用，checker 必须给 warning。
```

### 16. `export_bundle`

```text
export_bundle_id
final_delivery_id
session_id
account
delivery_page_mode
entrypoint_path
export_manifest_path
resource_count
required_resource_count
missing_resource_count
checksum_manifest_path
export_status
export_package_type
export_package_check_result
artifact_path
next_skill
```

`export_package_type` 允许值：

```text
portable_bundle
standalone_html
offline_tester_package
public_release_candidate
```

`export_package_check_result` 允许值：

```text
pass
fail
not_run
not_applicable
```

### 17. `workflow_session_record`

`workflow_session_record` 是轻量版 save / restore / report。它不替代内容产物，只记录本轮 workflow 跑到哪里、有哪些产物、下一次如何接上。

`current_artifact` 必须优先指向账号/session 目录下的具体交接物文件，例如：

```text
accounts/示例垂类经营号/runs/SAMPLE-HISTORICAL-001/intermediate/03-content-brief.md#B20260706-002
```

```text
session_id
account
product_profile_id
campaign_profile_id
started_at
updated_at
current_stage
current_artifact
artifact_ids
last_decision
blocked_reason
next_recommended_actions
human_prompt
human_reply_examples
session_status
account_profile_confirmed_for_session
account_profile_confirmed_at
confirmation_status
```

`confirmation_status` 允许值：

```text
confirmed_by_user
simulated_by_agent_for_test
not_required
pending_human_confirmation
```

`session_status` 允许值：

```text
session_active
session_waiting_human
session_ready_to_restore
session_completed
session_archived
```

`current_stage` 必须使用标准交接物名或 `done`，不能写“做到一半 / 差不多 / 继续”。

### 18. `execution_trace`

`execution_trace` 记录本轮生产链路中，每一步到底来自 skill、agent、用户还是环境能力。它不替代内容产物，只用于判断 skill 成熟度和发布风险。

```text
execution_trace_id
session_id
account
trace_status
agent_assist_level
actions
skill_maturity_observations
agent_intervention_log
release_risks
artifact_path
next_skill
```

`trace_status` 允许值：

```text
active
waiting_human
completed
blocked
```

`agent_assist_level` 允许值：

```text
low
medium
high
```

`execution_source` 允许值：

```text
skill_defined
skill_inferred
agent_orchestrated
agent_created_rule
user_decision
environment_capability
manual_fallback
```

### 18.1 R7-L3 自治证据对象

R7-C133-C160 使用三层证据，不再从单次 `workflow_autonomous_completion_count` 直接推导项目成熟度。

```text
maturity_baseline
run_capability_snapshot
autonomy_run_observation
intervention_ledger
session_autonomy_evidence
autonomy_certification_cohort
route_autonomy_evidence
project_maturity_evidence
```

共同身份和时间字段：

```text
schema_id
schema_version
maturity_baseline_digest
session_id
entry_route: direct_delivery / hotspot_to_delivery
run_started_at: ISO 8601 with timezone
```

`maturity_baseline` / `run_capability_snapshot` 必须同时带：

```text
contract_surfaces:
  workflow_blueprint_bundle
  maturity_schema_bundle
  skill_contract_bundle
  runtime_entry_bundle
capabilities
```

四个合同面和每个 registered capability 的当前摘要共同派生 `maturity_baseline_digest`；任一合同面或能力身份改变都必须开启新基线。

`autonomy_run_observation` 是派生输入，至少包含：

```text
input_identity
expected_step_ids
step_executions
artifact_commits
external_tasks
human_gates
file_writes
final_delivery
manual_intervention_declarations
current_contract_blockers
```

不得提供或信任自报 `manual_patch_detected=false`。`intervention_ledger` 由 runtime 从上述事实派生，`intervention_code` 当前至少包含：

```text
unregistered_execution
agent_or_manual_execution
artifact_unregistered_producer
artifact_commit_provenance_missing
external_task_unregistered
external_evidence_parity_missing
unregistered_or_untyped_human_action
file_write_unregistered_producer
machine_object_producer_bypass
run_specific_helper_output
declared_manual_intervention
```

`session_autonomy_evidence.outcome` 允许值：

```text
autonomous_delivery
autonomous_waiting
assisted_delivery
failed
```

`route_autonomy_evidence.route_status` 允许值：

```text
not_proven
candidate
l3
```

`project_maturity_evidence.project_status` 允许值：

```text
l2_8
l3_candidate
l3
```

项目能力覆盖固定为能力类别而非每篇配额：

```text
generated_context
source_bound_evidence
explicit_existing_asset
deterministic_postprocess
revision_resume
honest_waiting
```

认证 cohort 必须在运行前物化并只追加。`autonomous_waiting` 不增加也不清零连续交付计数；`assisted_delivery` 和 `failed` 清零；同一输入指纹不能冒充第二条不同样本。

### 19. `workflow_check_report`

`workflow_check_report` 是只读 checker 的标准输出。它只记录检查结论、证据、阻断项、警告项和建议反写位置，不自动修改业务产物。

```text
check_id
checked_at
check_scope
target_path
checker_version
readonly
overall_result
maturity_observed
blocking_count
warning_count
info_count
checks
next_action
human_prompt
artifact_path
next_skill
```

`check_scope` 允许值：

```text
session
sample
project
```

`overall_result` 允许值：

```text
pass
pass_with_warnings
fail
blocked
```

`severity` 允许值：

```text
blocker
warn
info
```

### 19.1 `release_check_report`

`release_check_report` 是 P3 对公开候选包、zip、hash、开源健康文件、隐私和链接的只读检查报告。它不自动 push、不自动 tag、不创建 GitHub release。

```text
check_report_id
check_scope
check_run_id
command_name
command_version
exit_code
severity_policy
checked_at
checked_by
input_path
overall_result
blocker_count
warning_count
blockers
warnings
info_items
evidence_paths
machine_readable_report_path
human_readable_report_path
artifact_manifest_path
remediation_items
reproducibility_status
privacy_scan_result
link_check_result
field_gate_result
contract_sync_result
image_asset_check_result
release_state_result
zip_path
sha256_path
artifact_path
next_action
```

`check_scope` 允许值：

```text
public_release
zip
release_candidate
```

`overall_result` 允许值：

```text
pass
pass_with_warnings
fail
blocked
```

`exit_code` 允许值：

```text
0：pass
1：fail，有 blocker
2：blocked，输入缺失或检查无法完成
3：tool_error，checker 自身错误
4：usage_error，命令参数错误
```

`severity_policy` 允许值：

```text
blocker_fails
warning_allows
info_only
```

`reproducibility_status` 允许值：

```text
reproducible
not_reproducible
not_run
unknown
```

### 19.2 `sample_check_report`

`sample_check_report` 是 P3/P4 对 examples 样例的只读检查报告。它检查样例是否能让人和 AI 看懂输入、预期行为、产物位置、失败恢复和字段状态。

```text
check_report_id
check_run_id
sample_id
sample_goal
sample_status
command_name
command_version
exit_code
severity_policy
happy_path_result
failure_case_result
expected_recovery_result
privacy_status
link_status
field_gate_status
image_asset_status
human_guidance_status
evidence_paths
machine_readable_report_path
human_readable_report_path
artifact_manifest_path
remediation_items
reproducibility_status
artifact_path
next_action
```

`sample_status` 允许值：

```text
draft
ready_for_review
accepted
needs_fix
```

`exit_code`、`severity_policy` 和 `reproducibility_status` 允许值同 `release_check_report`。

### 19.3 `sample_record`

`sample_record` 是 P4 样例的标准说明记录。每个 sample 必须回答用户输入、AI 应该做什么、会生成哪些产物、失败后如何恢复。

```text
sample_id
sample_goal
sample_status
sample_persona
sample_type
sample_level
estimated_time
prerequisites
run_mode
golden_path_prompt
failure_prompt
expected_output_summary
success_criteria
known_limitations
validator_command
input_prompt_path
expected_behavior_path
expected_artifacts_path
manifest_path
execution_trace_path
final_delivery_path
check_report_path
machine_readable_report_path
sample_privacy_status
sample_link_status
sample_field_gate_status
artifact_path
next_skill
```

`sample_type` 允许值：

```text
tutorial
how_to
regression
failure_recovery
reference_sample
```

`sample_level` 允许值：

```text
beginner
intermediate
maintainer
```

`run_mode` 允许值：

```text
read_only
agent_simulated
human_interactive
validator_only
```

### 19.3.1 `workflow_replay_report`

`workflow_replay_report` 是 D 批 runner-lite 的只读输出。它不执行 AI 写作、不联网、不生成图片，只检查样例 / dry-run 的 manifest、expected artifacts、execution trace 和实际文件是否闭合。

```text
replay_report_id
runner_id
runner_version
replay_mode
sample_id
sample_path
manifest_path
trace_path
expected_artifacts_path
overall_result
blocker_count
warning_count
step_count
passed_step_count
warning_step_count
failed_step_count
agent_assist_level_observed
replay_evidence_status
missing_artifacts
unexpected_artifacts
step_results
maturity_impact
next_action
artifact_path
blocker_reasons
warning_reasons
```

`replay_mode` 允许值：

```text
trace_replay_readonly
artifact_replay
guided_replay
```

`overall_result` 允许值：

```text
pass
pass_with_warnings
fail
blocked
```

`agent_assist_level_observed` 允许值：

```text
none
low
medium
high
unknown
```

`replay_evidence_status` 允许值：

```text
sufficient
partial
insufficient
blocked
```

### 19.3.2 `regression_suite_report`

`regression_suite_report` 是 E 批 sample regression fixture 的汇总输出。它不重新生产内容，只顺序运行样例结构检查和只读 trace replay，并判断这些样例是否能作为回归证据。

```text
regression_suite_report_id
suite_id
suite_version
suite_manifest_path
runner_id
runner_version
fixture_count
passed_fixture_count
warning_fixture_count
failed_fixture_count
overall_result
blocker_count
warning_count
allowed_warning_policy
fixture_results
maturity_impact
next_action
artifact_path
machine_readable_report_path
```

`overall_result` 允许值：

```text
pass
pass_with_warnings
fail
blocked
```

`allowed_warning_policy` 允许值：

```text
explicit_allowlist
none
```

`maturity_impact` 允许值：

```text
regression_fixture_ready
regression_fixture_ready_with_warnings
not_regression_evidence
blocked
```

### 19.4 `release_record`

`release_record` 是 P5 GitHub release 分层状态记录。它防止把 public_release candidate 误说成 GitHub release。

```text
release_id
release_state
version
tag_name
release_channel
release_candidate_path
zip_path
sha256_path
release_notes_path
public_manifest_path
release_checklist_path
remote_url
commit_hash
publish_status
human_approval_required
artifact_path
next_skill
```

`release_state` 允许值：

```text
release_candidate_built
release_commit_ready
tag_ready
remote_ready
github_release_published
```

`publish_status` 允许值：

```text
not_published
publish_ready_waiting_human
published_to_github
publish_blocked
```

`release_channel` 允许值：

```text
alpha
beta
stable
local_candidate
```

P5 版本一致性要求：

```text
VERSION
CHANGELOG.md
RELEASE_NOTES.md
public-manifest.yaml
release-record.json
```

上述文件中的 `version` 必须一致。`release_state != github_release_published` 时，`publish_status` 不得写成 `published_to_github`。

### 19.4.1 `release_gate_report`

`release_gate_report` 是公开候选包通过后、进入 release commit / tag / remote / push / GitHub Release 前的人类门禁报告。它只判断是否可以进入下一步，不自动执行任何 Git 或 GitHub 发布动作。

```text
release_gate_report_id
gate_version
release_candidate_path
zip_path
sha256_path
target_tag
overall_result
blocker_count
waiting_human_count
next_action
checks
artifact_path
machine_readable_report_path
```

`overall_result` 允许值：

```text
ready
ready_waiting_human
blocked
```

`checks.status` 允许值：

```text
pass
blocked
waiting_human
```

`next_action` 允许值：

```text
fix_release_gate_blockers
ask_human_release_approval
eligible_for_release_actions_after_human_confirmation
```

### 19.4.2 P0-H1 运行合同对象

P0-H1 只编译单账号、单 session、单篇内容的运行合同。Schema 真源位于 `templates/schema/p0/`；`examples/p0-runtime-fixture/` 仍是 v0.1 legacy runtime，实际 renderer 切换到 v0.2 属于 P0-H2。

#### `session_execution_plan`

```text
plan_id
session_id
workflow_definition_version
contract_bundle_version
plan_schema_id
event_schema_id
artifact_lineage_schema_id
render_input_schema_id
renderer_version
template_version
runtime_mode
topic_count
final_delivery_count
steps
```

版本固定值：

```text
workflow_definition_version = p0-single-runtime-v0.2
contract_bundle_version = p0-contract-bundle-v0.2
runtime_mode = single
topic_count = 1
final_delivery_count = 1
```

`steps[].retry_policy`：

```text
mode：never / bounded / reconcile_first / human_decision_required
automatic_retries：0 / 1
max_attempts：1 / 2
idempotency_scope：session_step_input_digest
```

只有 `deterministic_tool` 可以配置最多 1 次自动重试。`agent_required`、`human_gate` 和 `external_side_effect` 不得自动重试；外部结果不确定时必须 `reconcile_first`。

#### `execution_event_log`

每行一个 JSON event，固定 envelope：

```text
event_id
event_type
event_schema_id
event_source
session_id
subject_id
step_id
sequence_no
previous_event_id
occurred_at
recorded_at
causation_event_id
correlation_id
idempotency_key
execution_attempt_id
attempt_no
privacy_class
state_before
state_after
payload_digest
input_digest
output_artifact_ids
result_code
safe_summary
failure
```

`privacy_class`：

```text
local_private
support_safe
public_sample
```

event 只保存 ID、状态、digest、相对引用和安全摘要，不保存完整口播、完整 prompt、用户原话、provider 原始响应或绝对本机路径。`sequence_no` 是顺序真源；同一 `event_type + idempotency_key` 的 payload digest 不一致时为 `idempotency_conflict`。

失败细节：

```text
failure_category：contract / input / transient_environment / tool / external_provider / authorization / quality / unknown
retryability：retryable / non_retryable / reconcile_first / human_decision_required
attempt_no
max_attempts
next_retry_not_before
recovery_action
```

`outcome_unknown` 必须使用 `retryability=reconcile_first`，不能直接重试外部图片请求。

#### `artifact_lineage_manifest` 与 `artifact_check_set`

血缘字段：

```text
artifact_id
artifact_type
producer_event_id
input_artifact_ids
materialization_status
quality_status
delivery_eligibility
path
sha256
check_ids
source_artifact_id
source_session_id
source_content_digest
source_beat_digest
```

三轴状态：

```text
materialization_status：absent / pending / materialized / integrity_failed
quality_status：not_run / pass / pass_with_warnings / fail / human_review_required
delivery_eligibility：blocked / trace_only / preview_only / ready_for_delivery / ready_for_upload
```

`ready_for_upload` 至少要求 `materialized + quality pass + check_ids 非空`。文件存在不等于质检通过。

资产检查字段：

```text
check_id
check_version
target_artifact_id
status：pass / pass_with_warnings / fail / not_run
severity：blocker / warning / info
evidence_path
executed_at
execution_source：deterministic_tool / agent_review / human_review
```

`not_run` 时不得伪造 `evidence_path` 或 `executed_at`。

#### `deterministic_final_delivery_render_input`

v0.2 固定使用 `schema_version=typed_components_v0.2`，顶层字段：

```text
schema_id
schema_version
render_input_id
final_delivery_id
account_name
session_id
research_run_id
template_version
generated_at
topic
script_card
production_status
cover_cards
cover_empty_reason
pip_cards
pip_empty_reason
platform_cards
trace_cards
action_cards
source_artifact_ids
```

业务卡类型固定为：

```text
script
cover
picture_in_picture
platform
trace
action
```

禁止输入任何 `*_html`、`style`、`class` 或事件处理器字段。`cover_cards` / `pip_cards` 为空时必须说明原因；目标平台卡不得为空；追溯卡至少覆盖 topic、brief、draft、quality 和 delivery。

#### `p0_contract_compatibility_matrix`

```text
from_version
to_version
replay_readable
resume_executable
renderable
migration_required
reason
```

`p0-runtime-v0.1 -> p0-single-runtime-v0.2` 只允许只读 replay，不允许原地 resume 或用 v0.2 renderer 直接渲染；`p0-single-runtime-v0.2 -> 同版` 允许 replay / resume / render。历史 event 不原地改写。

### 19.4.3 P0-H2 输入编译与渲染回执

上游 agent 生成 `final-delivery-render-candidate.json`，字段形状与 `typed_components_v0.2` 一致，但其中 `production_status.delivery_readiness` 不是最终真源。确定性 `compile_render_input` 必须根据口播、目标平台、质检状态和卡片 / 资产状态重新派生：

```text
ready
ready_with_warnings
needs_action
blocked
```

编译成功后写 `deliverables/p0/final-delivery-render-input.json`。renderer 只能读取该官方输入，不能读取 candidate 或临场解析上游 Markdown；输入中禁止 `*_html`，HTML 标签、样式、上下文转义、相对链接和卡片顺序均由 renderer 独占。

#### `render_receipt`

```text
schema_id = taoge://schemas/p0/render-receipt/v0.2
schema_version = 0.2
receipt_id
render_input_sha256
renderer_version = final-delivery-renderer-v0.2
template_sha256
included_card_ids
included_asset_ids
warning_codes
output_html_sha256
```

相同 render input、renderer 和 template 必须生成相同 HTML byte digest 与相同 receipt。重复执行发现已有成功事件、HTML 和 receipt digest 闭合时返回 `skipped_reused`，不得追加新的成功事件。页面必须只有一个 `h1` 和一个 `main`，正文交付先于追溯材料，运行证据默认折叠，无脚本仍可完整阅读和复制。

### 19.4.3A P0-H7 历史交付修订与发布执行工作台

本节记录 v0.3 历史合同；v0.4 进一步加入平台表面视觉合同。两者均只读 replay / reproduction，不得把旧输入原地补字段后冒充当前 v0.5。v0.3 在 v0.2 卡片基础上新增：

```text
delivery_revision
run_provenance
duration_estimate
warning_items
platform_delivery_units
```

`delivery_revision`：

```text
delivery_revision_id
revision_no
revision_status：preparing / compiled / current / superseded / blocked
supersedes_delivery_revision_id（可选）
source_artifact_bindings[]：artifact_type + artifact_id + sha256
generated_view_paths：final_html / final_script / final_visual_plan / final_platform_package / content_delivery_record / revision_manifest
semantic_gate_status：pending / pass / blocked
```

`platform_delivery_unit` 必须把 `platform_card_id`、`cover_card_id`、`cover_title`、`rendered_cover_text`、封面 asset/path/hash、video title、description、hashtags 和 publish readiness 绑定为同一平台发布单元。封面标题只允许忽略空白与 `/ | · — - ： :` 等分隔符后比较；语义文字不同直接 blocked。

`picture_in_picture` v0.3 必填 `trigger_text + insert_after_text + insert_before_text + prompt_path + generation_record_path + sidecar_path`。位置文字是正文边界，不得按文件路径校验；三个追溯路径必须实际 materialized。

`warning_item` 唯一键是 `warning_code + source_artifact_id`，必填：

```text
warning_category：research / copy / visual / cover / publishing / runtime_scope
severity：blocking / non_blocking / known_scope
user_message
impact
recommended_action
resolution_status：open / resolved / accepted
```

`production_status.warning_codes` 由未 resolved 的 warning items 去重派生；下游不得清空上游未关闭 warning。`duration_estimate_status` 只允许 `measured / derived_range / not_available`；derived range 必须绑定已登记的 speech-rate profile 和推导方法，缺实测或校准来源时必须 `not_available`。

v0.3 `render_receipt` 使用 `taoge://schemas/p0/render-receipt/v0.3`，增加 `delivery_revision_id` 和 `output_view_sha256`。HTML 与四个 Markdown 视图先写并校验，receipt 随后写，`deliverables/p0/delivery-revision.json` 最后写入，作为当前 revision 的 commit marker；这不宣称文件系统支持多文件原子事务。幂等 operation digest 必须同时覆盖 render input、renderer 和 template，避免模板更新时错误复用旧页面。

### 19.4.3B R6 / R3 当前 v0.5 最终交付

现行新运行固定：

```text
plan_schema_id = taoge://schemas/p0/session-execution-plan/v0.5
contract_bundle_version = p0-contract-bundle-v0.5
render_input_schema_id = taoge://schemas/final-delivery/typed-components/v0.5
renderer_version = final-delivery-renderer-v0.5
template_version = final-delivery-template-v0.5
```

v0.5 在 v0.4 平台封面 / visual insert 基础上新增：

```text
content_structure_card：选定策略、受众前后状态、核心承诺、1..N stages 及实际实现
content_beat_cards[]：按正文顺序展示 beat、stage、原句、主处置、理由、task / occurrence
script_review_card：script design review、revision decision、alignment、readiness 和问题项
visual_coverage_summary：coverage / delivery readiness、task summaries 和九类数量
production_status.script_readiness / visual_coverage_status / alignment_status
```

compiler 必须从 current refs 重新派生 readiness 与数量，不能相信上游 totals。最终 HTML 固定展示“这篇怎么讲 / 哪里建议改 / 哪里用图，为什么”；未解决 beat、非 ready 脚本或非 pass 对齐不得显示可发布。所有同步视图先写并校验，`render-receipt@0.5` 随后写，`delivery-revision@0.5` 最后写入。模板 digest 和 renderer 版本进入 operation digest；桌面和移动视口均须检查无横向溢出。

### 19.4.3C current pointer 物理提交标记

R6 / R3 现行机器对象的 immutable revision 写在：

```text
intermediate/contracts/revisions/{object_type}/{artifact_id}.json
```

对应 `.current.json` 使用 `content-analysis-current-pointer@0.1.0`，至少保存 object type、artifact ID、revision path、revision SHA-256、status、source draft digest 和更新时间。producer 先写 revision、校验和人类投影，最后原子替换 pointer；pointer 缺失、hash 不符或 source draft digest 变化一律 stale / blocked，checker 不得代替 producer 修 pointer。

### 19.4.4 P0-H3 独立失败 / 恢复 Fixture 结果

P0-F03 至 P0-F19 每个 fixture 必须在自己的目录内提供：

```text
fixture.json
session-execution-plan.json
execution-events.jsonl
fixture-state.json
expected-result.json
最小场景产物（仅在该场景需要时）
```

不得从另一个 fixture 目录借 plan、events 或产物补证据。checker 的统一 `fixture_result` 字段：

```text
fixture_id
expected_state
actual_state
failure_category
resume_advice
fixture_result：pass / fail
assertion_results
errors
```

`failure_category=not_failure` 用于 `waiting_agent`、`waiting_human`、`not_invoked`、合法幂等复用和等待外部取消对账，防止把等待或未授权动作误报为 workflow 失败。H3 结果只证明脱敏 fixture 的恢复语义，不证明 H4 evidence commands、真实图片、真实账号、外部 API 或发布已执行。

### 19.4.5 P0-H4 Evidence Runtime

#### `p0_evidence_command`

所有带输入的 evidence / reconciliation 命令固定使用：

```text
schema_id = taoge://commands/p0/evidence-command/v0.2
schema_version = 0.2
command
session_id
step_id
idempotency_key
expected_last_sequence_no
safe_summary
```

五个业务命令：

```text
create_session_plan
record_agent_result
record_human_choice
record_external_result
build_resume_summary
```

维护操作：

```text
rebuild_projection
reconcile_orphan_artifact
```

`record_agent_result` 额外登记 artifact ID / type / relative path / input artifact IDs / quality / delivery eligibility / check IDs。`record_human_choice` 只接受明确的 decision code，不保存用户整段原话。`record_external_result` 只接受 `not_invoked / succeeded / failed / outcome_unknown`，只登记已发生事实，工具本身不联网。`outcome_unknown` 必须 `reconcile_first`。

统一 writer 结果：

```text
appended
duplicate_reused
idempotency_conflict
concurrent_append_conflict
event_sequence_conflict
event_contract_failed
```

同 `event_type + idempotency_key` 且 payload digest 相同返回 `duplicate_reused`；digest 不同返回 `idempotency_conflict`；`expected_last_sequence_no` 与事实尾号不一致返回 `concurrent_append_conflict`。

#### `state_projection`

```text
schema_id = taoge://schemas/p0/state-projection/v0.2
projection_version = p0-state-projection-v0.2
session_id
plan_id
projected_through_sequence_no
source_event_digest
source_last_recorded_at
current_state
completed_step_ids
waiting_step_id
next_step_id
non_repeatable_step_ids
step_states
```

projection 只从 plan + event log 派生。落后可以重建；领先或同尾号 digest 不一致为 `state_projection_conflict`。显式重建前把冲突投影保留到当前 session 的 `intermediate/p0/quarantine/`，不得改写事实 event。

#### `resume_summary`

```text
schema_id = taoge://schemas/p0/resume-summary/v0.2
summary_id
session_id
plan_id
projection_version
projected_through_sequence_no
source_event_digest
current_state
completed_step_ids
waiting_for
next_step_id
non_repeatable_step_ids
recovery_action
human_message
```

resume summary 必须用同一 event log 说明已完成、正在等待、下一步和不可盲目重跑的外部步骤，不得另建事实源。

### 19.4.6 R7-H1 至 H4 语义工作流与确定性交付对象

R7-H1 编译蓝图、注册表、task / submission 合同和历史兼容边界；R7-H2 编译 selector、状态路由、提交路径、任务指导、submission v0.2 与 pointer-last runtime；H3 编译直供 producer adapters；H4 编译 candidate v0.6 与 renderer v0.6。P0 plan / event / projection 仍是唯一运行事实源；合法 task 只表示待执行，合法 submission 只在确定性 submitter 完成 revision、lineage、pointer、event 与 projection 后才成为 current。

#### `workflow_blueprint`

```text
schema_id
schema_version
registry_id
status
blueprints[]
  blueprint_id
  blueprint_version
  activation_status
  node_registry_ref
  entry_node_id
  terminal_node_id
  node_refs[]
  max_active_next_nodes
  single_next_node_required
  runtime_state_source
  implementation_batch
```

当前登记：

```text
direct_delivery_single_v0.1
direct_delivery_single_v0.2
hotspot_to_delivery_single_v0.1
```

本段记录 H5A 当时的历史合同：`direct_delivery_single_v0.1` 只保留历史缺陷证据，H5A 新建直供 session 当时默认 v0.2。当前默认版本以后文“R7-L3-H3 current 直供视觉语义路由字段”为准。`max_active_next_nodes=1` 且 `runtime_state_source=p0_plan_event_projection`。未声明并行时出现两个下一步为 `blueprint_contract_error`，不能让 Codex 自选。

#### `workflow_node_registry`

```text
node_id
skill_ref
step_kind
input_selectors[]
required_contract_versions[]
output_artifact_type
output_schema_ref
allowed_result_statuses[]
action_registry_ref
success_route
warning_route
failure_route
stale_scope
retry_policy
implementation_batch
implementation_status
```

`step_kind` 允许值：

```text
deterministic_tool
semantic_skill
human_gate
external_side_effect
```

#### `contract_status_registry`

```text
contract_id
active_version
lifecycle_status
superseded_by
compiled_layers[]
implementation_batch
```

`lifecycle_status` 允许值：

```text
active_compiled
confirmed_pending_compile
superseded_pending_recompile
historical_compatibility
historical_contract_defect
```

R7-H5 完成后，`p0-agent-produced-delivery-candidate-v0.5` 保持 superseded；v0.5 renderer 只服务历史原合同回放。v0.6 candidate compiler、renderer、viewport acceptance 与 final human gate 均为 `active_compiled`；热点 adapter 仍为 `confirmed_pending_compile`。

#### `action_registry`

```text
action_code
label
allowed_target_types[]
requires_target_artifact
lifecycle_status
introduced_contract
```

R7-H1 合法 `action_code`：

```text
publish_primary_manually
publish_all_manually
review_secondary_covers
revise_copy
revise_visual
archive_session
export_handoff
adopt_delivery
request_revision
request_export
```

`r7-action-registry-v0.3` 在保留 v0.2 历史动作的基础上新增这三个 R8
action，且仅作用于 `final_delivery_human_decision`。v0.2 独立文件只供
历史 replay。`regenerate_visual`
未登记。遇到未登记自然语言返回 `enum_registry_error` 和合法 code，不做近似猜测。

### 19.4.7 R7-H6A 热点入口对象

热点 v0.2 的 current 前链固定为：

```text
hotspot_research_request -> hotspot_research_set -> topic_selection_panel -> topic_selection_decision -> selected_topic_source -> content_brief v0.4 -> short_video_structure_plan v0.1 -> draft v0.4
```

每个 node 只提交一个 artifact。`artifact_ref` 固定含 `artifact_id / revision / sha256`；`component_ref` 固定含 `container_artifact_ref / component_type / component_id / component_sha256`。业务摘要一律使用 `sha256:<64hex>`，不能把路径、裸摘要或未来对象当引用。

`hotspot_research_request.request_mode`：

```text
initial
same_policy_rerun
broaden_within_account_policy
manual_source_refresh
revalidation_after_reversal
```

`hotspot_research_set.research_set_status`：`ready_for_panel / ready_no_recommendation`。外部读取未完成时不提交半成品 current set。

`topic_selection_panel.panel_status`：`panel_ready_waiting_human / panel_no_recommendation`。panel 的排序、推荐和 component digest 必须与 research set 的 `panel_model` 完全一致。

`topic_selection_decision` 的 decision/action 固定映射：

```text
select_one -> select_topic
rerun_same_policy -> rerun_hotspot_research
broaden_within_account_policy -> broaden_hotspot_scope
add_manual_source -> attach_manual_hotspot_source
select_multiple -> branch_selected_topics
stop -> archive_session
```

无推荐面板禁止 select；多选只交既有 R2 `detect_branch_request`；人工来源必须先形成 current input set，不能把 URL 藏在 decision。

`selected_topic_source.selected_source_status`：`ready_for_brief / ready_for_delivery / revalidation_required`。H6A 初次选择只生成新 ID、revision 1、`ready_for_brief`；只有后续 freshness apply 可递增同一 ID。

`freshness_policy.policy_mode`：`duration_bound / event_triggered / always_revalidate_before_delivery`。没有版本化小时或 trigger 依据时必须使用最后一种及 `fallback_reason=no_trusted_duration_or_trigger_policy`，不得填 fixture 常量。

热点 Brief v0.4 固定 `content_origin=hotspot_selected_topic`、`content_source_id=selected_topic_source_id`、current source ref + semantic digest、`next_skill=short-video-structure-planner`。热点 Draft v0.4 固定 `generate_from_structure` 并绑定 current Brief + structure；原稿字段为 null。用户选题不改变 fact / propagation / risk / claim evidence / allowed expression。

#### `typed_delivery_candidate_v0.6`

```text
schema_id = taoge://schemas/final-delivery/typed-components/v0.6
schema_version = typed_components_v0.6
final_delivery_id
session_id
candidate_status
action_registry_version
presentation_registry_version
compiler_provenance
  producer = deterministic_compiler
  compiler_version
  compiled_at
source_map[]
  artifact_type
  artifact_id
  relative_path
  sha256
  producer_event_id
source_binding_digest
artifact_execution_contribution
  candidate_producer
  manual_patch_detected
  semantic_skill_step_completed_count
  deterministic_tool_step_completed_count
  human_gate_completed_count
  external_side_effect_step_completed_count
  agent_orchestrated_node_count
delivery_payload
```

`delivery_payload` 是严格通过 v0.5 presentation contract 的兼容视图；v0.6 外层独占 producer 身份、source map、注册表摘要和执行贡献。新 R7 session 只能由确定性 compiler 生成该对象，Agent 手工 candidate 不合法。

`content_beat_cards[].visual_task_ids[]` 与 `occurrence_ids[]` 使用不同口径：前者表示该 beat 受哪些 current visual task 覆盖，后者表示物理画面在哪一个 owner beat 开始出现。一个 occurrence 可以连续覆盖多个相邻 beat，但同一 occurrence ID 在所有 beat cards 中只能出现一次；owner 固定为 `visual_insert_occurrences[].covered_beat_ids[]` 按 current beat map 顺序最早的 beat。其余被覆盖 beat 保留 task ID、`occurrence_ids[]` 为空。非相邻再次出现必须新建 occurrence。

candidate compile 前必须验证：occurrence ID 唯一、visual task 存在、covered beats 非空且存在、无重复、连续，并且都属于该 task 的 `covered_beat_ids[]`。失败状态为 `candidate_occurrence_contract_error`，不得通过删字段或手工 candidate 继续。

#### R7 H4 视觉资产编译必填

`image_asset_set.assets[]` 除文件、hash 和来源外还必须提供：

```text
visual_task_id
width_px
height_px
alt_text
visual_text_summary
sidecar_path
generation_record_path
```

`cover_composition.renditions[]` 必须显式提供 `preview_path`。compiler 用 `review_ref` 验证唯一 rendition ID、output hash、preview hash、surface profile 和 pass 状态；carried-forward 总说明不能替代逐 rendition review。

#### `viewport_acceptance_report`

```text
viewport_report_id
session_id
final_delivery_ref
html_path
html_sha256
browser_invocation_status
layout_measurement_status
visual_acceptance_status
overall_result
profiles[]
  profile_id
  viewport_css_px
  document_client_width
  document_scroll_width
  body_scroll_width
  overflow_offenders[]
  failed_image_count
  failed_request_count
  screenshot_path
  screenshot_sha256
artifact_execution_contribution
workflow_autonomous_completion_count
autonomy_eligible
warning_codes[]
next_skill
```

`overall_result=pass` 必须同时有 desktop 1440×1000 与 mobile 390×844 两份真实测量和截图 hash；截图存在不能替代 DOM 测量。普通内容缺浏览器写 `not_tested`，template / renderer / release 缺该证据时阻断。

#### `workflow_session_record v0.1`

本节仅用于历史 v0.5 及更早 blueprint 回放。current v0.6 使用后文
`final_delivery_human_decision v0.1` 与 `workflow_session_record v0.4`。

```text
session_record_id
session_id
final_delivery_ref
viewport_report_ref
decision_status
requested_action
target_artifact_ref
  artifact_id
  artifact_type
decided_by = human
decided_at
next_skill
```

合法组合固定为：`human_confirm -> publish_all_manually -> done`、`revision_requested -> revise_copy / revise_visual -> owning_producer`、`export_requested -> export_handoff -> handoff-exporter`、`archive_requested -> archive_session -> archive-session`。`revise_copy / revise_visual / export_handoff` 必须带 target；修改目标只能来自当前 candidate source map，且 target type 必须匹配 action registry。确认与归档的 target 必须为 null。

#### `final_delivery_human_decision v0.1`

```text
decision_id
decision_revision
session_id
delivery_ref
viewport_acceptance_ref
delivery_visual_review_ref
business_delivery_acceptance_ref
action_code = adopt_delivery | request_revision | request_export | archive_session
user_reply_digest
requested_at
delivery_revision_request_ref
export_mode
decision_status = decision_recorded
```

`request_revision` 必须带 current `delivery_revision_request_ref` 且
`export_mode=null`；`request_export` 必须带 `export_mode` 且 revision ref
为 null；其余两种动作两字段均为 null。该对象只记录决定，不改 HTML、
不发布、不应用会话状态。

#### `workflow_session_record v0.4`

```text
session_record_id
session_record_revision
session_id
decision_ref
applied_action
session_status = delivery_adopted | revision_requested | export_requested | archived
next_skill = done | semantic-workflow-coordinator | handoff-exporter | archive-session
applied_at
```

只有 deterministic `final_delivery_decision_apply` 可以生成该 revision。

#### `semantic_task_envelope`

```text
schema_id
schema_version
task_envelope_id
session_id
plan_id
blueprint_id
blueprint_version
node_id
skill_ref
task_contract_version
action_registry_version
created_at
input_artifact_bindings[]
  artifact_id
  artifact_type
  relative_path
  sha256
  status
  materialization_status
  current_ref_status
input_binding_digest
business_objective
decision_boundaries[]
required_output_schema_ref
allowed_statuses[]
allowed_actions[]
output_commit_policy
idempotency_key
resume_context
```

本段记录 H5A 历史配对：v0.1 envelope 只服务 blueprint v0.1，直供 blueprint v0.2 使用 envelope v0.2 与 P0 plan v0.7；当前直供配对以后文 H3 字段为准。输入必须已经 `materialized + current`，路径必须相对 session，SHA256 必须绑定当前内容。`resume_context.pending_submission_status` 只允许 `none / reconciled`；存在未对账 pending submission 时不得生成新任务。`output_commit_policy` 固定为 `deterministic_submitter_pointer_last`。

#### `semantic_artifact_submission v0.1 / v0.2`

```text
schema_id
schema_version
submission_id
task_envelope_id
session_id
plan_id
node_id
skill_ref
attempt_no
submitted_at
input_binding_digest
output_artifact_type
output_contract_version
output_artifact_id              # v0.2
output_revision                 # v0.2
result_status
requested_action
source_artifact_ids[]
quality_status                  # v0.2
delivery_eligibility            # v0.2
check_ids[]                     # v0.2
payload
evidence_refs[]
idempotency_key
write_intent
requested_machine_writes[]
```

`write_intent=submit_for_deterministic_commit`，`requested_machine_writes` 必须为空。submission 不得包含或直接写 `current_pointer / event / projection / final_delivery_render_candidate / delivery_revision_marker`。它通过校验也不等于 current；revision、lineage、pointer-last、event 和 projection 由 H2 deterministic submitter 独占。

v0.1 submission 是 H1 合同验证历史面；runtime 只接受 submission v0.2。`output_artifact_id / quality_status / delivery_eligibility / check_ids` 由确定性 builder 按 task、adapter 和 registry 派生，不能由 Codex 猜。`output_revision` 必须从 artifact commit registry 声明的 payload `revision_field` 派生；没有 revision 字段的对象才使用 1。payload 内的 ID / revision / status 必须与 submission 顶层一致。

#### H2 机器注册表

```text
input_selector_registry:
  selector_id
  resolver_type
  path_candidates[]
  artifact_type
  artifact_id_fields[]
  status_fields[]
  allow_empty

artifact_commit_registry:
  artifact_type
  artifact_id_field
  revision_field             # 对有版本字段的对象必填
  status_field
  default_revision_path_template
  default_pointer_path_template

status_route_registry:
  node_id
  success[]
  warning[]
  waiting[]
  failure[]

task_guidance_registry:
  node_id
  business_objective
  decision_boundaries[]
```

#### `semantic_current_pointer`

```text
schema_id / schema_version
artifact_type
artifact_id
revision
revision_path
sha256
status
task_envelope_id
submission_id
producer_event_id
committed_at
```

pointer 只能在 immutable revision 与 lineage 都成功后写入。selector 读取 pointer 时必须复核 `revision_path + sha256`，不能只相信 artifact ID。

#### `semantic_commit_receipt`

```text
commit_receipt_id
session_id
task_envelope_id
submission_id
artifact_id / artifact_type
idempotency_key
phase
revision_path / revision_sha256
pointer_path
producer_event_id
projection_status
updated_at
```

`phase` 顺序：`validated -> revision_written -> lineage_written -> pointer_committed -> event_committed -> projection_rebuilt`。未到 `projection_rebuilt` 时 coordinator 必须先 reconcile，不能准备新的语义 task。已完成的同一 submission 重放返回 `duplicate_reused`，不得追加 event 或改写 revision。

#### H3 `producer_adapter_registry` 与状态映射

```text
node_id
payload_schema_path
artifact_type
validation_mode
```

每个 H3 直供 producer node 恰有一个 adapter。Skill 只产出注册 Schema 的 payload；`new-r7-semantic-submission.ps1` 从 task envelope、payload 与业务 `result_status` 派生 submission v0.2，不允许 Codex 手拼 source IDs、quality、idempotency 或机器写入意图。

`artifact_commit_registry.status_value_map` 明确区分：

```text
workflow result status -> payload native status
draft_preserved        -> baseline_ready
keep_current           -> accept_current
structure_ready        -> ready
```

映射缺失不得按词义猜。`waiting_human / waiting_authorization / waiting_external / waiting_visual_review` 属于等待，不是 success 或 blocked；等待 submission 不生成 current revision / pointer，projection 的 next step 保持不变。

#### `r7_contract_compatibility_matrix`

历史 `p0-runtime-v0.1` 与 `p0-contract-bundle-v0.2-v0.5`：

```text
replay_readable = true
resume_into_target = false
render_with_original_contract = true
render_with_target_contract = false
candidate_recompilation_required = true
autonomy_backfill_allowed = false
migration_status = legacy_replay_only
```

以上为历史 H1 replay 口径。current matrix 为 `R7-CONTRACT-COMPATIBILITY-v0.3`：直供 / 热点新 session 只允许 blueprint v0.3 + plan v0.9 + task v0.3 + delivery v0.8；直供 v0.2、热点 v0.2 及更早合同只读 replay，不得用旧绿灯证明当前合同完成。

### 19.4.8 R6 / R3 / R7 联合视觉与返修合同

#### `evidence_anchor_annotation` 与 `semantic_fact_binding`

```text
evidence_anchor_annotation_request.schema_id / schema_version
evidence_anchor_annotation_id
session_id
claim_ref.artifact_id
source_capture_ref
capture_viewport.width / height
visible_quote.exact_text
visible_quote.extraction_method
visible_quote.visual_review_ref
normalized_region.x / y / width / height
emphasis_style
output_artifact_id
output_path
requested_at
source_fact_layer
creator_commentary_layer

evidence_anchor_annotation_record.request_digest
evidence_anchor_annotation_record.attempts[].status / started_at / completed_at
evidence_anchor_annotation_record.outcome.status / action / output_ref
evidence_anchor_annotation_record.original_capture_ref
evidence_anchor_annotation_record.derived_asset_ref

semantic_fact_binding_id
fact_type
required
claim_value
visible_quote_value
overlay_value
summary_value
comparison_result
visual_review_ref
```

`comparison_result` 只允许 `match / mismatch / not_assessed`。整体 `semantic_parity_result` 按 `mismatch > not_assessed > match` 派生；任一 required fact 非 `match` 时不得进入 candidate。OCR 抽取的关键事实没有实际视觉复核记录时必须是 `not_assessed`。原始 capture 不可变，裁切、下划线、框选、标题和说明层必须作为带父 hash 的确定性派生资产。

物理生产链固定为 `typed request -> started attempt -> annotated child -> SHA256/outcome -> news_evidence_pip bundle`。`derived_asset_ref` 和摘要由 producer 计算，不允许上游预填未来 hash；同 request 重跑可返回 `reused_verified`，started 记录已有完整输出时返回 reconciled，identity 改变而复用同一路径必须阻断并要求新 revision。

`news_evidence_pip` 是单 claim 交付对象：一个 annotation / PIP / `claim_ref.artifact_id` 只能对应一条 current claim。上游可保留 visual task group，但 group 内多 claim 必须展开为多个 PIP 子任务及独立 attempt、annotation 和 asset ID。

#### `visual_source_route` 与 `asset_reuse_authorization`

```text
visual_task_id
source_class
classification_basis
excluded_source_class_reasons
production_path
test_profile
task_status
provider_task_ref
evidence_binding_ref
asset_reuse_authorization_ref
```

`source_class` 必须且只能是：

```text
source_bound_evidence
explicit_existing_asset
generated_context
```

`source_bound_evidence` 只走 R6 真实来源截图和确定性后工程；`explicit_existing_asset` 必须有 current `asset_reuse_authorization`，精确绑定 `session_id / visual_task_id / asset_sha256 / account_snapshot_id`，禁止通配未来 session；其余通过门禁的新视觉全部是 `generated_context`，必须先有 Image 2 基础生成结果，确定性叠字、裁切和封面只算派生。`no_provider / reuse_only` 只改变测试执行能力，不改变来源分类，缺基础图时为 `waiting_assets`。

#### `delivery_revision_request` 与 plan v0.9

```text
revision_request_id
request_revision
session_id
current_delivery_ref
current_candidate_ref
candidate_source_map_ref
source_plan_ref
user_instruction
normalized_action
change_items[]
restart_from_node_id
invalidated_artifact_refs[]
carried_forward_artifact_refs[]
request_digest
request_status
```

每个 `change_item` 必须包含 `change_item_id / target_ref / target_sha256 / change_class / original_instruction / normalized_instruction / owning_producer / candidate_source_map_ref`。一次人工退回只有一个 active request 和一个 plan revision；重启点取所有 owning producer 中最早节点，失效范围取依赖闭包并集。`revision_requested` 是非终态，旧交付、旧 HTML 与 viewport 只读保留；新分支必须重建 alignment、candidate、HTML、双 viewport，再进入新的最终人工门禁。重复同口径请求返回 `duplicate_reused`，不同请求在 active request 关闭前返回 `active_delivery_revision_request_exists`。

### 19.5 图片环境能力字段

图片环境能力字段用于 P3/P4/P5 判断 Codex 环境能否直接生成画中画，以及非 Codex 环境如何交付统一提示词。

```text
environment_capability.image_generation
image_asset_type
image_production_path
image_generation_decision
prompt_delivery_mode
provider_mode
image_status
```

`environment_capability.image_generation` 允许值：

```text
available
unavailable
unknown
```

`image_asset_type` 允许值：

```text
picture_in_picture_image
cover_image
```

`image_production_path` 允许值：

```text
codex_image2_render
seedream_prompt_delivery
manual_upload
not_available
```

`image_generation_decision` 允许值：

```text
render_now
deliver_prompt_only
manual_required
```

`prompt_delivery_mode` 允许值：

```text
html_copyable_prompt
prompt_card_md
external_model_payload
not_required
```

`check_status` 允许值：

```text
pass
fail
not_applicable
not_run
```

`readonly` 必须为：

```text
true
```

---

## 五、标准流程状态

```text
profile_ready
-> product_profile_ready
-> research_done
-> topic_pending_human_choice
-> topic_selected_for_brief
-> brief_pass
-> draft_created
-> visual_plan_pass
-> review_pass
-> input_pass
-> package_pass
-> delivery_ready
-> human_confirm
```

任一阶段不通过时，不能伪装成下一阶段产物，只能按 `next_skill` 回退。

---

## 六、用户交互引导语字段

所有需要用户选择的位置，输出中必须包含：

```text
human_prompt
human_reply_examples
recommended_action
auto_next_action
task_after_navigation
```

其中：

```text
human_prompt：给用户看的口语化引导语，不使用字段名代替人话。
human_reply_examples：用户可以直接回复的短句。
recommended_action：本轮默认推荐动作，必须说明为什么推荐。
auto_next_action：用户选择后系统会自动做什么；不能要求用户说“继续”。
task_after_navigation：参考 dbskill 的任务后导航，根据本轮结论推荐 2-3 个下一步并解释原因。
```

示例：

```text
human_prompt：最终 HTML 验收页已经生成。你可以直接人工发布；如果要改，直接说“只改抖音标题”“回到口播改前 5 秒”“回到画中画改首屏图”或“平台包装重来”，我会回到对应环节修完并重新生成 HTML。
human_reply_examples：认可 / 只改抖音标题 / 回到口播改前 5 秒 / 回到画中画改首屏图 / 归档今天不发 / 导出转交包 / 记录发布结果
recommended_action：先查看最终 HTML；满意则认可或人工发布；不满意则说清局部返工点。
auto_next_action：选题确认且平台包装通过后自动进入 final-delivery-builder，不等待“确认采用”。
task_after_navigation：认可确认 / 人工发布记录 / 局部返工并重建 HTML / 归档今天不发 / 导出转交包。
```

收口阶段不能只说“等待确认”。必须把用户可以怎么回说清楚：

```text
human_prompt：最终 HTML 验收页已经生成。你可以直接复制文案、下载图片、拿平台物料人工发布；如果要改，直接说要改哪里，我会按 revision_path 回到对应上游修订并重新生成 HTML。
human_reply_examples：认可 / 只改小红书标题 / 回到口播改前 5 秒 / 回到画中画改首屏图 / 归档今天不发 / 导出转交包 / 记录发布结果
```

不要只写：

```text
等待确认
涛哥确认
待选择
请确认
下一步怎么做
是否继续
请回复继续写口播
```

自动推进规则以 `docs/reference/人类引导与任务后导航规范.md` 为准。Brief 通过、质检通过、平台包装通过、最终 HTML 生成前的下一环节都不得再让用户回复“继续”或“确认采用”。

---

## 七、字段治理规则

1. 新增 skill 前，先在本文件登记它吃什么交接物、产什么交接物。
2. 修订 skill 输出格式时，必须核对本词典，避免新增同义字段。
3. 如果发现历史文档使用旧字段，优先在词典里登记别名，再逐步修订核心 skill。
4. 传播研究资产文件可以用中文标题，但结构化块必须带技术字段。
5. 每轮“小扫地”要检查是否出现新的模糊字段：`状态`、`pass`、`下一步`、`是否通过`、`等待确认`。

---

## 八、R7-H7 最终素材与双层验收字段

### `image_asset_set` v0.3

```text
finalize_requested_at
assets[].asset_role: base / derived_rendition
assets[].asset_revision
assets[].parent_asset_ref
assets[].postprocess.completed_steps[]
assets[].postprocess_record_path / postprocess_record_sha256
assets[].sidecar_path / sidecar_sha256
assets[].generation_record_path / generation_record_sha256
assets[].visual_review_ref.path / sha256
delivery_bindings[].base_asset_ref
delivery_bindings[].derived_rendition_refs[]
delivery_bindings[].required_postprocess[]
delivery_bindings[].delivery_asset_ref
delivery_bindings[].finalize_status
```

`finalize_requested_at` 由上游请求先物化，finalizer 逐字继承，不用当前机器时间补值。声明 `required_postprocess` 后，基础图不得成为最终交付素材。

### `asset_finalize_record` / `image_asset_delivery_set`

```text
finalize_record_id
visual_task_id
base_asset_ref
delivery_asset_ref
required_postprocess[]
completed_postprocess[]
visual_review_ref
finalized_at
finalize_status

delivery_asset_set_id
image_asset_set_ref
delivery_assets[]
finalize_record_refs[]
delivery_asset_count
```

每个视觉任务只允许一个 `delivery_asset_ref`。HTML、下载链接和剪辑单只消费该引用，不从基础图、最新文件名或临时兼容逻辑回退。

### `platform_package.delivery_title`

`delivery_title` 是最终交付对象的独立人类可读标题，不等于 core promise、why-now、结构策略名、诊断句、封面标题或任一平台视频标题。缺失时 candidate v0.9 必须阻断。

### `viewport_acceptance_report` v0.2

```text
technical_viewport_status: pass / not_tested / fail
overall_result: pass / not_tested / browser_invocation_error / technical_viewport_fail
profiles[].measurement_path / measurement_sha256
profiles[].screenshot_path / screenshot_sha256
```

v0.2 不得包含 `visual_acceptance_status`。它只证明几何、资源、浏览器调用和截图证据，不证明业务层级、文字、图片质量或发布准备度。

### `business_delivery_acceptance`

```text
business_acceptance_id
final_delivery_ref
viewport_report_ref
html_sha256
review_evidence.desktop_screenshot_ref
review_evidence.mobile_screenshot_ref
review_evidence.actual_images_viewed
dimensions[].dimension_id
dimensions[].status: pass / warning / fail
business_delivery_status: pass / pass_with_warnings / business_delivery_rejected
blocking_issue_codes[]
warning_codes[]
```

六个固定维度为 `information_hierarchy / delivery_title_quality / final_asset_binding / readiness_truthfulness / visual_human_review / action_usability`。存在任一 `fail` 时必须派生 `business_delivery_rejected`；未真实查看截图和最终图片不得填 `actual_images_viewed=true`。

## 44. R7-L3-H2 五段视觉语义字段

每个 current visual task 的语义阶段顺序固定为：

```text
visual_intent_decision
visual_source_route_decision
visual_prompt_brief（仅 generated_context；其他路线为 not_applicable）
visual_asset_review
delivery_visual_review
```

`visual_intent_decision` 必须绑定 `visual_task_id / beat_ref / account_snapshot_ref / input_binding_digest / producer_task_envelope_ref`，记录 `viewer_problem / value_jobs / counterfactual_loss / decision / no_visual_reason`。`no_visual` 只接受具体业务理由，成本、测试方便、没想到图或先跑通均无效。

`visual_source_route_decision.source_class` 在 `source_bound_evidence / explicit_existing_asset / generated_context` 中互斥。来源证据必须绑定 `claim_ref + evidence_requirement_ref`；既有资产必须绑定 exact-hash `asset_reuse_authorization_ref`；generated context 才允许 `production_path=codex_builtin_image2`。

`visual_prompt_brief` 只承载语义：`audience_problem / value_jobs / scene_and_subject / composition_and_attention_path / target_canvas_and_slot / brand_visual_constraints / truthfulness_constraints / forbidden_misrepresentation / text_layer_plan / reference_asset_bindings / required_postprocess_operations`。确定性编译器把它编译为：

```text
visual_prompt_package.full_prompt
visual_prompt_package.prompt_sha256
visual_prompt_package.provider_payload
visual_prompt_package.operation_registry_digest
visual_prompt_package.missing_operation_ids
visual_postprocess_plan.operations[]
```

未注册 operation 必须派生 `compile_status=waiting_capability`，不得现场创建 run-specific helper。Image 2 / capture 成功必须逐任务具备 `attempt_refs + outcome_ref + output_ref`；中断复用允许 `attempt_refs=[]`，但必须有 `reconcile_status=reused_existing_output` 和原 outcome/output。

`visual_asset_review` 的固定维度为：

```text
task_alignment
attention_composition
truthfulness
text_number_accuracy
crop_safe_area
small_screen_readability
brand_fit
artifact_integrity
```

它必须由 `visual_quality_reviewer` 实际查看 current raster，绑定当前 `asset_ref.sha256`；producer/reviewer task envelope 不得相同。`delivery_visual_review` 由 `delivery_reviewer` 实际查看 current delivery assets、final HTML、desktop/mobile screenshot，固定检查 `final_text_accuracy / crop_and_safe_area / insertion_context / platform_card_and_cover / duplicate_display / page_hierarchy`。查看 base-only 或旧 screenshot 不能通过。

两类 review 的 verdict 只有 `pass / revise / reject / not_applicable`。`revise` 必须写 `minimal_revision_target / owning_producer / stale_scope[]`；reviewer 只返回 typed request，不得顺手修改 producer 输出。

## 45. R7-L3-H3 直供 route current 合同

新直供 session 固定使用 `direct_delivery_single_v0.5 / session-execution-plan v1.1 / semantic-task-envelope v0.5`。v0.4 只允许历史 replay，不原地改节点顺序。

`visual_stage_set@0.1` 用于承载 0..N 个逐任务语义对象，字段为 `stage_set_id / set_revision / session_id / stage / source_coverage_ref / records[] / record_count / set_status / producer_task_envelope_ref / input_binding_digest / created_at / next_stage`。`record_count` 必须从 records 派生；stage 只能是 intent、source route、prompt brief、asset review。Prompt 集只包含 generated-context，其他来源不生成伪 Prompt。

直供视觉段顺序固定为：

```text
visual_need_analysis
-> visual_intent_decision_set
-> visual_source_route_decision_set
-> visual_prompt_brief_set
-> visual_production_l3
-> visual_asset_review_set
-> visual_asset_finalize_l3
```

`image_asset_set@0.4` 在生产后停于 review 前；0 图写 `no_visual_waiting_review` 且 assets、delivery_bindings、provider/capture/postprocess 计数均为 0。对应空 review set 必须是 `complete_no_visual`，finalizer 写 `image_asset_delivery_set@0.2` 和 `finalized_no_visual`。非 0 图必须由独立 review set 按 visual task 与 delivery asset hash 绑定后才能 finalize。

`waiting_capability / revision_required / revise` 是不推进状态：submission 保留，但不提交 current pointer，恢复后复用同一 input-digest task。scoped 视觉来源返修从 `visual_source_route_decision_set` 生成 plan revision 2，旧产物不覆盖，所有下游节点按新 revision 重跑。H3 脱敏 fixture 只证明合同、0 图、等待恢复与 replan；不证明真实 Image 2、网络 capture、私有账号、完整 HTML 或 L3。

## 46. R7-L3-H4 热点 route current 合同

新热点 session 固定使用 `hotspot_to_delivery_single_v0.5 / session-execution-plan v1.2 / semantic-task-envelope v0.6`。热点 v0.4 与 plan v1.0 只允许历史 replay，不得把旧绿灯解释为 current 合同通过。

热点入口先消费 current `account_identity_binding + account_snapshot + radar_policy`，由确定性节点提交 hash-bound `hotspot_research_request`；不得手工预制 future `hotspot_research_set / selected_topic_source / topic decision` 再倒填。研究外部能力未就绪时返回 `waiting_external`，不写 current pointer，恢复时复用同一 task 与 input digest。

热点稿通过 Topic Gate 后与直供稿共用 `visual_stage_set v0.1 / image_asset_set v0.4 / image_asset_delivery_set v0.2`。material fact update 从 `hotspot_content_brief` 开新 plan revision；reversal 或 identity change 先物化新的 revalidation request，再从 `hotspot_research` 重启；scoped visual-source revision 从 `visual_source_route_decision_set` 重启并重建全部受影响交付物。H4 离线 fixture 不证明网络、Image 2、私有账号、完整真实交付或项目 L3。

## 47. R8-H1 Skill context inventory

`skill_context_registry` 的 current 固定入口为 `routes/r8-skill-context-registry.yaml`。它登记项目内业务 Skill，不登记 Codex 系统 / 全局 Skill 或插件。顶层字段为：

```text
registry_id
schema_version
inventory_scope
inventory_status
skill_root
node_registry_ref
entry_policy.line_limit
entry_policy.warning_threshold
entry_policy.token_target
entry_policy.reference_depth_limit
skills[]
```

每个 `skills[]` 条目必须带：

```text
skill_id
skill_type: router / producer / reviewer / builder / compatibility / human_gate
user_invocable
current_contract_version
primary_input_artifact_type
primary_output_artifact_type
owned_node_ids[]
skill_entry_path
skill_entry_digest: sha256:<64hex>
entry_line_count
entry_line_limit: 500
entry_warning_threshold: 350
always_loaded_sections[]
conditional_references[]
legacy_references[]
machine_truth_refs[]
ownership_status: current / compatibility / superseded
embedded_legacy_status: absent / present_pending_extraction / whole_entry_compatibility
context_policy_status: compliant / warning_review_required / entry_limit_exceeded_pending_recompile
planned_context_batch
```

`conditional_references[]` 使用 `reference_id / path / load_when / content_owner`。`load_when` 只能由 `contract_version / node_id / status / mode / target_platforms` 的可判断表达式组成；“必要时”“相关时”等自然语言不合法。reference 必须位于对应 Skill 的 `references/` 直属层，并从 `SKILL.md` 一层直达。

H1 checker 从真实 `SKILL.md` 与 R7 node registry 派生行数、SHA256 和 owner，不信任登记值。超过 500 行的 current 入口只有明确写入 `entry_limit_exceeded_pending_recompile` 和后续批次时，才可作为**已识别编译债务**保留；它仍不满足 R8 最终 current 门禁。H1 报告的 `pass_with_warnings` 只证明 inventory 和拒绝路径可用，不证明 H2-H4 已完成入口收缩。

### R8-H4 `platform_package` 目标平台集合合同

current `platform_package` 的目标平台只从任务绑定的
`account_snapshot.captured_fields.publishing_platforms` 派生；不得从聊天文字猜测，
也不得在空集合时默认全部平台。

```text
target_platforms：非空、无重复、全部属于当前机器合同支持集合
primary_platform：必须属于 target_platforms
packages[].platform：非空、无重复
set(packages[].platform) == set(target_platforms)
packages.length == target_platforms.length
```

未选择的平台既不加载平台方法 reference，也不得生成空卡片或多余卡片。平台方法
reference 只改变知识加载，不新增 fan-out / fan-in：一个 current 语义节点仍只提交一个
`platform_package`。

当前 `platform-package-v0.2` 与下游 presentation registry 支持
`douyin / xiaohongshu / wechat_channels`。`kuaishou` 方法 reference 已隔离并可在
选中时加载，但在机器合同和下游展示能力未另行编译前必须返回明确阻断；不得静默丢弃。

## 48. R8-H5 v0.2 独立评估合同

H5 v0.2 的机器入口为 `routes/r8-h5-evaluation-contracts.yaml`，Schema 位于
`templates/schema/r8/h5/`。H5 v0.1 固定为 `invalid_evaluation`，只允许不可变
replay；旧匿名包不得恢复为 ready，也不得用于 current switch 结论。H5R5 的 current
编译状态为 `human_verdict_and_finalizer_compiled`：semantic case、
版本化 adapter、typed input、snapshot、sealed independent arm task、typed
submission、arm result recorder、machine evaluator、comparability producer、
私有 allocation commitment、匿名 review packet、human verdict recorder、
deterministic finalizer 与 finalization-only state projection 已可运行。本轮 current
evaluation 已完成 4 个盲评 verdict 和唯一 finalization，结论为
`current_switch_readiness=insufficient_samples`、`overall_result=fail`；这是运行证据，
不改变上述编译能力状态。

九类对象按以下顺序传递：

```text
h5_semantic_case
-> h5_dependency_snapshot[baseline | candidate]
-> h5_arm_input[baseline | candidate]
-> h5_arm_result[baseline | candidate]
-> h5_machine_verdict
-> h5_comparability_verdict
-> h5_blind_pair
-> h5_human_verdict
-> h5_evaluation_finalization
```

所有对象必须带 `schema_id / schema_version / evaluation_id / attempt_id` 和各自
对象 ID。原始对象不得原地修改；新修订必须使用新的 `attempt_id`，并通过
`supersedes` 指向旧对象。`evaluation_id` 与 `attempt_id` 由调用方先物化，路径从
这两个 ID 派生，不使用 `CURRENT`、日期常量或文件 mtime 补位。

### `h5_semantic_case v0.2`

关键字段：

```text
semantic_case_id
skill_id
case_class: normal / conditional_or_resume / rejection
prompt_text
semantic_input
shared_outcome_invariants[]
expected_primary_output_type
semantic_case_digest
case_status: ready / invalid / superseded
supersedes
```

### `h5_dependency_snapshot v0.2`

关键字段：

```text
snapshot_id
semantic_case_id
arm_role: baseline / candidate
isolation_level: instruction_isolated / read_audited / capability_isolated
source_commit
entry_skill_path
files[].relative_path / role / sha256
closure_digest
snapshot_status: ready / invalid / superseded
supersedes
```

当前 Codex 独立任务的声明上限是 `instruction_isolated`；没有读取审计或能力沙箱
证据时不得升级术语。

### `h5_arm_input v0.2`

关键字段：

```text
arm_input_id
semantic_case_id
arm_role
skill_id
semantic_case_ref / semantic_case_digest
dependency_snapshot_ref / dependency_snapshot_digest
adapter_id / adapter_version
artifact_type / contract_version / input_schema_ref
typed_input_relative_path / input_digest
input_status: ready / invalid_input / superseded
validation_errors[]
supersedes
```

baseline 与 candidate 共享 `semantic_case_digest`，但分别由对应版本 adapter 生成
canonical typed input；二者字段不要求字节相同。

H5R2 的 adapter 真源为 `routes/r8-h5-arm-adapters.yaml`，六个直接输入 Schema 位于
`templates/schema/r8/h5/inputs/`。三个目标 Skill 各有 baseline / candidate 两个
adapter；共享的是 `semantic_case_digest / semantic_projection_digest /
shared_outcome_invariants`，直接消费字段按各自合同版本独立生成。dependency snapshot
分别固定 baseline commit `716920e / 5403528 / 40ce0b0` 与 candidate current HEAD，
冻结 entry、CONTRACT、一层 references/assets 和 entry/CONTRACT 直接引用的机器真源。
同一 evaluation/attempt 的重复生成必须 byte-stable；内容变化必须新 attempt 或明确
报 `immutable_conflict`，不得覆盖。

### `h5_arm_result v0.2`

结果状态固定为：

```text
produced_business_artifact
waiting_valid
blocked_valid
execution_error
invalid_input
invalid_output
```

关键客观字段：

```text
prompt_digest / input_digest / snapshot_digest / output_digest
requested_node_id / selected_node_id / executed_node_id
reference_load_observation / loaded_reference_ids[]
manual_assist_observation / manual_assist_count
duration_ms
token_observation / input_tokens
business_artifact_ref
business_output_schema_gate
contract_selection_result
```

`produced_business_artifact` 必须同时携带业务 artifact ref、output digest 和
`business_output_schema_gate=pass`。其他状态禁止携带业务 artifact ref。
`not_observable` 对应计数必须为 null，不能用自报 0 冒充观察证据。

H5R3 的 recorder 请求 Schema 是
`templates/schema/r8/h5/requests/h5-arm-result-record-request.v0.1.schema.json`，
业务输出映射与共享语义 validator 真源是
`routes/r8-h5-machine-evaluation.yaml`。recorder 只能消费已存在的 arm-input 和
dependency snapshot；它必须重新读取业务输出、计算当前文件 SHA256、执行注册的
业务输出 Schema，并由 registry 派生 artifact type / artifact ID。执行臂自报的
success、hash、Schema gate 或质量结论均不是可信输入。

当前 router 历史合同没有独立机器 Schema，因此 H5 评估层使用
`templates/schema/r8/h5/business/router-decision.v0.1.schema.json` 作为 baseline /
candidate 共同的最小可比输出合同；它不改变 router 的项目职责，也不授权执行节点。
热点研究继续使用 `hotspot-research-set.v0.1`，平台 baseline / candidate 分别使用
`platform-package.v0.1 / v0.2`。

### verdict、blind 与 finalization

`h5_machine_verdict` 分开记录 baseline contract、candidate contract 和 shared
invariant 三类结果。`h5_comparability_verdict` 的状态固定为：

```text
comparable
behavior_only
regression_disposition
insufficient_comparable_samples
invalid
```

只有机器结论为 pass、双方均产生 Schema-valid 且同语义类型主产物时，才允许
`comparability_status=comparable` 与 `blind_pair_allowed=true`。rejection、
waiting、blocked 和 route / gate result 只进入机器审计。

H5R3 的 false-success 硬阻断至少覆盖：produced 但文件缺失、业务 Schema 失败、
文件 hash 在记录后变化、artifact type / ID 不匹配、nonproduced 却携带业务产物、
rejection 产生业务产物或 selected/executed node、`not_observable` 却写数字、
机器失败仍尝试 comparable / blind eligibility、输入或输出路径越界。只有
`comparability_status=comparable` 才允许 `blind_pair_allowed=true`。H5R4
还必须重新校验 current arm result、业务产物 hash、machine verdict 与 comparability
verdict，才可生成匿名包；machine fail、noncomparable 或 result tamper 均必须阻断。

H5R4 的执行 task 与 submission 分别使用
`h5-arm-execution-task.v0.1` 与 `h5-arm-execution-submission.v0.1`。baseline /
candidate 使用彼此隔离的 role 目录；task 固定 source commit、snapshot digest、
typed input digest、可读文件和业务输出 Schema。submission 只提交结果状态、业务
产物路径、执行观察与错误，不允许执行臂自报 machine pass、comparability 或匿名
资格。orchestrator 单独记录补充消息次数，执行臂不能把它写成 0。

`h5_blind_pair` 只包含 A / B 的 `artifact_type / presentation / content_digest`。
角色映射使用独立 `allocation_record_ref`，不得投影进人类评审表面。机器失败时
`pair_status` 只能是 `quarantined` 或 `superseded`，不能是 `ready`。

allocation 实例写在本地私有 evaluation 证据区，使用随机 nonce 和 commitment
digest；公开源码只保存其 Schema。匿名 review packet 不包含 arm role、source
commit、snapshot、allocation mapping 或可反推身份的路径。匿名投影必须保持原始
业务产物的 object / array / scalar 拓扑；空数组、单元素数组或字符串不得被
PowerShell success pipeline 折叠为 `{}`、标量或 `{"Length": n}`。拓扑不一致时
packet producer 必须阻断，旧 evaluation 保留为不可评证据并新建 evaluation/attempt。

`h5_human_verdict.choice` 只允许 `A / B / tie`。`h5_evaluation_finalization` 是
`current_switch_readiness` 的唯一 producer；aggregate checker 与 state projection
只消费 finalization。readiness 为 `passed` 时必须同时满足：

```text
all_machine_gates_pass = true
all_rejection_cases_fail_closed = true
human_verdicts_complete = true
三个目标 Skill 的 comparable count 均至少为 1
所有可比案例的人类结果为 candidate 或 tie
```

human verdict recorder 只读取匿名 pair，不读取 allocation；它重新校验 A/B
presentation digest，并把用户的 `A / B / tie + business_reason` 写成不可变 verdict。
finalizer 在 verdict committed 后才读取私有 allocation，把 A/B 解析为
candidate / baseline，并按以下优先级产生唯一结论：

```text
缺 human verdict -> waiting_human
任一目标 Skill comparable count < 1 -> insufficient_samples
machine fail / rejection 未 fail-closed / 任一 baseline 胜出 -> failed
其余全部条件满足 -> passed
```

`h5_evaluation_state_projection v0.1` 只能逐字投影 finalization 的 readiness、
overall result 和 blocker，并绑定 finalization digest；不得重新计算或覆盖结论。

H5R1 编译对象、状态、兼容和正反 fixture；H5R2 实现 semantic case、双版本
adapter 与 dependency snapshot producer；H5R3 实现 recorder、machine evaluator、
comparability 和 false-success 防护；H5R4 实现独立执行 task/submission、机器复核、
私有 allocation 与匿名包；H5R5 实现 human verdict recorder、deterministic
finalizer 和 finalization-only state projection。实现完成不等于人类评审完成：
未提交匿名选择时必须停在 `waiting_human`，router 无可比样本必须保留为
`insufficient_comparable_samples:propagation-router`，不得宣称 current switch。

## 49. M4/M5/M5.1 session 代际与兼容隔离合同

`kernel_session_runtime_binding v0.1` 是 M4 已提交 session 的历史合同，固定字段为：

```text
schema_id / schema_version / architecture_change_id
binding_id / session_id / route_id / runtime_generation
workflow_ir_id / workflow_ir_sha256
component_catalog_sha256 / compatibility_catalog_sha256
architecture_control_sha256 / switch_policy_id
selection_reason / rollback_state_at_creation / runtime_entry_ref
created_at / immutable / migration_allowed
```

新 session 必须先写
`intermediate/workflow-kernel/session-runtime-binding.json`，再以该文件精确
SHA256 写 `session-runtime-binding.sha256`；marker 是物理 commit 点。缺任一文件、
hash 不一致、未知字段或身份 / route 不一致都属于合同阻断。已提交 binding
不可重写，`migration_allowed` 固定为 false。

`kernel_session_entry_decision v0.1` 只表达本次 start / resume 的路由结果：
`runtime_generation` 为 `legacy_r7 | kernel_v1_current`，`binding_status` 为
`created | reused | legacy_inferred_read_only`。M4 激活时新 session 默认
`kernel_v1_current`；回滚只让未来新 session 选择 `legacy_r7`。已有 binding
始终按原代际续跑。没有 binding 但存在 version-pinned R7 plan 的旧 session
只读推断为 legacy，不补写 binding，不修改原 plan。

`runtime_certification` 在 M4 固定为 `not_run`。M4 fixture 通过只能证明代际选择、
不可变提交、legacy 续跑与回滚边界，不能证明 semantic worker、真实外部 activity、
私有账号或 L3。

M5 新建 binding 使用 `kernel_session_runtime_binding v0.2`。公共字段与 v0.1
保持一致，但按 `runtime_generation` 收紧：

```text
kernel_v1_current：必须省略 compatibility_catalog_sha256
legacy_r7：必须携带 compatibility_catalog_sha256
```

current IR、current component catalog 和 current binding 均不得携带 legacy
blueprint / node / step 投影。version-pinned 旧 plan 与版本钉住的 compile-time
parity 输入只允许
`WorkflowCompatibilityLoader.ps1` 解析为
`kernel_compatibility_resolution v0.1`；解析结果固定包含 blueprint、route、
catalog digest、`read_only=true`、`migration_allowed=false`。current caller、
未知 blueprint、route 不匹配或未登记资产必须 fail-closed。binding 的
`runtime_generation` 与 `runtime_entry_ref` 必须属于同一代际。旧 v0.1 binding
继续按原字节恢复，不原地升级到 v0.2，也不改写旧 plan。

M5.1 允许仍有消费者的历史资产迁入 `compatibility/legacy-r7/` 做目录级归档。
catalog 必须登记每个资产、固定共享 action 快照摘要、保留原 runtime/CLI 稳定
shim，并且只允许 loader 解析。当前基线为 15 项数据合同、2 个实现文件、2 个
shim；退休或删除为 0。只有消费者计数归零、历史 replay 门禁通过且另有明确决定
后才能退休或删除。M5 fixture 只证明加载边界、只读解析、目录迁移和代际绑定合同，
不等于 runtime certification；后者仍是 M6 的独立任务。

## 50. M6 direct same-digest certification 合同

`m6_direct_certification_request v0.1` 由
`M6DirectCertificationRuntime.ps1` 从版本化 fixture catalog 确定性生成，
固定字段为：

```text
schema_id / schema_version / certification_case_id
session_id / route_id / runtime_generation / source_revision
initialized_at / waiting_at / resumed_at / completed_at
input
component_results[]
```

`component_results[]` 必须与 current direct route 的 25 个 component slot
逐项同序，且每项带：

```text
stage_id / component_id / component_kind
artifact_id / artifact_revision
result_status / output_contract_ref / occurred_at
validation_receipt
fixture_adapter_id
payload_kind / payload | payload_relative_path
```

只允许 `fixture_adapter_id=m6-direct-fixture-adapter-v0.1`。它表达确定性测试
输入，不等于真实 semantic worker、provider 或人工决定。Runtime 是 event、
artifact、projection、run-state 和 writer ledger 的唯一写入者。

`m6_direct_certification_report v0.1` 固定包含：

```text
suite_id / mode / source_revision
result / certification_status
freeze_before_sha256 / freeze_after_sha256 / freeze_stable
prerequisite_evidence
direct_route
assertion_count / assertion_pass_count
negative_count / negative_pass_count
certified_scope / excluded_scope / boundaries
underlying_evidence / errors
```

`CompileSmoke` 的 `certification_status` 只能是
`not_run_compile_smoke_only`。正式 `certified` 必须满足：

```text
clean tracked worktree
source revision == HEAD
evaluator 与 runtime 报告均 certified
evaluator / runtime / direct 使用同一 source revision 和 freeze digest
freeze before == freeze after
current v0.2 binding 与 marker 有效
25 个组件槽位全部接受
第一次推进 waiting_human
typed fixture resume 后 completed
projection rebuild byte-stable
completed replay 不新增 event
unexpected_writer_count = 0
6 个 false-success 负例全部 fail-closed
```

认证范围只覆盖 direct 控制面。真实账号、语义质量、网络、provider、真实人工
决定、hotspot route 和项目 L3 必须分别保留为未认证。
