param([string]$ProjectRoot='',[string]$ReportPath='')
$ErrorActionPreference='Stop';if([string]::IsNullOrWhiteSpace($ProjectRoot)){$ProjectRoot=Split-Path -Parent $PSScriptRoot};$ProjectRoot=[IO.Path]::GetFullPath($ProjectRoot)
. (Join-Path $ProjectRoot 'tools/WindowsRuntimeHelper.ps1')
$work=Join-Path $ProjectRoot 'state/checks/r7-body-rendition-fixture';if(Test-Path $work){Remove-Item -LiteralPath $work -Recurse -Force};New-Item -ItemType Directory -Force -Path "$work/session/assets/images"|Out-Null
Add-Type -AssemblyName System.Drawing;$bmp=New-Object Drawing.Bitmap(640,400);$g=[Drawing.Graphics]::FromImage($bmp);$g.Clear([Drawing.Color]::SlateGray);$g.Dispose();$bmp.Save("$work/session/assets/images/base.png",[Drawing.Imaging.ImageFormat]::Png);$bmp.Dispose()
$hash=(Get-FileHash "$work/session/assets/images/base.png" -Algorithm SHA256).Hash.ToLowerInvariant();$request=[ordered]@{schema_id='taoge://schemas/r7/body-image-rendition-request/v0.1';schema_version='0.1';operation_id='crop_fit_pad';session_id='S-BODY-001';rendition_id='REN-BODY-001';source_asset_ref=[ordered]@{artifact_id='BASE-001';relative_path='assets/images/base.png';sha256='sha256:'+$hash};target_canvas=[ordered]@{width_px=1080;height_px=1920};output_relative_path='assets/images/renditions/body-r001.png';record_relative_path='assets/images/records/body-r001.json';requested_at='2026-07-17T00:00:00Z'}
$requestPath="$work/request.json";Write-TaogeUtf8NoBomJson -Path $requestPath -Value $request -Depth 12;$script=Join-Path $ProjectRoot 'tools/invoke-r7-body-rendition.ps1';$first=& $script -SessionRoot "$work/session" -RequestPath $requestPath 2>&1;$one=$?;$second=& $script -SessionRoot "$work/session" -RequestPath $requestPath 2>&1;$two=$?;$output="$work/session/assets/images/renditions/body-r001.png";$record="$work/session/assets/images/records/body-r001.json";$size=[Drawing.Image]::FromFile($output);$dimension=($size.Width-eq1080-and$size.Height-eq1920);$size.Dispose();$report=[ordered]@{result=$(if($one-and$two-and(Test-Path $output)-and(Test-Path $record)-and$dimension-and([string]::Join(';',$second)-match'BODY_RENDITION_ACTION=reused_verified')){'pass'}else{'fail'});case_count=4;cases=@('render','target_canvas','record','reconcile');first=[string]::Join(';',$first);second=[string]::Join(';',$second)};if([string]::IsNullOrWhiteSpace($ReportPath)){$ReportPath=Join-Path $ProjectRoot 'state/checks/r7-body-rendition-report.json'};Write-TaogeUtf8NoBomJson -Path $ReportPath -Value $report -Depth 10;if($report.result-ne'pass'){throw 'r7_body_rendition_fixture_failed'};Write-Output 'R7_BODY_RENDITION_CHECK=pass'
